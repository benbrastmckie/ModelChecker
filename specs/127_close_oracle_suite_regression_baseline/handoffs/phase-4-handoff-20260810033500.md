# Phase 4 Handoff: Exhaustive Scan Completed on Third Attempt

**Date:** 2026-08-10
**Phase 4:** `[COMPLETED]` — third launch reached `SCAN_COMPLETE`; attempts 1 and 2 (session 2)
remain recorded as unadjudicable environmental non-results.
**Task 127 overall:** all 8 phases now `[COMPLETED]` or terminal. Gating oracle suite remains RED
under rubric category (c) — the task's honest, accepted recorded outcome, not an open item.

## Phase 4: three launches, third completed

| Attempt | Window | Quiet at launch | Outcome |
|---|---|---|---|
| 1 | 2026-08-09 22:54:17-23:06:28Z | load 1.05-1.14, no foreign hog | Contention at +85s (cslib `lean --worker` 291-467%, `lake` 794.8%). Aborted at formula 14/274. |
| 2 | 2026-08-09 23:57:44Z-~01:07Z | load 0.57-0.76, zero lean/lake | Clean ~46 min through formula ~215, then sustained `lean --worker` contention from 00:47. Terminated at formula 234/274. |
| 3 | 2026-08-10 02:20:56Z-03:29:12Z | load1 0.97, no foreign process >50% CPU | **Completed.** `SCAN_COMPLETE` written. 9/68 60s samples showed transient contention (peak `lean` 577%, `runLinter` 772%); did not invalidate the run. |

`SCAN_COMPLETE` (attempt 3): `total_formulas=274, conclusive=105, disagreements=0,
timeout_count=169, wall_clock_seconds=3555.065`.

## Two slow tests, one passed one failed

`pytest oracle -m slow -s` runs both `slow`-marked tests together:

- `TestFullScanReport::test_complexity_5_scan_self_consistent` (produces `report.json`/
  `SCAN_COMPLETE`) — **PASSED**. 105 conclusive >= 90-formula floor, 0 disagreements.
- `TestBimodalHarnessIntegration::test_temporal_only_agreement_complexity_5` — **FAILED** on its
  signature-check assertion. A formula's `external_bh_defect` classification has the *opposite*
  polarity (`mc_sat=True, bh_sat=False`) from `KNOWN_EXTERNAL_DEFECTS.md`'s documented signature
  (`mc_sat=False, bh_sat=True`). Genuine, non-environmental finding — a content check, not a
  timing outcome. Not investigated further here (out of this task's scope); recorded as a
  follow-up.

This is why the runner's aggregate summary reports `pytest: FAILED (exit 1)` even though the
coverage-record test itself passed and the marker is present — a mixed outcome the plan's two
pre-declared verification branches did not anticipate. Recorded explicitly, per test, in
`specs/126_refactor_repo_core_infrastructure_theory_lib/baselines/exhaustive-scan/STATUS.md`
rather than forced into either branch.

## Contention caveat

`specs/126_refactor_repo_core_infrastructure_theory_lib/baselines/gate-run-2026-08-09/exhaustive-attempt3-contention-watch.log`
(68 samples @60s, 58 quiet / 9 contention). The 169/274 timeout count is recorded as an
observation made under partial contention — an upper bound, not a clean characterization of
solver capability at `SELF_SCAN_SOLVE_TIMEOUT_MS=10000`. The 0-disagreements finding is
unaffected (scoped to the 105 formulas both sides actually decided).

## Evidence

- `specs/126_.../baselines/exhaustive-scan/{report.json,SCAN_COMPLETE,exhaustive-run.txt,STATUS.md}`
- `specs/126_.../baselines/gate-run-2026-08-09/exhaustive-attempt3-{machine-before.txt,contention-watch.log}`
  (README updated)
- `specs/126_.../baselines/oracle-baseline-STATUS.md` — exhaustive-scan section rewritten
- `specs/127_.../plans/02_rebaseline-gating-oracle-suite.md` — Phase 4 heading -> `[COMPLETED]`,
  plan-level Status -> `[COMPLETED]`
- `specs/127_.../summaries/02_rebaseline-gating-oracle-suite-summary.md` — Session 3 section added

## Integrity

`git diff --stat -- oracle/bimodal_logic/` empty. `SELF_SCAN_SOLVE_TIMEOUT_MS`,
`MIN_CONCLUSIVE_SCAN_FORMULAS`, `MIN_CONCLUSIVE_TEMPORAL_BH_FORMULAS`,
`known_conclusive_complexity5.json`, `ORACLE_EXHAUSTIVE_TIMEOUT` all untouched. Nothing
reclassified to make anything pass.

## State at handoff

- Phases 1-8 all `[COMPLETED]`.
- Gating oracle suite: RED, category (c), as established in session 2. Unaffected by Phase 4.
- Exhaustive scan: `SCAN_COMPLETE` present and adjudicated; the coverage-record test passed; a
  separate, genuine new finding on a co-run test needs follow-up.

## Follow-up (carried forward, not this task's scope)

1. Investigate the opposite-polarity `external_bh_defect` signature — is it a second, distinct
   external BimodalHarness defect requiring its own `KNOWN_EXTERNAL_DEFECTS.md` entry?
2. `test_mixed_and_all_future_neg` (new) `OracleTimeoutError` at 60000ms — failing set grew 1->2
   across two quiet-machine gating runs on an unchanged tree.
3. `BM_CM_1`/`example_case7` — deterministic, fast, in-package failure; also causes Step 7's
   misleading "reported regressions" via `set -euo pipefail`.
4. `test_temporal_propositional_interleaving` does not terminate within 900s.
