# Phase 2 Handoff: Gating-Suite Baseline Run Into Staging

**Status:** COMPLETED (the run completed and was adjudicated; the suite itself is RED)
**Date:** 2026-08-09

## Reuse decision

Attempt 1's artifacts were **not** reused; Phase 2 was relaunched in full. Rationale recorded in
`run2/phase2-reuse-decision.md` — attempt 1 had no `exit-code.txt`, no runner summary block, and
only one of two per-pass JUnit files, failing three of Phase 2's five completeness clauses.
Splicing its pass-1 XML onto a later pass-2 run would have produced a "run" that never existed as
one execution. Attempt 1's files are retained under an `attempt1-` prefix as corroborating
evidence.

## Quiet-machine evidence

**Correction notice.** An earlier revision of this section reported the pass-2 re-run as running
on a verified-quiet machine. That was wrong. The live display filter used while reading the
capture files selected only the section-label lines and so hid the CPU-hog lines the captures had
correctly recorded. The captures themselves were always right; the reading of them was not. The
table below is a re-audit of the raw files, and the conclusions drawn downstream have been
revised accordingly rather than left standing on the mistaken reading.

| Capture | File | 1-min load | Foreign >50% CPU / z3 / lean |
|---|---|---|---|
| Before main run (4 samples) | `run2/machine-before-phase2.txt` | 0.34–0.56 | **none** |
| After main run (3 samples) | `run2/machine-after-phase2.txt` | 1.85–2.22 | **none** (the one >50% hit is the capture's own `ps`; the six "lean" string hits are the grep pattern echoed inside the capture command line, not processes) |
| Before pass-2 re-run (3 samples) | `run2/machine-before-pass2-rerun.txt` | 1.16–1.85 | **CONTENDED — `lean` at 61.7% (sample 2) and 110% (sample 3)** |
| After pass-2 re-run (3 samples) | `run2/machine-after-pass2-rerun.txt` | 1.28–1.44 | none |
| Before Phase 3 isolated re-run (6 samples) | `run2/machine-before-phase3.txt` | 1.38–2.86 | **CONTENDED — `lean` at 493% and 100% (sample 6)** |
| After Phase 3 isolated re-run (3 samples) | `run2/machine-after-phase3.txt` | 12.87–13.63 | **HEAVILY CONTENDED — `runLinter` at 770–776%, `lean` workers at 121–190%** |

The contender is a Lean lint job (`cslib/.lake/packages/batteries/.lake/build/bin/runLinter`,
plus `lean --worker` processes) belonging to a sibling project — the exact `lean --worker`
contention this plan names as documented and expected on this machine.

**Consequence, applied rather than noted:** the pass-2 re-run and the Phase 3 isolated re-run are
**unadjudicable** under the plan's own rule ("Not quiet after, on a failing run: the failures are
unadjudicated ... A contended failing run is not evidence of anything and must not be promoted,
triaged, or reported as a result"). Their results are recorded below as observations but are
**not** used as evidence for any verdict. The main run is unaffected: it was quiet before and
after, and it alone carries the adjudication.

The after-main-run 1-minute average briefly exceeded the plan's 2.0 convenience threshold
(2.22 at the first sample, 24 seconds after our own `-n 6` pass ended). This is the decay tail of
this task's own six workers, not foreign contention: the CPU-consumer criterion the plan actually
states ("no process other than this task's own run exceeds ~50% CPU — in particular no foreign
`pytest`, no `z3`, no `lean --worker`") was satisfied at every sample, and the top consumers were
agent-session processes at 2.8–17.7%. It is recorded rather than smoothed over. The pass-2 re-run,
which is the run that carries the decisive timeout finding, was quiet on both criteria at both
ends.

## What ran

Main run (`run2/oracle-run.txt`, `run2/exit-code.txt` = `exit=1`), 13:29:58 → 13:54:26 (24m28s):

```
== oracle suite summary (gating: slow deselected on both passes) ==
pass 1 (parallel, -n 6, not xdist_serial and not slow, budget 1300s): FAILED (exit 1)
pass 2 (serial, xdist_serial and not slow, budget 900s):             TIMED OUT (exit 124)
```

- **Pass 1**: `1 failed, 586 passed, 3 skipped, 4 xfailed in 567.62s`. 594 testcases in
  `run2/junit-oracle-pass1.xml`. Byte-identical verdict to attempt 1 (`579.39s` there), same
  single failing test — a second independent observation.
  - Only failure:
    `test_oracle_interface.py::TestMixedFormulas::test_mixed_and_box_next` —
    `OracleTimeoutError: Z3 solver did not decide the formula within 60000 ms
    (temporal_depth=1, time_bound M=3)`.
  - JUnit skipped-tag breakdown `{'pytest.skip': 3, 'pytest.xfail': 4}`: all four strict xfails
    still xfailed. **No strict-xfail XPASS.**
- **Pass 2**: SIGTERM'd by the runner's `timeout --kill-after=60s 900` wrapper after completing
  7 of 10 tests. Because pytest writes `--junitxml` only at session teardown, **no pass-2 JUnit
  file was produced** by either pass-2 execution.

Sanctioned pass-2 re-run (`run2/pass2-rerun.txt`, exit `124`), 13:58:06 → 14:13:07, launched per
Phase 2's own rule that "a timed-out pass is not a result — re-run it on a quiet machine". The
invocation was copied verbatim from the runner's pass 2 (same marker expression, same 900s
budget, same `timeout --kill-after=60s`, no `-n`); **the budget was not raised**. Result:

| # | Test | Main run | Re-run |
|---|---|---|---|
| 1 | `test_boundary_regression.py::TestExampleRegression::test_regression_all_active_examples[BM_CM_1-example_case7]` | PASSED | **FAILED** |
| 2 | `test_cross_oracle_differential.py::TestGatingConclusiveScan::test_known_conclusive_population_self_consistent` | **FAILED** | **FAILED** |
| 3 | `test_oracle_interface.py::TestEnrichedRoundTrip::test_enriched_vs_primitive_sat_agreement[some_past]` | PASSED | PASSED |
| 4 | `test_oracle_interface.py::TestMixedFormulas::test_mixed_or_diamond_prev` | PASSED | PASSED |
| 5 | `test_oracle_interface.py::TestSpotCheckCrossSignal::test_spot_check_individual_countermodels` | **FAILED** | **FAILED** |
| 6 | `test_soundness_regression.py::TestStateIsolationRegression::test_100_calls_mixed_temporal_depths` | PASSED | PASSED |
| 7 | `test_soundness_regression.py::TestStateIsolationRegression::test_sat_unsat_interleaving_stability` | PASSED | PASSED |
| 8 | `test_soundness_regression.py::TestStateIsolationRegression::test_temporal_propositional_interleaving` | **HUNG → SIGTERM** | **HUNG → SIGTERM** |
| 9–10 | `test_no_semantics_reference_leak_with_temporal`, `test_oracle_m_formula_depth1_boundary_safe` | never reached | never reached |

Pass 2 timed out at the same test both times. The re-run, however, was launched with a `lean`
process at 61–110% CPU (see the correction notice above), so **the re-run does not count as the
clean confirming run** and its verdict is not used as evidence. No budget was touched in either
execution.

The adjudication does not depend on it. Phase 3's category (c) trigger list includes "a pass
classified `TIMED OUT` on a verified-quiet machine", and the **main run** satisfies that clause on
its own: its pass 2 was classified `TIMED OUT (exit 124)` and its machine was verified quiet by
both criteria before and after.

Failure *reasons* for the three pass-2 failures are not recoverable from these runs: pytest emits
tracebacks in its summary section, which the SIGTERM pre-empted. Phase 3's bounded isolated re-run
is what will supply them.

## Deviations

1. **JUnit merge not performed.** `run2/merge-junit.py` was written as planned, but there is no
   pass-2 XML to merge (see above). Writing pass 1's 594-test XML out under the name
   `junit-oracle.xml` would misrepresent a partial as the 604-test gating report, so it was
   deliberately not done.
2. **Pass 2 was re-run on its own** rather than by re-running the whole two-pass runner. This is
   the action Phase 2's verification clause specifies verbatim for a timed-out pass. It does not
   produce a spliced *baseline* artifact, because nothing here is being promoted — the outcome is
   category (c) and the record is the red run itself.
3. After-run load average threshold, recorded above rather than waved through.

## Nothing was weakened

`git diff --stat -- oracle/bimodal_logic/` is empty: no oracle test file, assertion, budget,
floor, marker, or guard was touched. The only `oracle/` change is the additive, opt-in
`ORACLE_JUNIT_DIR` hook in `run-oracle-suite.sh`, which is inert when the variable is unset.

## Next phase

Phase 3, and the rubric category is determined: **(c) — a failure that reproduces on a quiet
machine**, established by the main run alone:

1. **Pass 2 classified `TIMED OUT (exit 124)` on a verified-quiet machine.** This is an explicit
   category (c) trigger and it is met by the main run, whose before/after captures are clean.
2. **Pass 1's `test_mixed_and_box_next` failure is reproduced across two independent quiet-machine
   runs** (attempt 1 and the main run), with an identical `OracleTimeoutError` at 60000 ms.

Two further observations point the same way but are **not** counted as evidence, because the runs
that produced them were contended: the pass-2 re-run's repeat timeout, and the isolated re-run in
which all four failing node IDs — including `test_spot_check_individual_countermodels`, which the
plan pre-registered as category (c) "by definition" on the strength of
`specs/137_investigate_mc_bh_resolved_and_wrong_disagreements/summaries/01_record-external-bh-boundary-defect-summary.md`
— failed again. They are logged for the next dispatch to re-derive on a quiet machine, not
relied upon here.

Phase 3 therefore takes the honest-recording path: write
`baselines/oracle-run-RED-2026-08-09.txt` and `baselines/oracle-baseline-STATUS.md`, do not
promote or overwrite the committed partial `baselines/oracle-run.txt`, skip Phase 6's marker flip
in favour of the framing correction, and expect Phase 7's `verify-refactor.sh` to fail at Step 6.
