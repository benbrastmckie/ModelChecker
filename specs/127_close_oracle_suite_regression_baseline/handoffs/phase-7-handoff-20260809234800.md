# Phase 7 Handoff: Independent `verify-refactor.sh` Confirmation With Step 6 Live

**Status:** COMPLETED. Ran, adjudicable, recorded. Expected-RED under category (c), and the RED is
*larger* than previously recorded.
**Date:** 2026-08-09, 23:16:57–23:45:56Z (28m59s)

## What ran

`nix develop --command bash -c 'bash code/scripts/verify-refactor.sh'` — no `--skip-oracle`, no
`PYTEST_ADDOPTS`. Exit **1**, `3 check(s) FAILED` (Steps 4, 6, 7). **Step 6 executed, not
`SKIPPED`.** Output: `run2/verify-refactor.txt`.

Then `bash code/scripts/verify-refactor.sh --skip-oracle` separately: exit **1**,
`2 check(s) FAILED` (Steps 4, 7). Output: `run2/phase7-verify-refactor-skip-oracle.txt`.

## Results

- **Steps 1, 2, 3, 5**: all OK, including all four re-pinned sub-counts and all of 5a–5d.
- **Step 4**: FAILED on both attempts, sole failing test `test_example_cases[BM_CM_1-example_case7]`
  (`1 failed, 297 passed`, 124.96s / 153.23s). Exactly the adjudicated defect, no other test.
- **Step 6 pass 1**: `2 failed, 584 passed, 4 skipped, 4 xfailed in 549.76s`.
  - `test_mixed_and_box_next` — adjudicated failure, third reproduction.
  - `test_mixed_and_all_future_neg` — **NEW**, in no earlier record (verified zero occurrences in
    `oracle-run-RED-2026-08-09.txt`). Same mode: `OracleTimeoutError`, 60000 ms, depth=1, M=3.
- **Step 6 pass 2**: `TIMED OUT (exit 124)` at the unchanged 900 s budget, as before.
- **Step 7**: FAILED — but as a *consequence* of the same defect. `compare_bimodal_baseline.sh`
  runs under `set -euo pipefail`; its first pytest pipeline exits non-zero on
  `BM_CM_1-example_case7`, so the script aborts after printing `Running bimodal test suite...`
  and never reaches its comparison logic. Its "reported regressions" message is misleading here.

## Machine quietness — adjudicable

Before 1.09–1.11; during, continuous 60s sampling 23:27:18–23:45:59 plus a manual capture at
23:26:55; after 3.38–4.39. **No foreign compute job at any sample.** The load1>6 readings between
23:23 and 23:28 are this run's own `-n 6` workers — the 23:26:55 capture records four `python3`
xdist workers at 99.7% each while every `lean` process sat at ≤5.9% lifetime-average and none
appeared in the instantaneous top-20. Captures: `run2/machine-{before,during,after}-phase7.txt`,
`run2/contention-watch.log`.

Deviation: idle cslib `lean --worker` LSP processes were present throughout, which the quietness
rule names categorically. Adjudicated non-contending **on measurement**, not by assumption.

## Deviations

1. **The "confirm the failure is … not a new one" criterion is NOT met.** Step 6 pass 1 grew from
   1 failure to 2 across two quiet-machine runs on an unchanged tree. Recorded as an enlargement
   of the category (c) finding. Nothing widened or relaxed in response.
2. **Phase 7 was run before Phase 4** — shorter run, short quiet windows, and the phases are
   independent (7 depends on 5 and 6; 4 on 3). Ordering choice, not a dependency violation.
3. `All checks passed` / exit 0 not achieved — the expected category (c) outcome, reported as
   failing rather than engineered away.

## Bonus: two previously-unadjudicable failures are now adjudicated

Pass 2's progress line, mapped against the established collection order, gives:
`test_known_conclusive_population_self_consistent` **FAILED** and
`test_spot_check_individual_countermodels` **FAILED** on a verified-quiet machine — both were
explicitly left open before. `test_regression_all_active_examples[BM_CM_1-example_case7]` FAILED
here having PASSED in the first adjudicable run, now consistent with the in-package Step 4 failure
on the same example. **This obviates the separately-suggested re-derivation run.**

## Not done

`known_conclusive_complexity5.json` not re-derived; `SELF_SCAN_SOLVE_TIMEOUT_MS` untouched; no
budget, floor, marker, guard, or xfail strictness changed. `git diff --stat -- oracle/bimodal_logic/`
is empty. `BM_CM_1`/`example_case7` not repaired (explicit non-goal).

## Next

Phase 4 retry (needs a 60–90 min quiet window; attempt 1 was aborted-unadjudicable at ~85s when a
sibling cslib Lean/lake job hit 291–794% CPU), then Phase 8 cleanup.
