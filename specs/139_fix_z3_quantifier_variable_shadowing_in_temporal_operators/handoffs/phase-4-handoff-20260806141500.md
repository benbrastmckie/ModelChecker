# Phase 4 handoff: rewrite the tests that enshrine the defect

**Status**: COMPLETED

## What was done

- Rewrote `test_gg_p_returns_none`, `test_gg_p_returns_none_at_m4`, and (newly discovered
  affected, not in the plan's original list) `test_gg_p_spurious_unsat` to
  `pytest.raises(OracleTimeoutError)`, matching the measured post-fix outcome.
- Corrected `TestBoundaryVacuity` and `TestKnownBoundaryUnsafe` class docstrings: mislocated
  `false_at` attribution corrected to `true_at`; both collapse directions documented.
- Resolved `test_ff_p_returns_none_at_m4`'s "not confirmed" hedge with the corrected mechanism
  explanation (aliasing caused the corrupted conclusion; the pre-existing expensive frame search
  at M=4 causes the timeout itself).
- Confirmed `test_fg_p_returns_none`, `test_fg_p_returns_none_at_m4`, `test_fg_p_spurious_unsat`,
  `test_gf_p_returns_none_at_m4`, `test_imp_gg_p_gf_p_returns_none_at_m4` all unaffected by direct
  re-measurement; left unchanged.
- `grep -in shadow` clean (one historical-context mention, correctly attributed).
- Full suite green: 30/30 passed in 358.02s.
- Pre-fix spot-check: all three rewritten `GG_P` tests fail against `PRE_FIX_SHA`'s
  `operators.py` (`DID NOT RAISE OracleTimeoutError`), confirming they assert genuine post-fix
  semantics, not encoding-agnostic catch-alls.

## Files modified

- `oracle/bimodal_logic/tests/test_soundness_regression.py`

## Files created

- `specs/139_.../evidence/false_at_deadness_probe.py` (Phase 5 prep, not yet run)

## Next phase

Phase 5: resolve the dead `false_at` implementations via a runtime invocation-counter deadness
proof (script already drafted).
