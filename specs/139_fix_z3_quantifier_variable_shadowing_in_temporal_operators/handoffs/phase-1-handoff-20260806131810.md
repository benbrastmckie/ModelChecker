# Phase 1 handoff: pre-fix evidence and collapse census

**Status**: COMPLETED

**PRE_FIX_SHA**: `d795b5f4444a4a3a326a4775b7431b89144e930c`

## What was done

- Built `evidence/collapse_census.py`: a zero-solve, construction-time structural probe that
  builds `conclusion_constraints[0]` for every complexity<=5 primitive formula (274 total, via
  the existing `_enumerate_primitive_formulas`) and applies `z3.simplify()`.
- Ran it pre-fix -> `evidence/pre-fix-census.json`. 104/274 folded (64 True, 40 False); within the
  158 temporal-only subset, 82 folded (56 True, 26 False), matching research exactly.
- Confirmed both collapse directions live: `G(G(p))` -> fast `None` (0.089s); `F(F(p))` ->
  `OracleTimeoutError` (5.096s); the two minimal `\Until`/`\Since` reproductions both time out at
  the 5000ms default too.
- `bimodal_harness` confirmed not importable (`ModuleNotFoundError`) -- recorded, not assumed.

## Deviation recorded (not silently resolved)

The plan expected exactly 2 non-`\bot` folded survivors; measured 4 (temporal-only) / 7 (full
274-formula population). The 2 extra (temporal-only) / 5 extra (full population) are genuine
propositional/modal tautologies (`p -> p`, `p -> (p -> p)`, and three Box-involving variants)
that contain **no** quantified-operator node at all -- structurally outside the aliasing defect's
blast radius. Full analysis in `evidence/pre-fix-state.md`. The core claim (exactly two formulas
collapse *because of* nested same-primitive quantifier aliasing) is preserved.

## Files created

- `specs/139_.../evidence/collapse_census.py`
- `specs/139_.../evidence/pre-fix-census.json`
- `specs/139_.../evidence/pre-fix-state.md`

## Next phase

Phase 2: replace the 14 `z3.Int(...)` bound-variable declaration sites in
`code/src/model_checker/theory_lib/bimodal/operators.py` with `z3.FreshInt(...)`, then re-run the
census to confirm the two survivors are gone.
