# Phase 5 handoff: resolve the dead `false_at` implementations

**Status**: COMPLETED (decision flipped from plan default via the plan's own gate)

## What was done

- Ran the deadness-proof instrument (`evidence/false_at_deadness_probe.py`), redirected to
  `evidence/false_at_deadness_probe.log` (was previously run once with output lost to an
  unrecoverable shell association; re-run this dispatch with output captured to a durable file
  per the coordinator's correction).
- Bimodal package suite: 298 passed in 108.62s. Oracle gating suite (soundness_regression +
  encoding_nondegeneracy): 34 passed in 494.12s. Both green under instrumentation.
- Invocation counters: `NecessityOperator=0, FutureOperator=1, PastOperator=1, UntilOperator=3,
  SinceOperator=2` (7 total, 4 of 5 operators non-zero).
- **Gate fired**: deletion is OFF. Every recorded call traces to a unit test
  (`test_foralltime.py`, `test_until_since.py`) invoking `operator.false_at(...)` directly to
  assert its structural shape, with `semantics.false_at` monkeypatched to a mock -- never a call
  originating from `find_countermodel()` or `BimodalSemantics.false_at`. This does not contradict
  the research report's production-reachability claim; it contradicts only the stronger
  whole-package-including-tests claim.
- Decision flipped to **keep-and-fix**: the five `false_at` methods are retained unmodified. They
  already carry the Phase 2 `_fresh_bound_int()` fix at all five sites (reconfirmed by grep), so
  no further code change was required.
- Per the plan's "skip the remaining tasks in this phase" instruction, skipped: the deletion
  itself, the `BimodalSemantics.false_at` docstring expansion (its claim would be false post-flip),
  and removing the Phase 2 fix at the (non-deleted) sites.
- Full analysis, caller table, and grep-discrepancy discussion: `evidence/phase5-deadness-proof.md`.

## Files modified

None outside `specs/139_.../evidence/` and `specs/139_.../plans/`. No production code changed in
this phase (Phase 2's fix already covers the retained methods).

## Files created

- `specs/139_.../evidence/false_at_deadness_probe.log`
- `specs/139_.../evidence/phase5-deadness-proof.md`

## Next phase

Phase 6: full-suite verification before re-baselining (bimodal package suite + oracle gating
suite via `oracle/run-oracle-suite.sh`, classify every failure as (a) expected stale-manifest
effect, (b) pre-existing at `PRE_FIX_SHA`, or (c) genuine regression).
