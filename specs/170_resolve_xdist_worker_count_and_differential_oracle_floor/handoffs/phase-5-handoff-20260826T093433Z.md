# Phase 5 Handoff: Documentation, final gate, and honest close-out

**Status**: [COMPLETED]

## What happened

Rewrote `code/docs/core/TESTING_GUIDE.md` section 8.13's closing paragraph to record: the
widened `_COVERED` (four logos files -> seven), the actual count raised (57, not the original
22), the two-cohort measurement basis (22-item `2`/`3` cohort from prior research, 35-item
`max_time: 5` cohort measured this task), and the disjoint-value-range non-conflict with
bimodal's `BM_CM_1`/`BM_CM_4` recalibration record. The "Raise the budget; never lower the
floor" discipline paragraph is preserved unmodified (grep-verified). Added a second new
paragraph recording the `-n 6` -> `-n 4` change, its falsification-screen evidence basis, the
explicit "cannot prove CI safety" limitation, and the `timeout-minutes` decision. Extended
section 8.11 with a full D subsection: root cause **not determined**, the confirmed-innocent-
bystander finding, all three live hypotheses, the new telemetry, and explicit "left open for a
future task" framing.

Ran the full gate set:
- `pytest code/tests/ci/ -q`: 58/58 green.
- `pytest code/tests/ -q -m "not packaging" --timeout=300 --timeout-method=thread`: 439 passed,
  1 skipped, 0 failed, 47.77s.
- Full gating selection at the settled `-n 4`, two-pass split (backgrounded/polled per this
  batch's process discipline): parallel pass 2343 passed, 1 skipped, 0 failed in 230.44s; serial
  (`xdist_serial`) pass 9 passed, 0 failed in 2.26s. Neither pass hit its ceiling; no narrowing
  was needed anywhere in this phase.

Hard-constraint gate (`git diff` over the whole task's commits, `b168d347`..HEAD):
- `oracle/bimodal_logic/tests/test_cross_oracle_differential.py`: zero diff (untouched).
- `GATING_RECHECK_SOLVE_TIMEOUT_MS`: still 40000.
- `MIN_CONCLUSIVE_GATING_FORMULAS`: still 100.
- `_MIN_MAX_TIME`: still 10.
- Every `max_time` value removed by this task's diffs across `bimodal`/`exclusion`/
  `imposition`/`examples.py` was 2, 3, or 5 (never >= 10) — confirmed by extracting every
  removed-line numeric value and checking the set is exactly `{2, 3, 5}`.
- `BM_CM_1` (60) and `BM_CM_4` (120) confirmed byte-identical in the current tree.

## Files changed

- `code/docs/core/TESTING_GUIDE.md` — section 8.13 closing paragraph rewritten (two new
  paragraphs replacing the stale "Coverage is deliberately partial" text); section 8.11 extended
  with a new D subsection before 8.12.
- `specs/170_.../summaries/01_budget-floor-worker-count-telemetry-summary.md` — new (see next).
- `specs/170_.../evidence/phase5-final-*.log` (gitignored `*.log`) — final-gate raw output.
- Plan file: Phase 5 heading and task checklist updated; this is the last phase.

## Deviations from plan

None in this phase's own scope. All prior phases' documented deviations (Phase 2's manual
backgrounding, Phase 4's inline-step and unconditional-backgrounding choices) are recorded in
their own handoffs and remain accurate; nothing here supersedes them.

## Item-by-item disposition (final)

- **B**: closed, untouched throughout (confirmed again by this phase's hard-constraint gate).
- **C**: landed in Phase 1 — 57 below-floor budgets raised to 10 across bimodal/exclusion/
  imposition, fully measurement-backed (both cohorts).
- **A**: decided in Phase 3 on the clean branch — `-n 6` -> `-n 4`, evidence-limited, revert
  trigger recorded, `timeout-minutes` left unchanged with a written comparison.
- **D**: instrumented in Phase 4, not fixed — root cause remains explicitly open in both the
  module docstring and `TESTING_GUIDE.md`.

## Next steps

None remaining in this plan. All 5 phases closed. A future task can read the telemetry once it
accumulates on a Python 3.12 CI run and decide D's memory hypothesis on data.
