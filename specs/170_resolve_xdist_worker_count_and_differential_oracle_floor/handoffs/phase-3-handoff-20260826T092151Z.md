# Phase 3 Handoff: Decide and apply the `-n 6` -> `-n 4` change

**Status**: [COMPLETED]

## What happened

Phase 2's screen was CLEAN, so the plan's decision rule required taking the clean branch:
change `-n 6` to `-n 4` in both `.github/workflows/tests.yml` and `flake.nix`, together.

- **RED**: changed `-n 6` to `-n 4` in `tests.yml` only, ran
  `pytest code/tests/ci/test_workflow_parity.py -v`, confirmed `test_worker_count_matches` failed
  with `-n worker count diverged ... ('4') and ... ('6')`.
- **GREEN**: made the matching change in `flake.nix`; parity module 5/5 green.
- Reworded `tests.yml`'s "Run general test suite" step comment: kept the `-n {N}, never xdist's
  auto worker-count mode` argument and the `BM_CM_1-example_case7` citation intact (only the
  literal digit describing this job's own value updated 6->4), appended a new paragraph stating
  the falsification screen's four-draw result, the corroborating prior 554-test measurement, the
  explicit "falsifies, does not prove CI-safe" limitation (citing `TESTING_GUIDE.md` 8.13), and a
  named revert trigger pointing at `test_worker_count_matches` as the two-sided-revert guard.
- Updated `flake.nix`'s parallel cross-reference comment to point at `tests.yml`'s step comment
  as the full rationale, and to reflect `-n 4`.
- Added a comment directly above `timeout-minutes: 20` recording the decision to leave it
  unchanged: `-n 6` averaged 260.4s, `-n 4` averaged 272.8s across Phase 2's draws — a ~5%
  difference well inside the ~70s draw-to-draw spread, with the single fastest draw overall
  actually being an `-n 4` draw — so no systematic slowdown to project against the 20-minute
  backstop.
- Verification: `pytest code/tests/ci/ -q` 38/38 green (including `test_workflow_parity.py`
  specifically). `git diff` confirms the `-n` value literal changed in exactly the two `pytest`
  invocation lines `test_workflow_parity.py` extracts, plus 8 comment-line digit updates, and that
  the `BM_CM_1`/never-auto rationale text is present in both files. `nix flake show` (substituted
  for `nix flake check --dry-run`, which is not a recognized flag on this Nix version) parsed
  `flake.nix` cleanly, evaluating `checks.x86_64-linux.default` as a valid derivation.

## Files changed

- `.github/workflows/tests.yml` — `-n` value (6->4), rationale comment rewritten with the new
  evidence and revert trigger, `timeout-minutes: 20` decision comment added (value unchanged).
- `flake.nix` — `-n` value (6->4), cross-reference comment updated.

## Deviations from plan

None in scope or outcome. One process note: `nix flake check --dry-run` is not a valid flag on
the Nix version in this environment (`error: unrecognised flag '--dry-run'`); substituted
`nix flake show`, which the plan itself names as the "at minimum" fallback.

## Next phase

Phase 4 (peak-RSS-per-worker telemetry, Python 3.12-gated, non-gating) is sequenced after this
phase specifically because both edit `.github/workflows/tests.yml`.
