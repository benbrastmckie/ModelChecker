# Phase 2 Handoff: Bounded full-selection `-n 6` vs `-n 4` falsification screen

**Status**: [COMPLETED]

## What happened

Ran the four planned draws of the full gating selection (`taskset -c 0,1,2,3`, identical command
shape, only `-n` varied): `-n 6` draw 1 (reused from Phase 1), `-n 4` draw 1, `-n 6` draw 2,
`-n 4` draw 2. All four passed 2323/2323 (1 skip, 0 failed, 0 errors). All four pairwise diffs
(cross-`-n` x2, within-`-n` x2) over the sorted `PASSED`/`FAILED`/`ERROR` node-id lists were
empty. Outcome classified **CLEAN** per the plan's decision-input vocabulary.

Wall clocks: `-n 6` 240.63s / 280.14s, `-n 4` 307.19s / 238.35s — no systematic `-n 4` slowdown;
the fastest single draw overall was actually `-n 4` draw 2. Full detail, including the required
"this screen falsifies, it does not prove CI safety" evidence-limitation statement (per
`TESTING_GUIDE.md` 8.13's already-recorded local-vs-CI divergence), is in
`specs/170_resolve_xdist_worker_count_and_differential_oracle_floor/evidence/phase2-screen-results.md`.

**Process deviation** (not a scope deviation): each poll command issued via the Bash tool was
itself killed at a 2-minute wall-clock limit before the underlying (disowned, truly-backgrounded)
pytest process finished. Recovered each time by re-polling with `kill -0 $PID` / `tail` — the
disowned process was never affected by the poll command's own timeout. All four draws still ran
to completion sequentially with the identical command shape; no test selection or `-n` value was
altered by this.

## Files changed

- `specs/170_.../evidence/n4-draw1.log`, `n6-draw2.log`, `n4-draw2.log` (gitignored, `*.log`).
- `specs/170_.../evidence/{n4-draw1,n6-draw2,n4-draw2}-outcomes.txt` (tracked).
- `specs/170_.../evidence/phase2-screen-results.md` (tracked) — full results table, diffs,
  wall-clock note, evidence-limitation statement.
- Plan file: Phase 2 heading and task checklist updated.

## Next phase

Phase 3 applies the plan's three-branch decision rule. Screen is CLEAN, so Phase 3 takes the
**clean branch**: change `-n 6` to `-n 4` in both `.github/workflows/tests.yml` and `flake.nix`,
demonstrate the parity guard RED on a one-sided edit first, reword the rationale comment while
retaining the `BM_CM_1`/`auto` text verbatim, and record a named revert trigger. Phase 3 must also
weigh the `timeout-minutes: 20` margin question using this phase's wall-clock note (no strong
CI-side extrapolation is available from this host's numbers) and must not claim CI safety —
only that the local falsification screen cleared.
