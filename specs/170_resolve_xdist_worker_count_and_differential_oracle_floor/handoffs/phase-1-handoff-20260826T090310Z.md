# Phase 1 Handoff: Extend the example solve-budget floor to bimodal, exclusion, imposition

**Status**: [COMPLETED]

## What happened

Re-ran the below-floor AST scan over `bimodal/examples.py`, `exclusion/examples.py`, and
`imposition/examples.py` using the identical method to `_budgets()` in
`code/tests/ci/test_example_budget_floor.py`. Found **57** below-floor `max_time` dicts (bimodal
21, exclusion 26, imposition 10) — confirming the plan's Scope Hypothesis, not the research
report's/task description's "22" (20 at `max_time: 2`, 2 at `3`). The extra 35 sit at
`max_time: 5` and had never been measured.

Measured all 35 `max_time: 5` examples in isolation (single process, no contention), directly via
`run_test()` rather than pytest, since three names (`TN_CM_1`, `MF_MODAL_FUTURE_TH`,
`BM_TH_5`) are not collected by the pytest suite at all. Range: 0.012s-1.549s — 6.5x-800x
headroom over the 10s floor.

RED: added the three paths to `_COVERED`; confirmed exactly 57 violations (21+26+10), matching
the scan.

GREEN: raised all 57 below-floor `max_time` values to exactly 10 via a scoped regex substitution
(any `'max_time': N` with N<10 -> 10); confirmed via `git diff` audit that every removed value was
<10 and every added value is exactly 10, and that `BM_CM_1` (60)/`BM_CM_4` (120) are byte-
identical (untouched). Guard re-run: 8/8 green. Full `code/tests/ci/` suite: 38/38 green.

Rewrote the guard's docstring scope paragraph to record: the measurement basis for both cohorts
(22-item `2`/`3` cohort from the research report, 35-item `max_time: 5` cohort measured this
phase), the total count raised (57), that no above-floor value was touched, that this does not
conflict with bimodal's `BM_CM_1`/`BM_CM_4` recalibration record (disjoint value ranges), and that
`MD_TH_2`/`TN_CM_1`/`MF_MODAL_FUTURE_TH`/`BM_TH_5` are measured and raised but not exercised by
CI's collected suite (three via `KNOWN_TIMEOUT_EXAMPLES`, one never added to `unit_tests`).

Ran the full gating selection (doubling as Phase 2's `-n 6` draw 1):
`taskset -c 0,1,2,3 pytest tests/ src/model_checker -m "not packaging and not performance and not unstable and not xdist_serial" -n 6 -q --timeout=300 --timeout-method=thread -rA`
from `code/`. Backgrounded via explicit `&`/`disown` (not the harness's `run_in_background` param)
and polled by checking `kill -0 $PID` in short loops, since the poll command itself exceeded 2
minutes and was killed by the tool's own timeout — the underlying pytest process survived that
because it was disowned. Result: **2323 passed, 1 skipped, 0 failed, 0 errors, in 240.63s**
(well under the 20-minute ceiling and the ~8-12 minute extrapolation). Sorted PASSED/FAILED/ERROR
outcome list saved to
`specs/170_resolve_xdist_worker_count_and_differential_oracle_floor/evidence/n6-draw1-outcomes.txt`
(2323 PASSED lines, 0 FAILED, 0 ERROR — the raw log's apparent "ERROR" lines were application
logger output, not pytest outcomes; filtered out by requiring `::` in the matched line).

## Files changed

- `code/tests/ci/test_example_budget_floor.py` — `_COVERED` extended to the three files;
  docstring scope paragraph rewritten.
- `code/src/model_checker/theory_lib/bimodal/examples.py` — 21 below-floor `max_time` values
  raised to 10.
- `code/src/model_checker/theory_lib/exclusion/examples.py` — 26 raised to 10.
- `code/src/model_checker/theory_lib/imposition/examples.py` — 10 raised to 10.

## Deviations from plan

None. All six task-list items executed exactly as specified; see checked items in the plan file
for confirmation notes.

## Next phase

Phase 2 (bounded full-selection `-n 6` vs `-n 4` falsification screen) reuses this phase's `-n 6`
draw 1 (already saved) and needs three more draws: `-n 4` draw 1, `-n 6` draw 2, `-n 4` draw 2.
