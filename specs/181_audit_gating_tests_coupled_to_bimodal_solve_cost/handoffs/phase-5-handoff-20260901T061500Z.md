# Phase 5 Handoff: three packaging workflows brought under the gating-selector contract

**Status**: COMPLETED (atomic-batch)

## What changed

- `.github/workflows/packaging.yml`: `-m packaging` -> `-m "packaging and not unstable and not development"`.
- `.github/workflows/release.yml`: both packaging-suite invocations (`build` job, `verify-pypi`
  job) gained `and not development` (quoted `-m "packaging and not unstable"` -> `-m "packaging and not unstable and not development"`).
- `.github/workflows/pypi-smoke.yml`: same `and not development` addition.
- `code/tests/ci/test_unstable_deselection_wiring.py`: added `PACKAGING_YML`/`RELEASE_YML`/`PYPI_SMOKE_YML`
  constants; extended `_SCANNED_FILES`, the parametrize list, the `checked_any_marker_expr`
  known-`-m`-bearing guard, and `test_scanned_invocation_counts_match_known_shape`;
  `EXPECTED_GATING_MARKER_INVOCATIONS` 6 -> 10; module docstring and all in-file explanatory
  comments updated; `_INVOCATION_COUNT_ANCHORS` six -> ten (must_contain) / six (must_not_contain,
  preserving the earlier seven -> six history in comments rather than deleting it).
- `code/docs/core/TESTING_GUIDE.md`: four count anchors (8.9's, three in 8.14's "Where the
  deselection is wired" narrative and its exit-path paragraph) updated to ten; 8.9's and 8.14's
  driver-list prose extended to name the three new drivers.
- `code/src/model_checker/theory_lib/bimodal/tests/conftest.py`,
  `.../bimodal/tests/README.md`, `code/tests/ci/test_development_marker_application.py`: count
  anchors updated six -> ten.

## Verification

- Scope Hypothesis confirmed empirically via direct `_invocations_for()` calls against the edited
  workflow files: packaging.yml=1, release.yml=2, pypi-smoke.yml=1 -> 4 new, total 10 (matches the
  plan's hypothesized value exactly, no discrepancy to record).
- `pytest tests/ci/test_unstable_deselection_wiring.py -v`: 23 passed.
- `pytest tests/ci/ -v`: 160 passed.
- `grep -n 'pytest tests/packaging/' .github/workflows/{packaging,release,pypi-smoke}.yml`: all
  four invocations carry the quoted double-quote `-m "packaging and not unstable and not development"` form.
- YAML syntax of all three workflow files validated via `yaml.safe_load`.

## Note for Phase 6

Phase 6 depends on this phase (dependency graph: `3, 5`). The packaging-suite selectors are now
correctly wired to exclude `development`-marked tests, so Phase 6's per-parametrize marking of
`test_generate_then_execute[bimodal]` will actually take effect against all four packaging-suite
gating invocations once applied.
