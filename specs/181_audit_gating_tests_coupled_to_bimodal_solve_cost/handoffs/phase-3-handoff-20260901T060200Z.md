# Phase 3 Handoff: `builder/tests/unit/test_example.py` non-bimodal solves switched to logos

**Status**: COMPLETED

## What changed

`code/src/model_checker/builder/tests/unit/test_example.py`:
- `TestBuildExampleBasic._create_test_module()` fixture content: bimodal -> logos.
- `test_build_example_with_no_model`'s inline content: bimodal -> logos.
- `test_build_example_comparison_mode`'s inline content: bimodal -> logos.
- `test_iteration_via_iterate_api`'s inline content: bimodal -> logos; its `bimodal.iterate`
  import changed to `logos.iterate`; the `max_time: 30` override removed (not raised — logos's
  own default `max_time=10` is ample at N=2).
- `test_build_example_bimodal_theory_countermodel` left entirely untouched (Phase 6's subject).

## Verification

`pytest src/model_checker/builder/tests/unit/test_example.py -m "not development" --durations=20`:
**17 passed in 10.93s** (Phase 1 baseline: 36.13s, slowest 31.47s). New slowest:
`test_iteration_via_iterate_api` at 10.01s (down from 31.78s/31.47s), no longer within 20% of any
explicit budget.

## Note for Phase 6/7

`test_iteration_via_iterate_api`'s 10.01s is suspiciously close to logos's own default
`max_time=10` — plausibly the iterate step's second-model search is itself hitting that cap rather
than deciding quickly. This does not fail the test (its own docstring already tolerates the
iterate finding only the initial model), and no budget was touched, but it is worth a passing
mention if Phase 8's after-figures show this file's wall clock as a non-trivial fraction of the
CLI/e2e/packaging trio total.
