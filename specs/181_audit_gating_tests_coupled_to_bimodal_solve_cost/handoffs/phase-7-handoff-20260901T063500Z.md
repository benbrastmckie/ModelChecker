# Phase 7 Handoff: executable no-bimodal-in-gating contract

**Status**: COMPLETED

## What changed

- New: `code/tests/ci/test_gating_selection_bimodal_decoupling.py` — scans both gating selections
  (main `tests src/model_checker` expression, packaging `tests/packaging/` expression), finds
  every file outside `theory_lib/bimodal/tests/` that textually references bimodal as an example
  fixture, and asserts each is classified in exactly one of `_SOLVE_FREE_BIMODAL_REFERENCES` (9
  entries) or `_DELIBERATE_BIMODAL_GATING` (1 entry: `test_full_pipeline.py`). Includes exact-match
  staleness checks on both allowlists, a no-double-classification check, and an anti-vacuity guard
  exercised directly against a deliberately empty collection.
- Fixed (not just classified): `src/model_checker/builder/tests/integration/test_performance.py`
  — a previously unaudited file constructing real bimodal solves via
  `BuildModule`+`runner.run_examples()` at 4 call sites (N=2/3/5), none bimodal-specific. Swapped
  to logos; also corrected several docstrings that had gone stale (bimodal's max_time=1
  assumption, and a bimodal-specific `get_theory` object-identity claim that does not hold for
  logos — replaced with an accurate logos-specific explanation).
- `code/docs/core/TESTING_GUIDE.md`: added a cross-reference from section 8.14 to the new
  contract.

## Deviation from plan

The Scope Hypothesis's seed list (report-derived) named ~19 files; the real scan found 11 (most of
the report's list uses bimodal only as a string literal, out of this contract's textual-fixture
scope by design — not a contradiction). Of the 11, one
(`builder/tests/integration/test_performance.py`) was a genuine, previously-unaudited real-solve
gating file — a sibling of the report's already-covered `builder/tests/unit/test_example.py` in a
different directory. Fixed via the established remedy (logos swap), consistent with HARD
CONSTRAINTS and the plan's own "Definition of done."

## Genuine-RED evidence

`tests/e2e/test_batch_output_real.py`'s Phase-4-fixed logos fixture was temporarily reverted to
bimodal; the contract went RED naming exactly that file; reverted immediately, confirmed
byte-identical to HEAD via `git status --short`. The anti-vacuity guard was exercised directly
(`pytest.raises(AssertionError)` against an empty collection set), not merely asserted to exist.

## Verification

- `pytest tests/ci/test_gating_selection_bimodal_decoupling.py -v`: 6 passed.
- `pytest tests/ci/ -v`: 167 passed (was 161).
- `pytest src/model_checker/builder/tests/integration/test_performance.py -v`: 6 passed in 1.00s
  (down from ~8.6s of bimodal solve cost the file's own pre-fix docstrings recorded).
- No budget value increased.
