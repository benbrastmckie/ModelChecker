# Phase 3 Handoff

**Phase completed**: 3 (of 4)

Created `oracle/run-oracle-suite.sh` (executable, `set -uo pipefail`, xdist-import guard,
PYTHONPATH default resolved from script location, -n 6 hard-coded for pass 1, no -n for pass 2,
two-line pass/fail summary, non-zero exit if either pass fails). Added "Running the Test Suite"
section to `oracle/bimodal_logic/README.md` (lines 73-100) documenting the one-command invocation
and the xdist_serial convention for new tests.

Verified: `bash oracle/run-oracle-suite.sh --collect-only -q` reports pass 1 = 543 collected,
pass 2 = 7 collected, both green, script exit 0. `test -x` confirms executable bit. No
task-number citations in the new script or the new README section (pre-existing task 118
references elsewhere in README.md, outside my new section, are untouched per plan scope).

## Next
Phase 4: run the serial pass (foreground, ~5-6 min) then the full parallel pass
(background, ~44 min) and compare failure lists against the known-failing set
(`test_complexity_5_scan_self_consistent`, `test_enriched_vs_primitive_sat_agreement[all_future]`).

No deviations from plan.
