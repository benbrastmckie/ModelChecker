# Phase 1-2 Handoff

**Phases completed**: 1, 2 (of 4)

## Phase 1: oracle/conftest.py
Created `oracle/conftest.py` with `pytest_configure` (registers `differential`, `slow`,
`xdist_serial`) and `pytest_collection_modifyitems` (marks the two parametrize cases:
`test_enriched_vs_primitive_sat_agreement[some_past]` and
`test_regression_all_active_examples[BM_CM_1...]`).

Verified: 0 PytestUnknownMarkWarning; 550 total collection unchanged; -m xdist_serial selects
exactly 2 items before Phase 2's static decorators.

## Phase 2: static decorators
Added `@pytest.mark.xdist_serial` to `TestStateIsolationRegression` (class-level, 4 methods)
and to `TestOracleMFormulaBoundarySafe.test_oracle_m_formula_depth1_boundary_safe` (method-level)
in `oracle/bimodal_logic/tests/test_soundness_regression.py`.

Verified: -m xdist_serial selects exactly 7 items; -m "not xdist_serial" selects exactly 543;
543 + 7 = 550.

## Next
Phase 3: create `oracle/run-oracle-suite.sh` two-pass runner + README section.
Phase 4: validate serial pass (7 passed) and background parallel pass (~44 min) against known
failing set.

No deviations from plan.
