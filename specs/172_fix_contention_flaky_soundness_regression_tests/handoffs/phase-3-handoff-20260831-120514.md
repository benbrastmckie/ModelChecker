# Phase 3 Handoff: task 172

## State

Phases 1 and 2 are [COMPLETED] and committed (`cf55f56b`, `2aae7217`). Phase 3 (full two-pass
verification) is [PARTIAL]: the plan's own remedy is fully verified working (none of the four
newly-marked tests fail in two independent full pass-1 runs; pass 2 is green both times with the
expected +4 test-count growth, well inside the 1800s budget). Phase 4's decision-record content
is written and [COMPLETED].

## What blocks a clean Phase 3 close

A fifth, reproducible failure, unrelated to this task's remedy:
`TestShiftClosure::test_shift_closure_on_extracted_worlds_m3` in
`oracle/bimodal_logic/tests/test_soundness_regression.py:541` — `AssertionError: Solver should
find SAT for atom 'p' at M=3 with depth-bounded abundance` (`z3_model_status` is `False`).
Reproduced identically across 2 independent full `-n 6` pass-1 runs (705.05s and 718.15s). This
test bypasses `find_countermodel()` entirely (direct `BimodalStructure` construction with its
own `max_time: 15.0`), so it is genuinely outside this task's remedy mechanism even though it is
inside the target file. This plan's Non-Goals forbid fixing it here.

## Recommended next step

`/spawn 172` (or a fresh task) to investigate why the M=3 depth-bounded-abundance solve no
longer reliably finds SAT for `atom('p')` — this is reproducible, not flaky, so it reads as a
solver/constraint-layer regression rather than a scheduling issue. Once that is fixed, re-run
`bash oracle/run-oracle-suite.sh` once more; if it comes back fully green, task 172 can close as
`[COMPLETED]`.

## Artifacts

- Plan: `specs/172_fix_contention_flaky_soundness_regression_tests/plans/01_mark-flaky-tests-xdist-serial.md`
- Summary: `specs/172_fix_contention_flaky_soundness_regression_tests/summaries/01_mark-flaky-tests-xdist-serial-summary.md`
- Modified: `oracle/bimodal_logic/tests/test_soundness_regression.py` (four `xdist_serial` decorators)
- Run logs (scratchpad, not committed): `oracle-suite-post.log`, `oracle-pass1-rerun.log`
