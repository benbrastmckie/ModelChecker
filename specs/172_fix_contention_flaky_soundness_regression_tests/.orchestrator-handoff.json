{
  "dispatch_seq": 7,
  "phase": "plan",
  "status": "planned",
  "summary": "4-phase plan implementing research remedy (a) only: add @pytest.mark.xdist_serial plus inline rationale comments to four tests in oracle/bimodal_logic/tests/test_soundness_regression.py (the three reported failures plus TestBoundaryVacuity::test_depth1_boundary_safe_is_true, the in-scope sibling with an identical risk profile), matching the in-file precedent at line 1092. Phases: read-only call-site inventory, marker application, full two-pass verification via the real oracle/run-oracle-suite.sh driver, decision record. Estimated 2 hours, fully sequential.",
  "artifacts": [
    {
      "type": "plan",
      "path": "specs/172_fix_contention_flaky_soundness_regression_tests/plans/01_mark-flaky-tests-xdist-serial.md",
      "summary": "4-phase plan marking four contention-flaky oracle tests xdist_serial with full two-pass verification."
    }
  ],
  "phases_completed": 0,
  "phases_total": 4,
  "blockers": [],
  "next_action_hint": "implement",
  "files_modified": [],
  "decisions_made": [
    "INCLUDE the Finding 4 sibling: TestBoundaryVacuity::test_depth1_boundary_safe_is_true is marked alongside the three reported failures -- same file, same class, same F_P call, same unmodified timeout_ms=5000 default; its absence from the measured failure list reflects probabilistic scheduling noise, not safety",
    "Per-method decorators only, never a class-level marker -- both enclosing classes hold fast depth-0 and exception-path methods that must stay in pass 1",
    "No max_rlimit at these call sites (TESTING_GUIDE 8.13): xdist_serial removes the contention entirely, so max_rlimit would add a second independent failure mode with no correctness benefit. Recorded in plan Phase 4",
    "No AST floor guard in code/tests/ci/test_example_budget_floor.py: the risk is a shared function default crossed with per-formula temporal_depth, not a per-call-site dict literal, so that guard's scan shape cannot distinguish at-risk sites from raises-timeout-on-purpose sites. Recorded in plan Phase 4",
    "provider.py and test_example_budget_floor.py are explicit non-goals and must show zero diff",
    "Verification is the full detached `bash oracle/run-oracle-suite.sh` two-pass run (setsid nohup, poll the log), never a narrowed -k selection",
    "test_oracle_provider.py::test_future_sat_returns_dict carries the same risk but is outside file_scope -- recorded as a /spawn follow-up candidate, not pulled in"
  ],
  "dead_ends": []
}
