# Implementation Plan: Task #17

- **Task**: 17 - Fix remaining uppercase directory references missed by task 16
- **Status**: [NOT STARTED]
- **Effort**: 1.5 hours
- **Dependencies**: Task #16 (completed)
- **Research Inputs**: specs/017_fix_remaining_uppercase_directory_references/reports/research-001.md
- **Artifacts**: plans/implementation-001.md (this file)
- **Standards**: plan-format.md, status-markers.md, artifact-management.md
- **Type**: general
- **Lean Intent**: false

## Overview

This plan addresses remaining uppercase directory references (`Code/`, `Docs/`) discovered after task 16's bulk rename. The primary user-facing issue is the neovim keybinding `<leader>rm` failing due to external neovim config references. This plan fixes in-repository files and documents the external neovim configuration changes required.

### Research Integration

Research identified 6 categories of remaining references:
1. Neovim config (external) - ROOT CAUSE of the immediate error
2. .gitignore - References non-existent paths
3. Jupyter notebooks - Cached execution outputs
4. PKG-INFO - Auto-generated during build
5. LaTeX .fls - Auto-generated during compilation
6. Documentation plan - Historical reference

## Goals & Non-Goals

**Goals**:
- Fix `.gitignore` entries referencing old `Code/` paths
- Document the external neovim configuration fix for the user
- Clean up generated files that can be safely regenerated
- Provide verification steps to confirm all fixes are complete

**Non-Goals**:
- Modify files in `specs/` archive (historical records)
- Change GitHub commit history (old URLs in history are unavoidable)
- Directly modify external neovim configuration (outside repository scope)

## Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Neovim config is external | User must apply fix manually | High | Provide exact changes needed in summary |
| PKG-INFO regeneration requires package rebuild | Old paths remain until rebuild | Low | Include rebuild command in verification |
| Jupyter notebook outputs contain many paths | Time-consuming to clear all | Low | Clear outputs in bulk with nbconvert |

## Implementation Phases

### Phase 1: Fix .gitignore Entries [NOT STARTED]

**Goal**: Update .gitignore to reference the correct lowercase `code/` directory paths

**Tasks**:
- [ ] Edit `.gitignore` lines 21-23 to change `Code/` to `code/`

**Timing**: 10 minutes

**Files to modify**:
- `.gitignore` - Change 3 entries from `Code/` to `code/`

**Verification**:
- `grep -n "Code/" .gitignore` returns no matches
- `grep -n "code/" .gitignore` returns the corrected entries

---

### Phase 2: Clean Generated Files [NOT STARTED]

**Goal**: Remove or regenerate files containing stale path references

**Tasks**:
- [ ] Delete PKG-INFO (will regenerate on next build)
- [ ] Delete LaTeX .fls auxiliary file
- [ ] Clear Jupyter notebook outputs (optional but recommended)

**Timing**: 15 minutes

**Files to modify**:
- `code/src/model_checker.egg-info/PKG-INFO` - Delete (regenerated by build)
- `talks/truthmaker_adv_II/handout.fls` - Delete (regenerated by LaTeX)
- `code/src/model_checker/jupyter/notebooks/*.ipynb` - Clear outputs
- `code/src/model_checker/jupyter/notebooks/.ipynb_checkpoints/` - Delete checkpoint files
- `code/src/model_checker/jupyter/debug/.ipynb_checkpoints/` - Delete checkpoint files

**Verification**:
- Deleted files no longer exist
- `grep -rn "Code/" --include="*.ipynb" code/` returns no matches after clearing

---

### Phase 3: Update Documentation Plan (Optional) [NOT STARTED]

**Goal**: Update historical documentation plan for consistency if desired

**Tasks**:
- [ ] Review `docs/specs/plans/documentation_revision_plan.md`
- [ ] Update `Docs/` references to `docs/` if appropriate

**Timing**: 10 minutes

**Files to modify**:
- `docs/specs/plans/documentation_revision_plan.md` - Update path references

**Verification**:
- `grep -n "Docs/" docs/specs/plans/documentation_revision_plan.md` returns no matches

---

### Phase 4: Document External Neovim Fix [NOT STARTED]

**Goal**: Create clear documentation for user to fix external neovim configuration

**Tasks**:
- [ ] Document the exact changes needed in `~/.config/nvim/lua/neotex/plugins/editor/which-key.lua`
- [ ] Include line numbers and before/after examples
- [ ] Note the session file will auto-update on next save

**Timing**: 15 minutes

**Documentation content** (to include in implementation summary):

```
External Neovim Configuration Fix Required
==========================================

File: ~/.config/nvim/lua/neotex/plugins/editor/which-key.lua

Changes needed (6 occurrences):
- Line 104-105: "/Code/dev_cli.py" -> "/code/dev_cli.py"
- Line 109-110: "/Code/dev_cli.py" -> "/code/dev_cli.py"
- Line 113-114: "/Code/dev_cli.py" -> "/code/dev_cli.py"
- Line 121: Error message (optional, for clarity)

After editing, the keybinding <leader>rm should work correctly.
```

**Verification**:
- User runs `<leader>rm` in neovim after applying fix - no error
- `grep -n "Code/" ~/.config/nvim/lua/neotex/plugins/editor/which-key.lua` returns no matches

---

### Phase 5: Final Verification [NOT STARTED]

**Goal**: Confirm all in-repository uppercase references are resolved

**Tasks**:
- [ ] Run comprehensive grep to find any remaining `Code/` references
- [ ] Run comprehensive grep to find any remaining `Docs/` references
- [ ] Verify no references remain outside specs/ archive

**Timing**: 10 minutes

**Verification commands**:
```bash
# Should return empty or only specs/ matches
grep -rn "Code/" --exclude-dir=specs --exclude-dir=.git .

# Should return empty or only specs/ matches
grep -rn "Docs/" --exclude-dir=specs --exclude-dir=.git .
```

**Success criteria**:
- No matches outside specs/ directory
- All generated files either deleted or cleared

---

## Testing & Validation

- [ ] `.gitignore` grep verification passes
- [ ] No `Code/` references in active codebase (excluding specs/)
- [ ] No `Docs/` references in active codebase (excluding specs/)
- [ ] Generated files deleted or cleared
- [ ] External neovim fix documented for user

## Artifacts & Outputs

- Updated `.gitignore` file
- Cleaned generated files (PKG-INFO, .fls, notebook outputs)
- Implementation summary with external neovim fix documentation

## Rollback/Contingency

- .gitignore changes can be reverted via git
- Deleted generated files will regenerate on next build/compile
- Notebook checkpoints are non-critical and can be regenerated
