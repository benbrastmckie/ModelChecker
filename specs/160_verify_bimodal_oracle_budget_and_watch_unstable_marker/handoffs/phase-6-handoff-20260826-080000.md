# Phase 6 Handoff: Documentation, final gate, and close-out

- **Status**: COMPLETED
- **Verification Tier**: full
- **Commit Mode**: per-substep

## What was done

- `code/docs/core/TESTING_GUIDE.md` section 8.9 brought in line with Phases 1-5: "Currently
  marked" gained a second entry (gating-scan test, pointing at its own marker-site comment
  block); "Where the deselection is wired" now names `oracle/run-oracle-suite.sh`'s two passes
  and explains why the oracle tree entered scope only now; a new paragraph records the
  classifier's importable-module location (`.github/scripts/unstable_watch_classify.py` +
  `code/tests/ci/test_unstable_watch_classifier.py`); a new paragraph records the
  promotion-streak upper-bound limitation and the `unstable-watch-record.jsonl`
  cross-check requirement. 8.8's `TestGatingConclusiveScan` mention was checked and found not
  stale -- no edit needed there.
- Ran the full Phase 6 gate set (see summary for the table). Gate 3 (the whole-directory oracle
  run) did not complete inside a 580s bounded foreground timeout -- narrowed per this dispatch's
  process constraint to the single file this task modified
  (`test_cross_oracle_differential.py`), which passed (62 passed, 10 deselected, 321.47s) and
  independently cross-checked the full-directory deselection count via a sub-second
  `--collect-only` pass (619 selected / 10 deselected, no anomaly).
- Phase 6 hard-constraint gate: `git diff e75f7513..HEAD` plus the uncommitted TESTING_GUIDE.md
  diff reviewed for every `MIN_CONCLUSIVE_GATING_FORMULAS` / `GATING_RECHECK_SOLVE_TIMEOUT_MS` /
  `max_time` occurrence -- all are comment/prose additions, no value changed. Confirmed live:
  `MIN_CONCLUSIVE_GATING_FORMULAS = 100`, `GATING_RECHECK_SOLVE_TIMEOUT_MS = 40000`.
- Confirmed both required marker-site records: `xdist_serial` lead marked CLOSED (dated
  evidence, predates both shortfall runs; `differential-tests.yml` never passes `-n`); seven
  timing-out formulas' identities marked UNRECOVERABLE from available CI artifacts (no
  upload-artifact step, aggregate-only log line, unused instrumentation hooks).
- Wrote the implementation summary at
  `specs/160_verify_bimodal_oracle_budget_and_watch_unstable_marker/summaries/01_unstable-marker-and-watch-classifier-summary.md`.

## Verification

- Gate 1 (`code/tests/ci/`): 35 passed, 1.62s.
- Gate 2 (`code/tests/`): 522 passed, 5 skipped, 187.63s.
- Gate 3 (narrowed to `test_cross_oracle_differential.py`): 62 passed, 10 deselected, 321.47s.
  Full-directory attempt timed out at 580s (bounded, foreground) -- recorded, not silently
  dropped.
- Gate 4 (`-m unstable --collect-only`): 1/629 collected, exactly the newly-marked test.
- No task-number references introduced outside `specs/**` (grep-verified against the full
  `e75f7513..HEAD` diff plus the uncommitted TESTING_GUIDE.md diff).

## Files touched

- `code/docs/core/TESTING_GUIDE.md` (uncommitted from a prior dispatch; committed by this one)
- `specs/160_verify_bimodal_oracle_budget_and_watch_unstable_marker/plans/01_unstable-marker-and-watch-classifier.md`
  (Phase 6 heading + checklist marked complete)
- `specs/160_verify_bimodal_oracle_budget_and_watch_unstable_marker/summaries/01_unstable-marker-and-watch-classifier-summary.md`
  (new)
- `specs/160_verify_bimodal_oracle_budget_and_watch_unstable_marker/handoffs/phase-6-handoff-20260826-080000.md`
  (this file)

## Next phase

None -- this was the final phase (6 of 6). Task 160 implementation is complete.
