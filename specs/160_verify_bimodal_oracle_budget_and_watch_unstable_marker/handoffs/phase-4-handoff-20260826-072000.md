# Phase 4 Handoff: Mark the test `unstable` with the four-criteria record

- **Status**: COMPLETED
- **Verification Tier**: interface
- **Commit Mode**: per-substep

## What was done

- Added `@pytest.mark.unstable` directly above
  `TestGatingConclusiveScan::test_known_conclusive_population_self_consistent` (method-level,
  class-level `@pytest.mark.xdist_serial` and docstring otherwise untouched).
- Replaced the stale "USER ACTION REQUIRED... NOT YET VERIFIED" tail of
  `GATING_RECHECK_SOLVE_TIMEOUT_MS`'s comment block with the verified-outcome paragraph (real CI,
  2026-08-25, commit 93cda5b9: agreements=96 disagreements=0 timeout_count=7 conclusive=96/103,
  byte-for-byte identical to the pre-widening measurement) and the full four-criteria record,
  drawn from the research report's proposed text with the task-number reference in criterion (3)
  rephrased to a date-only citation (no "task N" phrasing, per
  `.claude/rules/no-task-references-in-deliverables.md`).
- Added a one-line docstring pointer from the test method to the criteria block.

## Deviation (phase ordering)

Phase 5's edits were drafted in the working tree before this phase, but Phase 5's own
`--collect-only` verification bullet implicitly depends on this phase's marker already
existing (an unstated cross-phase dependency inside the wave-4 `[4, 5]` grouping). Corrected by
closing Phase 4 first; recorded inline in Phase 4's own plan section.

## Verification

- `-m unstable` collects exactly the one marked test (1/72; was 0/72 before).
- `-m "not slow and not differential and not unstable"`: 62/72 (was 63/72 -- diff of exactly 1).
- `-m "xdist_serial"` still collects it.
- `TestGatingConclusiveScanMechanism` (Z3-free): 3 passed, unaffected.
- No `PytestUnknownMarkWarning`; `oracle/conftest.py` unchanged (already registers `unstable`).
- `git diff` confirms neither `GATING_RECHECK_SOLVE_TIMEOUT_MS` nor
  `MIN_CONCLUSIVE_GATING_FORMULAS` changed value.

## Files touched

- `oracle/bimodal_logic/tests/test_cross_oracle_differential.py`

## Next phase

Return to Phase 5 (edits already drafted): finish its final verification (the collect-only
check that now has this phase's marker to check against), tick its checklist, and commit.
