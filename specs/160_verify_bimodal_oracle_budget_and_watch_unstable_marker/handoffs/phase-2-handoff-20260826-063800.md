# Phase 2 Handoff: GREEN A — Extract the classifier into an importable module

- **Status**: COMPLETED
- **Verification Tier**: interface
- **Commit Mode**: atomic-batch

## What was done

- Created `.github/scripts/unstable_watch_classify.py`: verbatim-behavior extraction of the
  heredoc's `MAX_TIME_BY_NODEID_FRAGMENT`, `FAILURE_SIGNATURE`, `parse_junit`, `classify`, the
  record-building loop, the `gh run list` trend query, the step-summary writer, and the exit-code
  contract. Wrapped in a parameterized `run(...)` function plus a thin `main()` reading env vars,
  guarded under `if __name__ == "__main__":` so import has no side effects. Stdlib only.
- Rewired `.github/workflows/unstable-watch.yml`'s classify step to
  `run: python3 .github/scripts/unstable_watch_classify.py`, deleting the heredoc. Preserved the
  step's `id: classify`, its `env:` block, and its position between the two watch steps and the
  artifact upload.
- Corrected the now-stale `watch_oracle` step comment: no longer claims exit code 5 is "expected
  every run" (false after Phase 4 marks a test in this tree); reworded to describe exit code 5 as
  the general tolerated no-collection case.

## Behavior preservation verified

- All 4 BM_CM_1 characterization tests and both `parse_junit` tests from Phase 1 pass, unmodified.
- The 2 gating-signature tests and 3 streak-honesty tests remain red (nothing added yet) --
  `compute_promotion_streak` does not exist; the gating branch is not in `classify()`. Confirmed:
  10 passed, 5 failed in `test_unstable_watch_classifier.py`.
- `PYTHONPATH=code/src pytest code/tests/ci/ -q`: 28 passed, 6 failed (the 5 above plus the
  pre-existing `run-oracle-suite.sh` wiring gap from Phase 1, untouched by this phase).
- YAML parse check: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/unstable-watch.yml'))"`
  succeeded cleanly (PyYAML available locally).
- End-to-end smoke against synthetic JUnit XML at the default `/tmp/watch-{code,oracle}.xml`
  paths: all-passed input -> exit 0; a NEW-classified failure -> exit 1 with the
  `::error title=UNSTABLE-WATCH: NEW FAILURE MODE::` annotation; `gh run list` failure (no `gh`
  CLI / no auth in this sandbox) degraded to `::warning::...` rather than crashing, in both runs.

## Atomic-batch discipline

`git status --short` before staging showed exactly the two files this phase's Files-to-modify
list names (`.github/scripts/unstable_watch_classify.py` new, `.github/workflows/unstable-watch.yml`
edited) -- no third file, confirming the extraction needed nothing else.

## Files touched

- `.github/scripts/unstable_watch_classify.py` (NEW)
- `.github/workflows/unstable-watch.yml`

## Next phase

Phase 3 (GREEN B): add `GATING_FLOOR_NODEID_FRAGMENT`/`GATING_FLOOR_SIGNATURE`/
`DISAGREEMENT_SIGNATURE`, the gating branch in `classify()`, `compute_promotion_streak()`, and
wire the promotion-notice honesty fix into `run()`.
