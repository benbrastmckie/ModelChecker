# Phase 5 Handoff: Wire the `unstable` deselection into the oracle suite driver

- **Status**: COMPLETED
- **Verification Tier**: local
- **Commit Mode**: per-substep

## What was done

- `oracle/run-oracle-suite.sh`: added `and not unstable` to both passes' `-m` expressions
  (parallel pass 1 and serial pass 2, matching the tests.yml/flake.nix both-passes convention).
  Added explanatory comments at both passes and corrected the class-docstring-adjacent header
  paragraph that claimed `TestGatingConclusiveScan` runs "in this second pass" (now correctly
  describes it as deselected). Updated the closing summary echo lines to mention `not unstable`.
- `.github/workflows/differential-tests.yml`: annotated (did not revert) the `--timeout=1500`
  rationale comment -- its 7-8-timeouts-add-140-160s estimate assumed
  `TestGatingConclusiveScan` still ran in that step, which is no longer true after Phase 4.
  Recorded that 1500 is retained as safe/harmless and 8.9 does not require re-tightening.

## Ordering note (see Phase 4's own deviation record)

This phase's edits were drafted before Phase 4 landed the marker, but one of this phase's own
verification bullets (the `--collect-only` check) implicitly depends on the marker already
existing. Closed Phase 4 first, then returned here to finish verification and close this phase
-- both phases' edits were already correct and needed no rework, only re-sequencing of closure.

## Verification

- `test_unstable_deselection_wiring.py`: 7 passed.
- `bash -n oracle/run-oracle-suite.sh`: clean.
- `pytest oracle -m "xdist_serial and not slow and not unstable" --collect-only -q`: does not
  collect the marked test (0).
- `test_workflow_parity.py`: 5 passed, unaffected.
- `yaml.safe_load` on the annotated `differential-tests.yml`: clean.
- Full `code/tests/ci/`: 35 passed, 0 failed.

## Files touched

- `oracle/run-oracle-suite.sh`
- `.github/workflows/differential-tests.yml`

## Next phase

Phase 6: TESTING_GUIDE.md documentation updates, full gate set, implementation summary.
