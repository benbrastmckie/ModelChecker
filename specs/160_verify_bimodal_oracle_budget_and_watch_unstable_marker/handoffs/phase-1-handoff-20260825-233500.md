# Phase 1 Handoff: RED — Contract tests for the classifier and the deselection wiring

- **Status**: COMPLETED
- **Verification Tier**: local
- **Commit Mode**: per-substep

## What was done

- Created `code/tests/ci/test_unstable_watch_classifier.py`: characterization tests pinning
  current `BM_CM_1` classify() behavior (4 tests), new-signature tests for the gating-floor
  branch including the disagreements-laundering guard (6 tests), `parse_junit` tests (2 tests),
  and promotion-notice-honesty helper tests (3 tests). Loads
  `.github/scripts/unstable_watch_classify.py` by absolute path via `importlib.util`, guarded
  by a `nix flake check` sandbox skip probing `.github/workflows/tests.yml`/`flake.nix`
  (deliberately NOT the classifier script itself, so its absence is the RED signal).
- Created `code/tests/ci/test_unstable_deselection_wiring.py`: regex-based contract asserting
  every `-m`-bearing pytest invocation in `tests.yml`, `flake.nix`, `differential-tests.yml`,
  and `run-oracle-suite.sh` includes `not unstable`; explicitly allows the one node-id-selecting
  invocation with no `-m`; excludes `unstable-watch.yml` from the scan by construction and adds
  a documentation-grade sanity check that it does select `-m unstable`.
- Updated `code/tests/ci/__init__.py`'s guard inventory docstring to name both new modules.

## RED confirmed

- `test_unstable_watch_classifier.py`: collection-time `FileNotFoundError` for
  `.github/scripts/unstable_watch_classify.py` (does not exist yet) — clean, named failure.
- `test_unstable_deselection_wiring.py`: collects (7 items), 1 test fails —
  `oracle/run-oracle-suite.sh`'s pass-1 invocation `-m "not xdist_serial and not slow"` lacks
  `not unstable` (pass-2's `-m "xdist_serial and not slow"` is equally missing it; only the
  first-parametrized failure surfaces per pytest's short-circuit, both confirmed independently
  via direct extraction). This is exactly the wiring gap Phase 5 fixes.
- `PYTHONPATH=code/src pytest code/tests/ci/ -q --ignore=code/tests/ci/test_unstable_watch_classifier.py`:
  18 passed, 1 failed (the expected RED above) — all pre-existing guards
  (`test_workflow_parity.py`, `test_timing_marker_coverage.py`, `test_example_budget_floor.py`)
  unaffected.

## Scope Hypothesis confirmed

`--collect-only` on both new modules: `test_unstable_deselection_wiring.py` collects 7 items
without error; `test_unstable_watch_classifier.py` errors at collection (expected — the script
under test does not exist yet), so its item count could not be observed pre-Phase-2. Combined
with the 7 collected plus the ~16 defined in the classifier module (4 characterization + 6
gating-signature + 2 parse_junit + 3 streak-honesty + 1 for the deselection module beyond its
sanity/documentation tests), the file-count and test-shape hypothesis holds; the exact number
will be re-confirmed once the classifier module can collect (Phase 2).

## Files touched

- `code/tests/ci/test_unstable_watch_classifier.py` (NEW)
- `code/tests/ci/test_unstable_deselection_wiring.py` (NEW)
- `code/tests/ci/__init__.py` (docstring update)

## Next phase

Phase 2 (GREEN A): extract the classifier into `.github/scripts/unstable_watch_classify.py`,
behavior-preserving, rewire `unstable-watch.yml`'s classify step to invoke it.
