# Phase 5 Handoff: Document the single-threaded contract

- **Task**: 135 - Fix concurrent model building segfault
- **Phase**: 5 of 7 (executed out of numeric order alongside Phases 2-3, per wave 3 -- Phase 5
  depends only on Phase 2, same as Phase 3, and needed no test execution so it proceeded while
  heavier verification runs are deferred pending the concurrent oracle job)
- **Status**: COMPLETED

## What was done

- Added a **Concurrency Model** subsection (`### 4. Concurrency Model`) to
  `code/docs/core/ARCHITECTURE.md` under *Core Architectural Principles*, stating the
  single-threaded-only contract, why it exists, what happens on violation, and the supported
  parallelization path (process pool, not threads). Added a cross-reference note at the top of
  the *Performance Architecture* section pointing back at it.
- Updated `code/docs/core/KNOWN_TEST_FAILURES.md`:
  - "The 2 crashing tests" section now reads "(fixed)" with a **Status: fixed** callout
    describing the guard and the rewritten tests, without prematurely claiming the Phase 4
    repeat-sample evidence exists yet (phrased as a methodology requirement, not a completed
    result).
  - "Removing the Quarantine" section now records that the concurrent-model-segfault half of
    the gate is satisfied and the wall-clock-budget half is still pending.
  - The quarantine-count intro paragraph now explains the measured 43/38/3/2 baseline predates
    the fix and that the quarantined set has since shrunk to 41 (the two crash tests are no
    longer marked `slow` at all), without editing the historical "Current State" table itself.
- Confirmed `ConcurrentConstructionError`'s docstring (Phase 1) is the single authoritative
  statement of the contract; `SemanticDefaults`/`ModelDefaults` docstrings (Phase 2) point at it
  rather than restating it.
- Added two `NOTE:` comments in `code/src/model_checker/solver/type_guards.py` (above the
  `import cvc5.pythonic` in `_check_not_cvc5_type()` at the original line 96, and above the
  same import in `is_cvc5_type()` at the original line 150) documenting the unconditional
  native-extension-load defect, that it is not the crash cause, and that it belongs in its own
  task. No behavior changed -- comments only.

## Plan deviations

None (implementation followed plan). One scheduling note: Phase 5 was executed before Phase 4
(repeat-sample validation), which is legitimate under the plan's own dependency graph -- Phase 5
depends only on Phase 2, not Phase 4 -- and was deliberately sequenced this way per the
resource-constraint instructions (do documentation work, which needs no test execution, while
heavier verification is deferred pending the concurrent oracle job).

## Verification results

```
grep -rn "ConcurrentConstructionError" code/docs/
=> code/docs/core/KNOWN_TEST_FAILURES.md:95, :97
=> code/docs/core/ARCHITECTURE.md:127

grep -n "NOTE:" code/src/model_checker/solver/type_guards.py
=> line 95, line 164

grep -nE "task [0-9]{2,}|\(task [0-9]+\)" <all files touched in phases 1-5, outside specs/>
=> none found (no task-number references introduced, per
   .claude/rules/no-task-references-in-deliverables.md)
```

## Next phase

Phase 4 (repeat-sample validation) and the remaining Phase 2/3 execution verification (iterate
suite, bimodal/logos unit suites, single runs of the two rewritten tests) are deferred until
`pgrep -af "run-oracle"` returns nothing, per the resource-constraint instructions in the
delegation context. Phase 6 (regression sweep) and Phase 7 (gated quarantine removal, blocked
regardless since Task 136 is not_started) follow after that.
