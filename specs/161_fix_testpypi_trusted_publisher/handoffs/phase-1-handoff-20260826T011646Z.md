# Phase 1 Handoff

**Immediate Next Action**: None for an agent. Phase 2 (register the trusted publisher on
test.pypi.org) and Phase 3 (verify on a real `v*` tag push) are both explicitly labelled
USER GATE / USER-ONLY in the plan. No further agent dispatch is expected for this task unless
the user reports Phase 2/3 issues that require a `release.yml` change.

**Current State**: Phase 1 is `[COMPLETED]`. `.github/workflows/release.yml` gained one
additive step, `Print OIDC claims (diagnostic only)`, as the first entry of the
`publish-testpypi` job's `steps:` list. Committed as
`task 161 phase 1: add OIDC claims diagnostic step to publish-testpypi`. Phases 2 and 3 remain
`[NOT STARTED]` — untouched, as required.

**Key Decisions Made**:
- Followed the plan's exact step shape verbatim (audience resolved at runtime with `testpypi`
  fallback, `set -euo pipefail`, step-level `continue-on-error: true`, four-claim `jq` whitelist).
- No deviations from the plan.

**Deviations from Plan**: None.

**What NOT to Try**: Do not attempt Phase 2 (test.pypi.org registration) or Phase 3 (real tag
push / CI verification) as an agent — both are USER GATE phases per
`.claude/rules/pr-prohibition.md` and the plan's explicit MUST-NOT-ATTEMPT language. Do not
push, tag, or open a PR.

**References**: Plan at
`specs/161_fix_testpypi_trusted_publisher/plans/01_fix-testpypi-trusted-publisher.md`,
Phase 1 (line 112). Research report at
`specs/161_fix_testpypi_trusted_publisher/reports/01_fix-testpypi-trusted-publisher.md`.
