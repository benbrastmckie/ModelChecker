# Phase 1 Handoff: Pinned tool manifest and runner scaffold

- **Status**: [COMPLETED]
- **Files created**:
  - `code/scripts/release-tools-requirements.txt` (3 exact pins: `build==1.5.0`,
    `twine==7.0.0`, `check-wheel-contents==0.6.3`, plus a rationale header)
  - `code/scripts/release-verify.sh` (executable, header contract, arg parsing,
    single-invocation `nix develop` re-exec guard, `note()`/`fail()`/`setup_fail()`/
    `record_step()` helpers, 10 named step stubs writing to `<out>/summary.txt`)

## Verification performed

- `bash -n code/scripts/release-verify.sh` — parses clean.
- `bash code/scripts/release-verify.sh --help` — prints usage, evidence-file table
  (12 files), and exit-code contract; exits 0 without entering `nix develop`.
- `bash code/scripts/release-verify.sh --out <tmpdir>` — entered `nix develop`
  once, ran all 10 stub steps, wrote `summary.txt` with one line per step, exited 0.
  Test output directory removed after verification.
- `grep -c '==' code/scripts/release-tools-requirements.txt` reports exactly `3`.
- `git status --porcelain` shows only the two new files under `code/scripts/`.
- `chmod +x` applied to the runner.

## Deviations from plan

- None. The bare positional `<REF>` alias was intentionally not implemented —
  the plan permitted this ("only if it does not complicate the parser;
  otherwise document `--ref` as the sole form") and `--ref VALUE` is documented
  as the sole form in both the header and `--help` output.

## Next phase

Phase 2 implements steps (a)-(d2): provisioning, build, twine check, and both
check-wheel-contents runs, replacing the current stubs with real logic.
