# Phase 4 Handoff: Checklist prose, reading guide, and scripts README entry

- **Status**: [COMPLETED]
- **Files modified**:
  - `.github/RELEASE_SETUP.md` — rewrote only the `### Local Rehearsal (No Publish)` section body
  - `code/scripts/README.md` — appended a `## release-verify.sh` section

## Verification performed

- `git diff .github/RELEASE_SETUP.md` shows a single hunk starting at
  `### Local Rehearsal (No Publish)` and ending exactly before
  `### Test Release Workflow (Dry Run on GitHub)` — confirmed via
  `git diff | grep '^@@'` (one hunk, `@@ -141,11 +141,69 @@`).
- The new section names all 12 evidence files (11 named files plus
  `summary.txt`); each name was cross-checked against the actual
  `record_step "<name>" ... "<evidence>"` calls in `release-verify.sh` and
  matches exactly (`grep -oE` over both files, manually diffed).
- The new section states all three exit codes (0/1/2) and the W002 reading
  guidance (expected nonzero, tracked separately, `--ignore W002` is the
  "anything NEW?" signal).
- `grep -n 'specs/archive/125' .github/RELEASE_SETUP.md` shows two hits: the
  pre-existing, untouched `PUBLISH-CHECKLIST.md` cross-reference (line 77,
  outside the rewritten section) and the new "Historical context only"
  sentence (line ~202) which explicitly states the archived
  `check-wheel-contents` result and sha256sums no longer reproduce — never
  presented as current evidence.
- Every filesystem path cited in the new prose was checked with `test -e`:
  `code/scripts/release-verify.sh`, `code/scripts/release-tools-requirements.txt`,
  `specs/archive/125_release_engineering_and_pypi_rehearsal/rehearsal/` — all present.
- `code/scripts/README.md`'s new `## release-verify.sh` section follows the
  file's existing `## <script>` + `### Usage` + `### Flags` + `### Output Files`
  convention (confirmed against the `## quantifier_benchmark.py` and
  `## test_cvc5_stability.py` precedent sections in the same file).
- `### Test Release Workflow (Dry Run on GitHub)` and the
  `PUBLISH-CHECKLIST.md` cross-reference in "Ordered Release Steps" are
  byte-unchanged (outside the single diff hunk).

## Deviations from plan

- None.

## Next phase

Phase 5 is the real end-to-end run: execute `release-verify.sh` against
`specs/156_portable_pinned_release_verification_runner/rehearsal/`, archive
the evidence, and report per-step outcomes quoted from the produced files.
Phases 1-3's interim test runs (in scratch directories, already cleaned up)
already exercised the full pipeline successfully multiple times, including a
`--ref` override and a simulated provisioning failure, so Phase 5's own run is
expected to succeed; the official archived evidence has not yet been
produced.
