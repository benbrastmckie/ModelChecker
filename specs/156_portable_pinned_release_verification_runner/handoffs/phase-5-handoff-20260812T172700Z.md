# Phase 5 Handoff: End-to-end run and evidence report

- **Status**: [COMPLETED]
- **Files added**: `specs/156_portable_pinned_release_verification_runner/rehearsal/` (12 evidence
  files, the official archived run)

## The run

```
bash code/scripts/release-verify.sh --out specs/156_portable_pinned_release_verification_runner/rehearsal/
```

Exit code **0**. Terminal summary: `All hard-gate checks passed`, evidence directory named
verbatim. Full per-step outcomes (quoted from the produced files) are in the implementation
summary.

## Verification performed

- Baseline `git status --porcelain` recorded before the run.
- All 12 evidence files present, non-empty (`find ... -empty` returned nothing).
- Archived-name mapping confirmed via `diff` against
  `specs/archive/125_release_engineering_and_pypi_rehearsal/rehearsal/`'s listing with
  `1.2.12` substituted for `<REF>` — zero differences beyond the two documented additions.
- `summary.txt` demonstrates the record-and-continue contract in the real archived run:
  `d1-wheel-contents info exit=1` followed immediately by `d2-wheel-contents-w002 gate exit=0`
  and every later step, `FAILURES=0`.
- Post-run `git status --porcelain`: only the new `rehearsal/` directory appeared under the task
  directory; no change under `code/` (code/dist/ is gitignored and does not appear);
  `git diff --name-only` contains neither `flake.nix` nor any `.github/workflows/` path.
- `--ref 1.2.10` confirmation run (scratch directory, not archived) produced correspondingly
  renamed `pip-download-1.2.10.log` / `ref-1.2.10-wheel-files.txt`, then was deleted.
- No inaccuracy in the Phase 4 prose was found — every filename, exit code, and described
  behavior in `.github/RELEASE_SETUP.md` and `code/scripts/README.md` matched what the real run
  produced. No documentation correction was needed.

## Deviations from plan

- None.

## Task complete

All 5 phases closed. See
`specs/156_portable_pinned_release_verification_runner/summaries/01_release-verify-runner-summary.md`
for the full per-step evidence report.
