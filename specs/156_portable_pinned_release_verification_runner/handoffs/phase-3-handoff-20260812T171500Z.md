# Phase 3 Handoff: Reference fetch, parity diffs, hashes, and the parity report (steps e-f)

- **Status**: [COMPLETED]
- **Files modified**: `code/scripts/release-verify.sh` (steps e1-f and `generate_parity_diff`
  implemented, replacing stubs; `setup_fail()` gained an optional `label` parameter so step
  (e1) failures are named `REFERENCE FETCH FAILED:` distinctly from provisioning's
  `SETUP FAILED:`)

## Verification performed (real, networked, full end-to-end runs)

- `bash -n code/scripts/release-verify.sh` — parses clean.
- Full real invocation produced all **12 files**, all non-empty:
  `build.log`, `twine-check.txt`, `wheel-contents.txt`,
  `wheel-contents-ignore-w002.txt`, `new-wheel-files.txt`,
  `ref-1.2.12-wheel-files.txt`, `wheel-files-diff.txt`, `top-level-dir-diff.txt`,
  `pip-download-1.2.12.log`, `sha256sums.txt`, `parity-diff.md`, `summary.txt`
  — matching the archived rehearsal's 10 names (with `<version>` substituted)
  plus exactly the two documented additions.
  - `sha256sums.txt` contained exactly 3 lines (new wheel, new sdist,
    reference wheel).
  - `parity-diff.md` carried the run's `<REF>` (1.2.12), the built version
    (1.3.0, parsed from the wheel filename), the UTC run timestamp, the
    pinned tool versions, an Artifact Identity table with real SHA256 values,
    a File Count Summary (514 reference files vs. 478-488 new-build files,
    varying slightly run to run — see note below), and a Classified
    Differences section that explicitly defers classification to a human
    reviewer rather than fabricating a verdict.
  - `wheel-files-diff.txt` / `top-level-dir-diff.txt` reproduced the same
    dist-info-rename + `model_checker/solver/` shape the archived task 125
    evidence showed.
- **`--ref` override test**: `--ref 1.2.11` produced correspondingly renamed
  `pip-download-1.2.11.log` and `ref-1.2.11-wheel-files.txt` in a second run,
  confirming the parameter is not hardcoded at any call site.
- **Simulated race/flake observed and diagnosed, not a runner defect**: one
  interim run (mid-Phase-3, before the final verification run above) hit a
  `FileNotFoundError` at step (e2) because `code/dist/model_checker-1.3.0-py3-none-any.whl`
  vanished between step (b) build and step (e2) — `git status --porcelain`
  taken immediately afterward showed **other tasks' files also changing
  concurrently** (`specs/157_.../plans/01_version-file-dedupe.md`), confirming
  another agent session is sharing this same working tree and independently
  touching `code/`/`code/dist/` during this window. An immediate retry with no
  code changes succeeded cleanly. This is an environmental hazard of running
  in a multi-agent-shared checkout, not a defect in `release-verify.sh`'s
  logic; Phase 5's real run will re-check `git status --porcelain` immediately
  before and after to detect (not silently absorb) any such interference.

## Deviations from plan

- `setup_fail()` was extended with an optional `label` parameter (default
  `SETUP FAILED`) rather than being duplicated into two functions, so step
  (e1)'s reference-fetch failure is now correctly labeled
  `REFERENCE FETCH FAILED:` per the plan's Risks & Mitigations table, distinct
  from provisioning's `SETUP FAILED:` label. This is an implementation detail
  addition, not a scope change — the plan's own Risks table already named both
  labels as required.

## Next phase

Phase 4 rewrites `.github/RELEASE_SETUP.md`'s "Local Rehearsal (No Publish)"
section and adds the `code/scripts/README.md` entry (prose only, no script
logic changes).
