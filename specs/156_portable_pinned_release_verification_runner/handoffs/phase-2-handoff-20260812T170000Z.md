# Phase 2 Handoff: Provisioning, build, and the two check tools (steps a-d)

- **Status**: [COMPLETED]
- **Files modified**: `code/scripts/release-verify.sh` (steps a-d2 implemented, replacing stubs)

## Verification performed (real, networked runs inside `nix develop`)

- `bash -n code/scripts/release-verify.sh` — parses clean.
- Full real invocation (`--out <scratch-dir>`): reached step (d2) with all of
  `build.log`, `twine-check.txt`, `wheel-contents.txt`,
  `wheel-contents-ignore-w002.txt` non-empty.
  - Step (a) provisioned `build==1.5.0`, `check-wheel-contents==0.6.3`,
    `twine==7.0.0` into a venv under `$TMPDIR`; versions recorded in
    `summary.txt`.
  - Step (b) built `model_checker-1.3.0-py3-none-any.whl` and
    `model_checker-1.3.0.tar.gz` fresh into `code/dist/`.
  - Step (c) `twine check --strict`: both artifacts `PASSED`.
  - Step (d1) bare `check-wheel-contents` exited **1** with `W002: Wheel
    contains duplicate files` for the four `theory_lib/*/VERSION` files —
    recorded in `summary.txt` as `info exit=1`, run continued into (d2)
    unaborted, `FAILURES` stayed at `0`.
  - Step (d2) `--ignore W002` exited **0** (`OK`) — no findings beyond W002.
- `summary.txt` demonstrated the record-and-continue contract concretely:
  `d1-wheel-contents info exit=1 ...` followed immediately by
  `d2-wheel-contents-w002 gate exit=0 ...` and all subsequent (still-stub)
  steps.
- Simulated provisioning failure: temporarily moved
  `code/scripts/release-tools-requirements.txt` aside, re-ran the script. It
  exited **2** with `SETUP FAILED: could not provision pinned release tools:
  pip install failed (network required)`, and the evidence directory
  contained only `summary.txt` (with the `SETUP FAILED` line) — no
  downstream evidence file was written, confirming no partial
  success-looking evidence set is produced. The requirements file was
  restored immediately after the test and confirmed present.
- `git status --porcelain -- code/ .github/` after all tests: only
  `code/scripts/release-verify.sh` modified; `code/dist/` (gitignored) not
  shown; `code/scripts/release-tools-requirements.txt` unchanged.

## Deviations from plan

- None.

## Next phase

Phase 3 implements steps (e1)-(f) (reference fetch, file listings, diffs,
hashes) and `parity-diff.md` generation, completing the 11-file evidence set
(12 with `summary.txt`).
