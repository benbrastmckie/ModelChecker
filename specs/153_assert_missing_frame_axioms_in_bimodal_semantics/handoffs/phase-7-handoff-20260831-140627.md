# Phase 7 Handoff: Full regression run, flip accounting, and final gate

**Status**: COMPLETED (phase work done; task-level status is PARTIAL, see summary)

## What was done

- Ran the full 52-example regression against the post-Phase-4 tree (`baseline` arm, real
  committed methods -- see the harness-artifact correction note below), writing
  `03_post-change-verdicts.json`.
- Diffed every key against both the task 152 recorded baseline and this task's own Phase 1
  pre-change run. 2/52 diverge: `BM_CM_4`, `BM_CM_1`. Every differing key has a written
  explanation in `baselines/README.md`.
- Confirmed `TN_CM_2` unchanged (already inconclusive pre-change in all three sources) and
  `BM_TH_1`/`BM_TH_2` unchanged inconclusive-at-~30.3s (no signal, per the plan's own rule).
  `BM_TH_3`/`BM_TH_4` stay `match` as predicted.
- Ran the full bimodal unit suite: 308 passed, 5 failed, all attributable to `BM_CM_1`/`BM_CM_4`
  and three counter-isolation parametrizations of `BM_CM_4`. Not green, and this is stated
  honestly rather than as a pass.
- Did **not** run the broader `code/tests/` suite in this session (time budget) -- recorded as
  an open item.

## Harness-artifact discovery mid-phase

The Phase 1 harness's `with_new_axioms` arm (a script-local inline reconstruction of the new
axioms, used before Phase 4 landed) was discovered to diverge from the real committed
`build_seriality_constraint`/`build_interpolation_constraint` methods on `BM_CM_4` specifically:
4.56s decided `match` via the inline reconstruction vs. 120s+ `inconclusive` via the real methods,
traced to Z3 MBQI/E-matching sensitivity to symbol naming and construction order, not a logic
difference. Phase 7's actual diff used the `baseline` arm (real disk code, unmodified) instead.
Documented in `baselines/README.md` with a correction note so the harness isn't misused this way
again.

## The blocker (task-level, not phase-level)

`BM_CM_4` regresses from 4.07s decided `match` to `inconclusive` at its own `max_time=120` with
both new axioms present -- four independent measurements against the real methods agree. `BM_CM_1`
shows the same direction but its own isolation table is non-monotonic and contradicts a
"superlinear interaction" mechanism claim, so no single mechanism is asserted. Full detail and all
evidence in the implementation summary and `baselines/README.md`.

Per explicit guidance received during implementation: no axiom was dropped, no `max_time` was
raised, no test was marked `unstable`/`xdist_serial`, and no expected verdict was adjusted to
route around this. Both axioms remain in the code as specified; landing is a user decision.

## Final artifacts

- `specs/153_assert_missing_frame_axioms_in_bimodal_semantics/summaries/01_seriality-interpolation-axioms-summary.md`
- `.return-meta.json` and `.orchestrator-handoff.json`, both status `partial`.
