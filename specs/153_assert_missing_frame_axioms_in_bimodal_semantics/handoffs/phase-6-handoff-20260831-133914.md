# Phase 6 Handoff: ARCHITECTURE.md frame-class ledger

**Status**: COMPLETED

## What was done

Added a new `### Frame-Class Axioms` subsection to
`code/src/model_checker/theory_lib/bimodal/docs/ARCHITECTURE.md`, directly after
`### Constraint Generation` and before `## Integration Strategy`. Confirmed it renders correctly
under `## Model Construction` in the document outline and that no Table of Contents edit was
needed (TOC lists `##`-level sections only).

Contents: a 7-row constraint-level table (nullity_identity, converse, forward_comp,
interpolation, seriality — asserted; Limit, Spherical — free) with per-row citations into
TaskFrame.lean/Frame.lean, plus:
- An explicit note resolving the "asserted axiom count" ambiguity: 5 asserted / 2 free at the
  Z3-constraint-row level, 2 asserted / 2 free at the paper-axiom level, with the reason the two
  counts differ stated rather than forcing one number.
- A duration-domain-guard footnote recording (not resolving) the bounded-window `(-M, M)` vs.
  the paper's unbounded `\Z` gap.

**Repo hygiene note**: the first draft used "Task 153" as a citation label in both
`ARCHITECTURE.md` and `core.py`'s docstring. Caught and corrected before commit — deliverable
files outside `specs/**` must not reference ephemeral task numbers (see
`.claude/rules/no-task-references-in-deliverables.md`); both files now cite durable anchors only
(method names, file paths) with no task-number language remaining anywhere in
`code/src/model_checker/theory_lib/bimodal/`.

## Next phase

Phase 7: full 52-example regression run against the post-Phase-4 tree, diffed against both the
task 152 recorded baseline and this task's own Phase 1 same-host pre-change run, with every flip
explained individually — including a full write-up of the BM_CM_4 cost regression already
characterized in the Phase 4 handoff.
