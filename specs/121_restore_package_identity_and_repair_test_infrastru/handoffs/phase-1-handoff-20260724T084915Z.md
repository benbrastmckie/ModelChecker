# Phase 1 Handoff - 20260724T084915Z

## Immediate Next Action
Phase 1 complete; proceed to Phase 2 (verify MANIFEST.in/package-data/version single-sourcing).

## Current State
- **File**: /home/benjamin/Projects/ModelChecker/code/pyproject.toml
- **Location**: `[project]` block and `[project.scripts]`
- **Work state**: `model-checker` identity fully restored (name, version 1.3.0, description,
  keywords, classifiers, dependencies, jupyter/all extras); single `model-checker` script; oracle
  entry-points table removed. Verified via `grep -n bimodal` and `tomllib` load.

## Key Decisions Made
1. Kept `networkx>=2.0` as a hard dependency (not an extra): `iterate/graph.py` imports it
   unconditionally at module scope.
2. Reused the pre-1.2.12 `model-checker` pyproject.toml (found via `git show ee5ba8ba:code/pyproject.toml`)
   as the reference baseline for description/keywords/dependencies/extras wording.

## What NOT to Try
1. Adding a `cvc5` extra: not requested by the plan's task list (only `jupyter`/`all` with 5 named
   packages); skipped to stay in scope.

## Critical Context (max 5 items)
- Plan: specs/121_restore_package_identity_and_repair_test_infrastru/plans/01_restore-package-identity-test-infra.md, Phase 1
- `[project.scripts]` must have exactly one entry (`model-checker`).
- Version `1.3.0` is provisional; the release task (parent 117) confirms the final number.

## References (read only if stuck)
- Plan: specs/121_restore_package_identity_and_repair_test_infrastru/plans/01_restore-package-identity-test-infra.md, Phase 1
- Pre-bimodal baseline: `git show ee5ba8ba:code/pyproject.toml`
