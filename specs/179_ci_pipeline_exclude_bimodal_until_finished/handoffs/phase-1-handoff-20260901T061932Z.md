# Phase 1 Handoff

**Immediate Next Action**: Begin Phase 2 (Confirm `unstable-watch.yml` stays non-gating) — its
evidence was already gathered opportunistically during Phase 1's wait window; finalize its
baseline-file section (already appended) and close it, then proceed to Phase 3.

**Current State**: Phase 1 [COMPLETED]. Plan file at
`specs/179_ci_pipeline_exclude_bimodal_until_finished/plans/01_ci-fast-green-bimodal-excluded.md`
is current. Baseline file created at
`specs/179_ci_pipeline_exclude_bimodal_until_finished/baselines/ci-wallclock-baseline.md` with
Phase 1 and Phase 2 sections both populated. Committed as `5680cad6`.

**Key Decisions Made**:
- Inherited `development`-marker state confirmed unchanged (bimodal blanket 313 items, oracle
  blanket 595/644 items exempting exactly the 6 `_SOUNDNESS_CORE_CLASSES`).
- Guard suite: 136 passed in 27.00s — matches plan's Scope Hypothesis exactly.
- `--collect-only` diff on `differential-tests.yml`'s two pytest steps: both select the identical
  49 node ids, diff empty — confirms the redundancy premise for Phase 4.
- `gh` CLI is installed and authenticated; historical CI numbers were re-measured directly (not
  merely copied from the research report) via `gh run view 32995122897/32995122906 --json jobs`
  and matched the report's table to the second.
- Local NON-GATING timing of the 49-item soundness core: first foreground attempt timed out at
  the 2-minute ceiling (expected, matches research report); background attempt (8-min budget)
  succeeded: 49 passed in 216.97s (~3m37s), about 68s slower than the CI-measured 3m14s despite
  more local cores — noted as an out-of-scope observation, not investigated further.
- Opportunistically completed Phase 2's evidence gathering (unstable-watch.yml triggers,
  continue-on-error on all 3 steps, no `needs:` reference, exclusion from `_SCANNED_FILES`, both
  named guard tests pass) while waiting on the Phase 1 background job — this is documentation-only
  investigation, not an edit, so it does not violate close-before-open; the Phase 2 heading has
  not yet been marked `[IN PROGRESS]`/`[COMPLETED]` and its plan-file heading transition plus
  final phase-boundary commit still needs to happen as the next actual step.

**Deviations from Plan**: None (Phase 1 followed the plan exactly). One incidental operational
note: an orphaned duplicate of the background local-timing pytest invocation was discovered
running concurrently partway through the observation window (from an earlier `nohup` attempt that
did not fully die on `kill`) and was killed once found; disclosed in the baseline file for
transparency. Does not affect the validity of the final single-process 216.97s measurement.

**What NOT to Try**:
- Do not attempt the 49-item soundness-core timing run in the foreground with the default 2-minute
  Bash ceiling — it reliably times out; use `run_in_background` with a several-minute budget.
- Do not treat the ~68s local-vs-CI timing gap as a regression or use it to justify widening any
  budget — out of scope per the plan's non-goals.

**References**: Plan at
`specs/179_ci_pipeline_exclude_bimodal_until_finished/plans/01_ci-fast-green-bimodal-excluded.md`,
currently at Phase 1 [COMPLETED], Phase 2 [NOT STARTED] (evidence pre-gathered).
