# Phase 5 Handoff

**Immediate Next Action**: Begin Phase 6 (final phase): consolidate the baseline file into one
before/after table across `tests.yml`, `differential-tests.yml`, and `flake.nix`'s
`checks.default`; state that `tests.yml`'s wall clock is bounded by `nix flake check` not the
Python matrix; record the out-of-scope follow-up (checkPhase ~50% slower than a matrix leg); record
the exact `gh run view` command for the repo owner's post-push capture; confirm `git status` /
`git log --oneline origin/master..HEAD` shows only local unpushed commits. This is the last plan
phase — once it closes, proceed to Stage 5 (overall verification), Stage 6 (implementation
summary), and Stage 7 (metadata file).

**Current State**: Phase 5 [COMPLETED], committed as `21f5a615`.

**Key Decisions Made**:
- `--collect-only` re-diff on the surviving gate step: still 49 node ids, empty diff against the
  Phase 1 baseline — no coverage lost by the Phase 4 collapse.
- `code/tests/ci/` re-run: 136 passed in 26.75s — same count as Phase 1.
- Broader gating regression check (`code/tests/ -m "not slow and not unstable and not
  development"`): **539 passed, 1 skipped, 110 deselected in 76.09s** — green.
- `differential-tests.yml`'s after-number is **derived, not measured post-push**: roughly
  3m14s-3m30s (down from the historical 8m22s), based on the surviving step's own historical
  per-step timing from Phase 1's `gh`-measured table. Explicitly labeled as derived in the
  baseline file, per the plan's own instruction not to fabricate a post-push number.
- Did not re-run the local NON-GATING 49-item timing a second time post-change (documented reason
  in the baseline file: the surviving step's test population and code are unchanged by this task,
  so a second run would only re-measure the same machine-contention variance, not reveal anything
  about the change).
- Did NOT run `bash oracle/run-oracle-suite.sh` at any point in this task, per TESTING_GUIDE.md
  8.8 and the plan's binding constraint.

**Deviations from Plan**: None.

**What NOT to Try**: Do not attempt to obtain a "real" post-push `differential-tests.yml` number
locally — it requires an actual push, which agents must not perform; Phase 6 records the exact
`gh run view` command for the repo owner to run themselves after pushing.

**References**: Plan at
`specs/179_ci_pipeline_exclude_bimodal_until_finished/plans/01_ci-fast-green-bimodal-excluded.md`,
Phases 1-5 [COMPLETED], Phase 6 [NOT STARTED] next (final phase).
