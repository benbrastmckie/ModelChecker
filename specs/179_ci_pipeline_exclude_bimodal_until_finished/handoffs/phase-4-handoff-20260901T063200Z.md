# Phase 4 Handoff

**Immediate Next Action**: Begin Phase 5 (post-change verification and after-measurement):
re-run `--collect-only` on the surviving gate step and diff against Phase 1's recorded 49-node-id
baseline; run the broader `code/tests/ -m "not slow and not unstable and not development"` gating
regression check; re-run `code/tests/ci/` and record count/duration; append to the baseline file.

**Current State**: Phase 4 [COMPLETED], landed as one atomic commit `7b55a59c` spanning exactly the
six planned files: `.github/workflows/differential-tests.yml` (redundant first pytest step
deleted), `code/tests/ci/test_unstable_deselection_wiring.py` (constant 7->6, invocation
assertion 2->1, anchors renamed `_INVOCATION_COUNT_ANCHORS` and inverted to require "six"/forbid
"seven", two test methods renamed with updated docstrings), `code/docs/core/TESTING_GUIDE.md`
(five count occurrences flipped plus the "first invocation" phrase replaced with an accurate
description and a new sentence explaining why the count dropped), and one docstring/prose phrase
each in `bimodal/tests/conftest.py`, `bimodal/tests/README.md`, and
`test_development_marker_application.py`.

**Key Decisions Made**:
- The `grep -rni "seven"` sweep found exactly the plan's enumerated in-scope set (8 prose
  occurrences across 4 non-test files plus the constants/anchors in
  `test_unstable_deselection_wiring.py` itself) — no additional in-scope occurrence was found, so
  no scope expansion was needed.
- `TestOracleSoundnessGateStaysUnconditionallyGating`'s four tests pass completely unmodified.
- `test_differential_tests_yml_gate_step_has_no_marker_expression` was left unmodified per the
  plan's explicit instruction (its "second invocation" docstring wording is now slightly loose
  since there is only one invocation left, but the plan named this test as one to re-verify passes
  unmodified, not to edit).
- `EXPECTED_GATING_MARKER_INVOCATIONS` moved from 7 to 6; `test_scanned_invocation_counts_match_known_shape`'s
  `DIFFERENTIAL_TESTS_YML` count assertion moved from `== 2` to `== 1`.
- 136 tests pass in `code/tests/ci/` — same count as the Phase 1 baseline.

**Deviations from Plan**: None. All six files matched exactly what the plan enumerated; no
in-scope grep hit was missed or deferred.

**What NOT to Try**: Do not re-open this commit to "fix" `test_differential_tests_yml_gate_step_has_no_marker_expression`'s
"second invocation" docstring wording — the plan explicitly named it as unmodified-and-still-passing,
not as an edit target.

**References**: Plan at
`specs/179_ci_pipeline_exclude_bimodal_until_finished/plans/01_ci-fast-green-bimodal-excluded.md`,
Phases 1-4 [COMPLETED], Phase 5 [NOT STARTED] next.
