# Phase 3 Handoff

**Immediate Next Action**: Begin Phase 4 (Collapse the `differential-tests.yml` redundancy,
atomic batch — six files, one commit). Pre-edit sweep and pre-checks already completed
opportunistically during Phase 1's wait window (see below); the actual edits have not yet been
applied.

**Current State**: Phase 3 [COMPLETED]. `code/docs/core/TESTING_GUIDE.md` section 8.14 repaired:
one reconciliation sentence added to the "Why a bimodal-only edit..." paragraph, one
rejected-alternative note added to the "Soundness stays gating" bullet, the "What it must not
hide" paragraph rewritten to state the true invariant (development IS registered in
oracle/conftest.py, exempting exactly the six soundness-core classes), and the "producing
workflow step does not exist yet" paragraph rewritten to describe `watch_development` as
implemented and passing. Committed as `5c7b3f5e`.

**Key Decisions Made**: None new beyond what the plan already specified — Phase 3 recorded the
plan's pre-resolved decision (keep the soundness gate) rather than re-litigating it.

**Deviations from Plan**: None.

**What NOT to Try**: Do not touch any "seven"/"six" count phrasing or the "first invocation"
phrase in `TESTING_GUIDE.md` during any future work believed to belong to Phase 3 — that is
exclusively Phase 4's job, and Phase 4 is not yet started.

**Phase 4 prep already done (read-only, no edits yet)**:
- `grep -rni "seven" --include=*.py --include=*.md --include=*.yml --include=*.nix --include=*.sh
  --include=*.toml code/ .github/ oracle/ flake.nix` run and every hit classified. In-scope
  (exactly matches the plan's enumerated set, 8 prose occurrences across 4 files plus the
  `test_unstable_deselection_wiring.py` constants/anchors themselves): `TESTING_GUIDE.md` (lines
  995, 1440, 1441, 1446, 1588 pre-Phase-3-edit line numbers — re-verify after Phase 3's edits
  shifted some line numbers below 1465), `bimodal/tests/conftest.py:25`,
  `bimodal/tests/README.md:10`, `test_development_marker_application.py:13`. Out-of-scope hits
  confirmed unrelated: `test_example_budget_floor.py` ("seven files"), `TESTING_GUIDE.md` "seven
  workshops"/"seven closed encoding-tuning avenues"/"seven: those four plus" (example-budget
  scope, unrelated), `models/tests/conftest.py`'s `'seven'` bitvector literal, `.github/
  RELEASE_SETUP.md`'s "seven jobs"/"seven-job topology" (release topology, unrelated), all Logos
  extensional-subtheory README mentions ("seven operators", unrelated), and
  `oracle/bimodal_logic/tests/test_cross_oracle_differential.py:198`'s "The seven timing-out
  formulas" (a specific historical incident count, unrelated to the gating-invocation count).
- Confirmed `GATING_RECHECK_SOLVE_TIMEOUT_MS`'s comment block in
  `test_cross_oracle_differential.py` (around line 187) carries the full 20000->40000
  justification the workflow's about-to-be-deleted comment defers to.
- Read `test_unstable_deselection_wiring.py` in full: `EXPECTED_GATING_MARKER_INVOCATIONS`
  (line 51), `_SEVEN_COUNT_ANCHORS` (lines 74-112), `test_scanned_invocation_counts_match_known_shape`
  (asserts `DIFFERENTIAL_TESTS_YML` invocations == 2, to become == 1),
  `test_total_gating_marker_expression_count_is_seven`,
  `test_seven_count_anchor_is_corrected`, and
  `test_differential_tests_yml_gate_step_has_no_marker_expression` (must still pass unmodified) —
  all located and their exact text confirmed.
- Confirmed `.github/workflows/differential-tests.yml`'s exact step boundaries for the step-1
  deletion (the entire "Run differential tests (non-slow, no BimodalHarness)" step including its
  large comment block, from `- name: Run differential tests...` up to but not including
  `- name: Run CI gate tests explicitly`).

**References**: Plan at
`specs/179_ci_pipeline_exclude_bimodal_until_finished/plans/01_ci-fast-green-bimodal-excluded.md`,
Phases 1-3 [COMPLETED], Phase 4 [NOT STARTED] next (atomic-batch commit mode — do not commit until
the full six-file set is edited and the full verification block passes).
