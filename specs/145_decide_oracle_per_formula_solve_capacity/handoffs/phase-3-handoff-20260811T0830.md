# Phase 3 Handoff — Bounded-Budget Recalibration (2026-08-11)

## Landed

- `test_mixed_and_box_next` timeout_ms 60000 → 240000 + docstring refresh
  (`oracle/bimodal_logic/tests/test_oracle_interface.py`), inline basis: uncensored
  92.8-104.2 s tail, 2.3x convention, `ea516a4b`/`3c0cf210` provenance.
- BM_CM_4 `max_time` 30 → **120** (DEVIATION from planned 60, per Phase 1 probe: 57.12 s
  worst draw → 120 = 2.1x) at both sync'd sites:
  `code/src/model_checker/theory_lib/bimodal/examples.py` and
  `oracle/bimodal_logic/tests/test_boundary_regression.py` (+ two stale docstring refreshes).
- Pass-2 headroom arithmetic note added at `ORACLE_PASS2_TIMEOUT` comment in
  `oracle/run-oracle-suite.sh`; value unchanged at 1800.

## Verified (inside nix develop; do NOT override PYTHONPATH — devShell sets
`code/src:../BimodalHarness/src`; overriding drops the harness path and breaks imports)

- `test_mixed_and_box_next` PASSED in 63.03 s — over the old 60 s budget, under the new one.
- `test_countermodel_bm_cm4_at_example_settings` + `test_regression_all_active_examples[BM_CM_4]`
  PASSED (44.75 s combined).
- Greps confirm both `max_time` sites read 120 with bases; 240000 carries basis; no
  task-number citations added.

## Next: Phase 4 (fallback branch)

Relocate ternary test to `xdist_serial`, per-leg 480000 ms override for `next_A`, inline
adjudication + both failed probe results. Then Phase 5 re-pin 627/609/16/2.
