# Phase 3 Handoff: Route decision gate

**Status**: COMPLETED

## Decision

**Route A** selected. Both formulas' rlimit spread is 0% (bit-identical across 3 draws each),
well under the 5% threshold; both have >= 2 completed draws; no ceiling event. Neither Route B
trigger condition applies.

## Plan file updates

- Phase 3 heading -> `[COMPLETED]`, checklist ticked.
- Phase 6 (Route B, untaken) heading -> `[COMPLETED WITH EXCLUSIONS]` with a
  `#### Reasoned Exclusions` table naming this measurement.
- ROUTE DECISION section written into
  `specs/167_flaky_testmixedformulas_failures/measurements/01_default-seed-probe.md`.

## Next phase

Phase 4 (Route A: plumb `max_rlimit` through the oracle provider) is next -- depends on Phase 3
(satisfied). Phase 6 is closed and will not be opened.
