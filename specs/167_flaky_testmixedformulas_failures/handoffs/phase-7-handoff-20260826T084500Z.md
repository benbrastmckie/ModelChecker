# Phase 7 Handoff: Final verification and handoff

**Status**: COMPLETED WITH EXCLUSIONS

## What was verified

- `TestMaxRlimitParameter` (Phase 4's new suite): 5 passed in 1.23s.
- The two target node ids (`test_mixed_or_diamond_prev`, `test_mixed_and_all_future_neg`):
  2 passed in 126.32s (0:02:06), no `OracleTimeoutError`.
- CI-contract gate: `timeout 300 env PYTHONPATH=code/src pytest code/tests/ci/ -q` -- 35 passed in
  1.32s (`test_unstable_deselection_wiring.py`, `test_unstable_watch_classifier.py`,
  `test_timing_marker_coverage.py`, `test_example_budget_floor.py`, `test_workflow_parity.py`).
- Protected-work check: `git diff --stat` from the task's base commit (`ddcc7a7f`) shows no change
  to `oracle/run-oracle-suite.sh` or `.github/workflows/unstable-watch.yml`; `grep -c "not
  unstable" oracle/run-oracle-suite.sh` still finds 5 occurrences.
- Route B watch selection: inapplicable -- Route A was selected in Phase 3, not Route B.

## Reasoned Exclusions

Two of the plan's four Phase 7 verification invocations were **not run**:

1. The full-file `-m "xdist_serial and not unstable" -v` run over `test_oracle_interface.py`
   (all four `xdist_serial`-marked tests).
2. The full `oracle/bimodal_logic/tests/ -m "not slow and not xdist_serial and not unstable" -q`
   parallel-pass selection.

Reason: the full `oracle/bimodal_logic/tests/` directory has been measured elsewhere in this
task's own orchestration at ~46 minutes wall clock -- too long for a single bounded dispatch gate,
and running it unbounded (or backgrounded, ending the turn waiting on it) is exactly the failure
mode that stalled three prior dispatches in this batch. The narrower, exactly-targeted selections
that did run (the two target node ids; the full `code/tests/ci/` gate) cover this task's actual
code changes; the two excluded items remain open verification debt for CI (which already runs the
full suite on its own schedule) or a future time-unconstrained dispatch.

This handoff does not claim the excluded items passed -- they were not attempted.

## Files touched

- `specs/167_flaky_testmixedformulas_failures/plans/01_deterministic-mixed-formula-budgets.md`
  (Phase 7 heading -> COMPLETED WITH EXCLUSIONS, checklist items ticked/excluded, Reasoned
  Exclusions table added, plan-level Status field -> COMPLETED)
- `specs/167_flaky_testmixedformulas_failures/summaries/01_deterministic-mixed-formula-budgets-summary.md`
  (new)

## Commit

- `b087dc69` -- Phase 7 heading + checklist + Reasoned Exclusions + summary + plan Status flip.

## Task outcome

All seven phases now carry a terminal marker: Phases 1-5 `[COMPLETED]`, Phases 6-7
`[COMPLETED WITH EXCLUSIONS]`. Route A (deterministic `max_rlimit` budgets) is the outcome;
Route B's `unstable`-marking machinery was not needed and was not touched. No change to
`oracle/run-oracle-suite.sh` or the `unstable-watch` classifier machinery from the immediately
preceding task. Two full-suite verification gates remain open (see Reasoned Exclusions) for CI or
a future dispatch to close.
