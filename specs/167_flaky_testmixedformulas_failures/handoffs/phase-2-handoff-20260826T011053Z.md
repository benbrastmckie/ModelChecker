# Phase 2 Handoff: Bounded measurement campaign

**Status**: COMPLETED (full campaign, no ceiling event)

## Result summary

| Formula | Draws | rlimit values | Spread | wall_s range |
|---|---|---|---|---|
| `mixed_and_all_future_neg` | 3 | 363423989 / 363423989 / 363423989 | 0% (exact) | 104.70-105.81 |
| `mixed_or_diamond_prev` | 3 | 250005414 / 250005414 / 250005414 | 0% (exact) | 68.53-70.73 |

Both formulas' rlimit is bit-identical across all 3 default-seed draws. This is a strong positive
determinism signal: the pass/fail boundary is measured in the wrong unit (wall clock), exactly as
the plan's central hypothesis predicted. wall_s varies (host-load-driven) while rlimit does not
(Z3-search-cost-driven) -- the direct separation Phase 2 exists to make.

Both verbatim failure texts (provider-level `OracleTimeoutError.__str__()` and pytest-level JUnit
`<failure>` text) were captured and are recorded in the measurement log, confirming the plan's
stated classifier defect directly: the pytest failure text does NOT contain `Test failed for
example:` (`FAILURE_SIGNATURE`).

CI history query: no CI serial-failure history exists for either target test, because no GitHub
Actions workflow currently invokes `oracle/bimodal_logic/tests/test_oracle_interface.py`'s
regular (non-`unstable`) selection at all -- a structural finding, not just an unavailability
record.

Campaign wall clock: ~11.5 minutes, well inside the 45-minute ceiling. No draw hit its
per-invocation timeout. The conditional `PYTHONHASHSEED=0` control did not fire (no rlimit
disagreement observed).

## Files touched

- `specs/167_flaky_testmixedformulas_failures/measurements/01_default-seed-probe.md` (new --
  the phase's only durable output, per the plan)
- `specs/167_flaky_testmixedformulas_failures/plans/01_deterministic-mixed-formula-budgets.md`
  (Phase 2 heading -> COMPLETED, checklist ticked, conditional item annotated as skipped-not-
  triggered)

## Next phase

Phase 3 (route decision gate). Given the 0% rlimit spread on both formulas (well under the 5%
threshold) and >= 2 completed draws per formula, the decision rule points unambiguously to
**Route A**. Phase 3 still needs to formally apply the rule and write the ROUTE DECISION section
into the measurement log, then mark Phase 6 (the untaken route) `[COMPLETED WITH EXCLUSIONS]`
with a `#### Reasoned Exclusions` record.
