# Phase 5 Handoff: Route A -- Apply calibrated budgets and record the fourth investigation

**Status**: COMPLETED

## What was built

- `oracle/bimodal_logic/tests/test_oracle_interface.py`:
  - `test_mixed_or_diamond_prev`: `max_rlimit=800000000` (~3.2x the Phase 2 measured 250005414),
    `timeout_ms=TEMPORAL_SOLVE_TIMEOUT_MS` (180000ms, ~2.5x the measured 70.73s worst draw).
  - `test_mixed_and_all_future_neg`: `max_rlimit=1100000000` (~3.0x the Phase 2 measured
    363423989), `timeout_ms=240000` explicit (180000ms clears only ~1.7x the measured 105.81s
    worst draw, short of the plan's own >=2x wall-clock headroom rule).
  - Both docstrings append a "Fourth investigation (2026-08-26)" record, preserving the three
    prior investigations' history rather than replacing it: the default-seed rlimit measured, the
    draw count, the Z3 version (4.16.0) the figure is valid for, an explicit recalibration trigger
    on a future Z3 upgrade, and a note that the previously recorded 80.6s/107.4s tail was measured
    under pinned non-default seeds and does not describe the production (default-seed) draw.
    `test_mixed_and_all_future_neg`'s docstring additionally states its standing "if this test
    ever fails SERIALLY, recalibrate" watch item is now resolved and the prior 60000ms figure is
    superseded.
  - Both `@pytest.mark.xdist_serial` markers are unchanged.

## Scope Hypothesis outcome

Confirmed: `TEMPORAL_SOLVE_TIMEOUT_MS` (180000ms) provides adequate headroom (>=2x the slowest
completed draw) for `test_mixed_or_diamond_prev` (~2.5x) but not for `test_mixed_and_all_future_neg`
(~1.7x) -- per the Scope Hypothesis's own "confirm at implementation time" instruction, the second
test was given its own larger explicit `timeout_ms=240000` with the measured basis recorded
inline, rather than adopting the shared constant for tidiness.

## Deviation from plan: narrowed verification gate

The plan's Phase 5 verify line is:
`timeout 900 env PYTHONPATH=code/src pytest oracle/bimodal_logic/tests/test_oracle_interface.py -m xdist_serial -v`
(all four `xdist_serial`-marked tests in the file, not just the two this phase modifies).

This dispatch narrowed the verify step to exactly the two target node ids:
`PYTHONPATH=code/src pytest
"oracle/bimodal_logic/tests/test_oracle_interface.py::TestMixedFormulas::test_mixed_or_diamond_prev"
"oracle/bimodal_logic/tests/test_oracle_interface.py::TestMixedFormulas::test_mixed_and_all_future_neg"
-q`
run foreground, no `timeout` prefix needed (completed in 126.32s, comfortably inside a bounded
dispatch gate). Result: exit 0, 2 passed in 126.32s (0:02:06), no `OracleTimeoutError` on either
test. The full four-test `xdist_serial` selection is deferred to Phase 7's own (analogously
narrowed) gate set, per the same rationale Phase 4 already recorded.

## Verification performed

- The two target node ids: 2 passed in 126.32s.
- `grep -n xdist_serial` confirmed both markers intact immediately above their respective `def`.
- `git diff` reviewed to confirm prior docstring history (three earlier investigations) is
  preserved, not overwritten.

## Files touched

- `oracle/bimodal_logic/tests/test_oracle_interface.py`
- `specs/167_flaky_testmixedformulas_failures/plans/01_deterministic-mixed-formula-budgets.md`
  (Phase 5 heading -> COMPLETED, checklist items ticked, deviation note added)

## Commit

- `d13f7823` -- code + docstrings + heading flip + checklist ticks + deviation note (single
  commit; the two hunks land together as one coherent phase-5 change).

## Next phase

Phase 7 (final verification and handoff) is next, depends on Phases 5 and 6 (both satisfied --
Phase 6 closed `[COMPLETED WITH EXCLUSIONS]` since Route A, not Route B, was selected in Phase 3).
