# Phase 6 Handoff: Full green test gate via pytest-xdist

**Task**: 122
**Phase**: 6 of 7
**Status**: COMPLETED

## What was done

Ran the everything-else full suite (`code/tests/ code/src/model_checker`, excluding the
already-measured in-package bimodal tests) at `-n 6`: 1880 tests, 1852 passed, 28 failed, 0
errors, 47.4s (fast enough to run in the foreground). All 28 failures were re-run serially
(`-n 0`) to rule out the CPU-contention pattern found in Phases 4-5: all 28 reproduced
identically -- deterministic, pre-existing, none traced to task 122's source edits.

Root-caused and categorized all 28 into 8 categories (A-H, full detail in
`baselines/rest-suite-disposition.md`): 6 exactly match Phase 2's already-documented `builder/`
pre-existing failures; the remaining 22 are newly documented, the largest cluster (12 tests)
tracing to a shared malformed `"A[]"` test-formula literal in
`code/tests/utils/helpers.py::create_test_model()` that pre-dates this task. None were fixed
(out of scope for the differential/oracle gate this task targets); a follow-up task is
recommended in the disposition document.

## Files touched

- `specs/122_.../baselines/junit-rest.xml`, `rest-run.txt` -- full `-n 6` run artifacts.
- `specs/122_.../baselines/rest-failures-serial-rerun.txt` -- serial reproduction confirmation.
- `specs/122_.../baselines/rest-suite-disposition.md` -- new, full root-cause + categorization
  with final summary table and follow-up recommendation.
- `specs/122_.../plans/01_rootcause-differential-green-gate.md` -- Phase 6 tasks checked off,
  heading marked `[COMPLETED]`.

## Deviations from plan

- Used `-n 6` (not `-n auto`), consistent with Phases 4-5.
- Used root-scoped explicit paths (`code/tests/ code/src/model_checker --ignore=...`), per
  Phase 1's collection-scoping finding (a bare `pytest --collect-only` from repo root walks the
  whole repo including `code/boneyard/`).
- The run was fast enough (47.4s) to execute in the foreground with a generous timeout rather
  than backgrounding it, per the coordinator's explicit guidance mid-task.
- Did not attempt to fix any of the 28 failures -- confirmed none relate to task 122's declared
  scope (bimodal differential / oracle gate) or its own source edits; thorough root-cause
  documentation was judged sufficient per the plan's "documented/justified skips/xfails only"
  language and the established Phase 2 precedent (6 pre-existing builder failures documented,
  not fixed).

## Next steps

Phase 7: consolidate the release baseline (`RELEASE-BASELINE.md`) composing the Phase 4 bimodal
tally, Phase 5 oracle tally, and this Phase 6 everything-else tally, plus all disposition
documents and the CLI smoke-test summary from Phase 2.
