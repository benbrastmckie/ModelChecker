# Phase 2 Handoff: Fix the stale output import in builder/module.py and pass CLI smoke tests

- **Status**: COMPLETED

## Work State

- `code/src/model_checker/output/__init__.py`: now exports `OutputManager`, `OutputConfig`,
  `create_output_config`, and `ModelDataCollector` (the last one an addition beyond the plan's
  literal text -- see below).
- `code/src/model_checker/builder/module.py`: `_initialize_output_management()` no longer imports
  the deleted `SequentialSaveManager`/`ConsoleInputProvider`; `config.sequential` truthy now
  raises `NotImplementedError` with a clear message instead. `_prepare_model_data()`'s
  `ModelDataCollector` import now resolves (see below).
- **Second stale import found and fixed**: `module.py:283`'s `ModelDataCollector` import was also
  broken (same commit `71ef79a1` deletion, never restored by task 119). Restored
  `code/src/model_checker/output/collectors.py` verbatim from git history and exported it -- this
  backs the still-supported `--save json` path, is non-interactive, and is not covered by the
  plan's non-goal (which names only SequentialSaveManager/ConsoleInputProvider).
- CLI smoke tests all green: `--help` (exit 0), representative bimodal example run (exit 0),
  `--save markdown json` (exit 0, valid MODELS.json + EXAMPLES.md), `--maximize` (exit 0).
- Full `builder/` suite regression check (not literally required by the plan phase text, but
  necessary to validate the fix didn't regress anything): 60 failed/199 passed before -> 6
  failed/249 passed after, zero new failures. Fixed a latent test-fixture bug (Mock() objects
  without an explicit `sequential=False` auto-create a truthy attribute, previously masked by the
  unconditional ImportError) across `mock_objects.py`, `test_build_module_theories.py`,
  `test_example.py`, `test_performance.py`, and rewrote one test
  (`test_output_directory_guidance.py::test_interactive_mode_integrates_output_directory_prompts_correctly`)
  that patched the now-nonexistent `ConsoleInputProvider`.
- 6 remaining builder-suite failures are pre-existing and unrelated (display-format drift, timing
  assertions, missing method, default-theory mismatch, serialization format); documented in
  `baselines/cli-smoke.txt` / `baselines/builder-suite-pre-existing-failures.txt` as a known gap,
  not masked as green. One flaky pre-existing test also documented, not fixed.

## Continuation Context

Phase 3 (oracle differential root-cause) can proceed independently -- disjoint files. Phases 4-6
(heavy backgrounded runs) depend on Phase 2's source edits being final, which they now are.

## Blockers

None.
