# Phase 4 Handoff — Ternary Fallback Landed (2026-08-11)

Fallback branch (not substitution): `test_all_sat_task_relation_ternary` now carries
`@pytest.mark.xdist_serial` and a per-leg 480000 ms override for `next_A` (sat_formulas
entries are now (name, formula, timeout_override) triples; TEMPORAL_SOLVE_TIMEOUT_MS
untouched). Full adjudication + both failed candidate probes + accepted ~1-in-7 residual
recorded inline in the docstring and at the list entry. Verified: test PASSED serially in
99.33 s (next_A leg ~99 s — over the old comfort band, well under 480 s). All five legs
hard-asserting; no xfail/skip.

Next: Phase 5 — relocate test_mixed_and_all_future_neg + Step 3 re-pin. NOTE: pins must
be 627/609/16/2 (TWO relocations this task: all_future_neg AND the ternary test).
