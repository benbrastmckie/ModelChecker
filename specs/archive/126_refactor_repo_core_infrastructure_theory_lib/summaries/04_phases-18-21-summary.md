# Implementation Summary: Task #126, Phases 18-21

**Completed**: 2026-07-24
**Duration**: ~1 session (continuation dispatch)

## Overview

Completed waves 7-8 (phases 18-21) of the core/theory_lib refactor plan, resuming from a
continuation handoff at 17/26 phases with phase 18 at 4/5 checklist items done. This dispatch
finished phase 18, then completed phases 19-21: folded the operator-free `relevance` subtheory
into `constitutive`, and fully normalized `bimodal` onto the same `semantic/` package contract
already established for imposition, exclusion, and logos -- including fixing a real, previously
undetected dual-class-identity bug in bimodal's pickling path.

## What Changed

### Phase 18 completion (logos semantic package round-trip)
- `code/src/model_checker/builder/tests/unit/test_serialize.py` -- added
  `test_serialize_deserialize_logos_theory_round_trips_through_semantic_package`, asserting
  `builder/serialize.py` correctly rehydrates logos classes via `importlib` after the
  `semantic.py` -> `semantic/` package split.

### Phase 19 (fold relevance into constitutive)
- `code/src/model_checker/theory_lib/logos/subtheories/constitutive/examples.py` -- absorbed all
  20 relevance examples (`REL_CM_1`-`11`, `REL_TH_1`-`9`), preserving the exact same 4 active
  entries in `example_range` (`REL_CM_1`, `REL_CM_2`, `REL_TH_1`, `REL_TH_2`).
- `code/src/model_checker/theory_lib/logos/subtheories/constitutive/RELEVANCE.md` -- relevance's
  ~20KB theoretical README, relocated from `relevance/README.md` with links and counts updated
  for its new home.
- `code/src/model_checker/theory_lib/logos/subtheories/constitutive/notebooks/relevance_examples.ipynb`
  -- relocated from `relevance/notebooks/`.
- `code/src/model_checker/theory_lib/logos/subtheories/relevance/` -- deleted entirely (operators,
  `__init__`, examples, tests; the boilerplate `notebooks/template.json` and `notebooks/README.md`
  were superseded by constitutive's equivalents rather than duplicated, since neither is
  referenced anywhere in code).
- `code/src/model_checker/theory_lib/logos/subtheories/__init__.py`,
  `code/src/model_checker/theory_lib/logos/{operators.py,__init__.py,examples.py}`,
  `code/run_tests.py`, `code/scripts/logos_solver_benchmark.py` -- every `'relevance'` call site
  removed or folded into constitutive's entry.
- `code/src/model_checker/theory_lib/tests/test_theory_conformance.py` -- removed
  `RELEVANCE_EMPTY_OPERATORS_XFAIL_REASON` and its conditional xfail branch.
- Swept and fixed ~9 documentation files with dangling `subtheories/relevance/` links.

### Phase 20 (bimodal: collapse the dual module identity)
- `code/src/model_checker/theory_lib/bimodal/semantic.py` (3194 lines) moved verbatim into
  `semantic/core.py`; `semantic/__init__.py` rewritten as a plain `from .core import ...`
  re-export, deleting the `importlib.util.spec_from_file_location` dynamic loader that used to
  execute `semantic.py` a second time under the synthetic module identity
  `bimodal_semantic_module`.
- `code/src/model_checker/theory_lib/bimodal/tests/unit/test_semantic_module_registration.py` --
  rewritten from "guard the synthetic-module-registration workaround" to "guard its absence and
  the resulting single class identity"; kept the pickle/`ProcessPoolExecutor` round-trip
  assertions (the actual `--maximize` acceptance criterion).
- **Deviation, discovered and fixed**: `witness_registry.py`/`witness_constraints.py`'s own
  pre-existing `from ...errors import ...` (3-dot relative import to the shared
  `theory_lib/errors.py`) broke scaffolded bimodal projects generated by
  `builder.project.BuildProject`, because a scaffolded project is one directory level shallower
  than a theory nested under `theory_lib`. Switched both files to absolute imports
  (`from model_checker.theory_lib.errors import ...`), mirroring the exact strategy the old flat
  `semantic.py` already used for its own witness imports. This restored 8 previously-passing
  builder tests that this normalization would otherwise have regressed. Confirmed (by direct
  reproduction) that exclusion and imposition have the identical, equally-latent scaffolding gap
  via `...errors`/`...logos.semantic`, pre-existing and untouched by this phase.
- Fixed an unrelated, pre-existing bug this investigation surfaced: `builder/strategies.py`'s
  exec-fallback exception handler called `PackageImportError(...)` with 4 positional arguments
  against a 3-argument constructor, raising a confusing `TypeError` that masked the real
  `ImportError`.
- Updated `builder/project.py`'s stale comment (claimed bimodal/logos "have not yet been
  normalized"; both now are) and `test_generated_projects.py`'s stale assertion that a flat
  `semantic.py` should exist in a scaffolded bimodal project.

### Phase 21 (bimodal: split into the canonical file set)
- `bimodal/semantic/core.py` (3192 lines, 3 classes, zero module-level helpers) split cleanly at
  each class boundary into `core.py` (`BimodalSemantics`), `proposition.py`
  (`BimodalProposition`), `model.py` (`BimodalStructure`), matching the layout established for
  imposition, exclusion, and logos. Each file's imports trimmed to only what it uses.
- `code/src/model_checker/theory_lib/bimodal/tests/conftest.py` -- new, matching exclusion's
  layout (`bimodal_theory`, three settings fixtures, `witness_registry`/`constraint_generator`).
- `test_theory_conformance.py`'s `SEMANTIC_PACKAGE_XFAIL_REASON` emptied; bimodal's remaining
  `ITERATE_MODULE_XFAIL_REASON` entry is untouched (that gap belongs to Phase 22).

## Decisions

- Bimodal's witness files use absolute imports for the shared `theory_lib/errors.py` dependency
  rather than the 3-dot relative form exclusion/imposition use, specifically because bimodal's
  witness files are exercised by scaffolded-project tests and exclusion/imposition's aren't
  (only by accident of an unrelated pre-existing registry-ordering bug). This is a deliberate,
  narrow deviation for a theory-specific reason, not a general policy change.
- Relevance's boilerplate `notebooks/template.json` and `notebooks/README.md` were not carried
  over verbatim (superseded by constitutive's near-identical equivalents) since neither is
  referenced anywhere in code; only the substantive theoretical README and the interactive
  notebook itself were preserved, per the plan's explicit "do not delete the scholarship"
  instruction.
- The oracle suite verification for Phase 21 was scoped to 279/550 tests (every file that
  directly exercises `BimodalSemantics`/`BimodalProposition`/`BimodalStructure`), not the full
  serial run, per the same sandbox constraint (`no pytest-xdist`, resource contention) that
  already kept Phase 2 `[PARTIAL]`.

## Plan Deviations

- **Phase 20 witness imports**: plan said "relative imports" for `witness_registry.py`/
  `witness_constraints.py`; implemented as absolute imports instead, for the reason above (see
  the phase's own "Deviation from plan" note in the plan file for full detail).
- **Phase 21 oracle suite**: ran 279/550 tests rather than the full suite, for the sandbox
  resource-contention reason already accepted for Phase 2.
- No other deviations; all other checklist items completed as specified.

## Verification

- Build: N/A (Python package, no build step)
- Tests: `code/src/model_checker/theory_lib/` (logos 495, bimodal 291, conformance 46 passed/2
  xfailed), `code/tests/test_layering.py` (4 passed), `code/src/model_checker/builder/` (580
  passed, 5 pre-existing failures unchanged, 2 xfailed) -- all green except the 5 briefed
  pre-existing failures (`test_theory_library_execution`, 2x `test_performance`,
  `test_find_next_model_basic`, `test_project_initialization_default`), none of which are new.
  `bash code/scripts/verify-refactor.sh --skip-oracle` passes all 7 steps after every phase.
  `code/scripts/compare_bimodal_baseline.sh` (with the archived baseline path): 0 regressions,
  43/43 passed. Oracle `bimodal_logic` suite: 279/550 run and green (see Phase 21 deviation note).
- Files verified: Yes

## Notes

Phases 22-26 remain: restore bimodal's `iterate.py` (git-archaeology restore-and-port from a
historic commit, plus a fresh `test_iterate.py`), flip conformance/layering fully green, doc
reconciliation, the full regression gate + wheel parity diff, and the ROADMAP.md update. Phase 22
is a meaningfully-sized unit of work (git show from `9b76ffa2^`, API porting to current
`iterate_example_generator` conventions, e2e test cleanup) flagged for a fresh dispatch rather
than continuing with reduced context budget in this one.
