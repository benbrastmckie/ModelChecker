# Phase 1 Handoff: Doc-flag extractor and allowed-token derivation

**Status**: COMPLETED

## What was built

`code/tests/cli/test_docs_flag_matrix.py` (new file):
- `_registered_option_strings()` / `_allowed_tokens()` — derived from `ParseFileFlags().parser._actions`, never hand-transcribed.
- `_DEV_CLI_WRAPPER_FLAGS = {'--iso-debug', '--load', '-load'}` with `test_dev_cli_wrapper_flags_still_exist()` guarding drift against `code/dev_cli.py`.
- `_iter_fenced_blocks` / `_iter_invocations` — fenced-block walker admitting bash/sh/shell/console/untagged blocks, backslash-continuation joining, prompt/env-prefix stripping, invocation-prefix matching, and truncation at shell control/redirection operators.
- `_extract_flag_tokens(command)` — long/short flag extraction plus unconditional multi-letter single-hyphen rejection (catches `-st`).
- 10 unit tests, all passing, covering every case named in the plan (valid invocation, python-block skip, pip/ls/apt non-invocations, piped-grep truncation, cProfile non-invocation, backslash continuation, `-st` rejection, `--iso-debug`/`-load`/`--load` allowlisting).

## Verification

`PYTHONPATH=code/src pytest code/tests/cli/test_docs_flag_matrix.py -v` — 10 passed.

Scope hypothesis confirmed: `git status --short` showed exactly one added path,
`code/tests/cli/test_docs_flag_matrix.py`.

## Deviations from plan

None. All Phase 1 tasks completed as specified.

## Next phase

Phase 2: add `_DOC_GLOBS` and `test_documented_flags_are_registered()` scanning the real
documentation tree, marked `xfail(strict=True)`, plus the violation inventory report.
