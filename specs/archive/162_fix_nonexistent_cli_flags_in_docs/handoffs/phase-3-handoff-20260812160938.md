# Phase 3 Handoff: Rewrite subtheory prose in WORKFLOW.md, PROJECT.md, GETTING_STARTED.md

**Status**: COMPLETED

## What was fixed

- `docs/usage/WORKFLOW.md`: replaced `--subtheory`/`-st` CLI examples with the real
  `logos.get_theory(subtheories=[...])` Python-API mechanism; relocated the automatic
  dependency-loading claim to describe `LogosOperatorRegistry.load_subtheory` correctly.
- `docs/usage/PROJECT.md`: same subtheory rewrite in two places (Quick Start and "Selecting Logos
  Subtheories" -> "Working with Logos Subtheories"); removed the fabricated
  `--test-all-settings` and `--benchmark` examples, replaced with the real `--maximize` example
  and a note that settings sweeps are done by editing `example_range` directly.
- `docs/installation/GETTING_STARTED.md`: same subtheory rewrite.

## Verification

- `grep -n "subtheory\|-st \|--benchmark\|--test-all-settings"` on the three files shows only the
  corrected prose (no remaining CLI-flag framing).
- `PYTHONPATH=code/src python -c "from model_checker.theory_lib import logos; logos.get_theory(subtheories=['modal'])"` -- OK.
- Doc-flag lint restricted to these three files: 0 remaining violations (was 15: WORKFLOW.md 4,
  PROJECT.md 8, GETTING_STARTED.md 3). Total inventory dropped from 46 to 31 remaining tokens.

## Deviations from plan

None. WORKFLOW.md had one additional `--subtheory` mention (Quick Command Reference, line ~410)
beyond the three the plan's Scope Hypothesis explicitly enumerated for that file; it was fixed as
part of the same rewrite since it is the same file already owned by this phase.

## Next phase

Phase 4 (wave 3, independent): `docs/usage/OUTPUT.md`, `docs/usage/TOOLS.md`.
