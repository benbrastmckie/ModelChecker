# Phase 6 Handoff: Rewrite settings/README.md flag documentation

**Status**: COMPLETED

## What was fixed

Whole-file pass on `code/src/model_checker/settings/README.md`:
- Fixed every hyphenated long-flag spelling to underscore form (`--print_z3`, `--print_constraints`,
  `--print_impossible`, `--non_empty`, `--non_null`, `--align_vertically`).
- Deleted the "Theory-Specific Flags" subsection's fabricated entries (`--coherence-check`,
  `--witness-optimization`, `--imposition-depth`, `--state-modification`, `-M`/`--M`) -- confirmed
  never real via `git log --all -S` per the plan.
- Documented the settings that genuinely exist as settings-dict keys instead of CLI flags: `M`
  (bimodal `DEFAULT_EXAMPLE_SETTINGS`), `save_output` (`SemanticDefaults.DEFAULT_GENERAL_SETTINGS`),
  `derive_imposition` (imposition `ADDITIONAL_GENERAL_SETTINGS`).
- Kept `--align_vertically`/`-a` documented as a real CLI flag (it genuinely is one), just with
  the corrected underscore spelling.
- Removed `-N` from the `./dev_cli.py` example and `--max-time=10000` from the debugging example
  (both settings-dict-only, no CLI equivalent).
- Removed `notebook` from `--save`'s documented formats (choices are `markdown`/`json` only) and
  the fabricated `--save-output` bullet.
- Left the "Implementing New Settings" Python illustrations (`--your-setting`, `--complexity-level`)
  untouched -- they are inside ` ```python ` blocks, correctly framed as instructional.

## Verification

- `grep -n -- '--print-z3\|--non-empty\|--non-null\|--align-vertically\|--print-constraints\|--print-impossible\|--max-time\|--M \|-N \|coherence-check\|witness-optimization\|imposition-depth\|state-modification\|save-output' code/src/model_checker/settings/README.md` -- no matches.
- Doc-flag lint restricted to this file: 0 remaining violations (was 12, matching the plan's
  Scope Hypothesis exactly). Total inventory dropped from 18 to 6 remaining tokens.
- `PYTHONPATH=code/src pytest code/tests/cli/ -q` -- 72 passed, 1 xfailed (green overall).
- `PYTHONPATH=code/src pytest code/src/model_checker/settings/tests/ -q` -- 24 passed (no
  regression to the settings package itself).

## Deviations from plan

None.

## Next phase

Phase 7 (wave 3, final independent phase): `code/docs/contracts/THEORY_LICENSING.md`,
`code/src/model_checker/theory_lib/docs/CONTRIBUTING.md`, `docs/installation/DEVELOPER_SETUP.md`,
and the `output/errors.py` source fix.
