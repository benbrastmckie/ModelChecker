# Phase 4 Handoff: Fix output and tools usage docs

**Status**: COMPLETED

## What was fixed

- `docs/usage/OUTPUT.md`: removed `--output-dir` from every example (auto-generated
  `output_<timestamp>/` is the only location; no CLI override exists), removed `--verbose` from
  example command lines, removed `notebook` as a documented `--save` value throughout (Jupyter
  Notebook Format section, Command-line Options block, Format Selection Guide table, Output
  Directory Structure diagram, all Practical Examples, Troubleshooting), resolved the bare-`--save`
  self-contradiction (deleted "Interactive mode - prompts for format", kept "Save in all formats"),
  and deleted the `--no-terminal` performance-tip bullet.
- `docs/usage/TOOLS.md`: applied the subtheory rewrite (`logos.get_theory(subtheories=[...])`)
  to its "Project Creation with Subtheories" section; removed `--verbose` and the fabricated
  `--save all --output-dir comparisons/` invocation, and removed the accompanying `--save
  notebook` line (unreachable from the CLI, same reasoning as OUTPUT.md).

## Verification

- `grep -n -- '--output-dir\|--verbose\|--no-terminal\|--save all' docs/usage/OUTPUT.md docs/usage/TOOLS.md` -- no matches.
- Doc-flag lint restricted to these two files: 0 remaining violations (was 10). Total inventory
  dropped from 31 to 21 remaining tokens.
- `PYTHONPATH=code/src pytest code/tests/cli/ -q` -- 72 passed, 1 xfailed (green overall).

## Deviations from plan

The plan's task list named only the "Jupyter Notebook Format" section for the notebook-as-format
removal; the fix was extended to every other `notebook`/`--save notebook` mention in OUTPUT.md
(Format Selection Guide, directory-structure diagram, Practical Examples, Troubleshooting) and to
the one `--save notebook` line in TOOLS.md, since `--save`'s `choices` are `['markdown', 'json']`
only and leaving a subset of these mentions would still misinform readers. This is a same-file,
same-task-family extension, not a scope change to a different file.

## Next phase

Phase 5 (wave 3, independent): `docs/architecture/PIPELINE.md`, `docs/architecture/SETTINGS.md`,
`docs/architecture/ITERATE.md`.
