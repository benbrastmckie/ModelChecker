# Phase 2 handoff: FreshInt replacement

**Status**: COMPLETED

## What was done

- Replaced all 14 `z3.Int('<name>')` bound-variable declarations with `z3.FreshInt('<name>')` in
  `code/src/model_checker/theory_lib/bimodal/operators.py` (`NecessityOperator`, `FutureOperator`,
  `PastOperator`, `UntilOperator`, `SinceOperator` -- both `true_at` and the still-live-at-this-point
  `false_at` methods).
- Added explanatory comments at the first site in each class (no task-number references).
- Re-ran the collapse census: the two `\Until`/`\Since` aliasing-defect survivors are confirmed
  gone. `G(G(p))` no longer returns a fast spurious `None` -- it now honestly times out.
  `Box(Box(p))` confirmed unchanged (still a genuine nested `ForAll`, both pre- and post-fix).

## Secondary finding (recorded, not in scope to fix)

`\Box(p) -> \Box(p)` (two *sibling*, not nested, `Box(p)` instances) stopped folding to a
constant post-fix. Root-caused: pre-fix, both instances' `z3.Int('nec_true_world')` interned to
the literal same term, making Z3's `simplify()` recognize `Implies(A, A)` trivially; post-fix,
`FreshInt` makes them alpha-equivalent-but-distinct terms, which `simplify()`'s local rewriting
does not recognize as tautological (a real solve still would). Not a soundness issue; not the
mechanism this task targets (Box remains confirmed immune to the nested-eval_time aliasing).
Full analysis in `evidence/post-fix-measurements.md`.

## Files modified

- `code/src/model_checker/theory_lib/bimodal/operators.py` (14 sites + explanatory comments)

## Files created

- `specs/139_.../evidence/post-fix-census.json`
- `specs/139_.../evidence/post-fix-measurements.md` (Phase 2 section; later phases append)

## Next phase (wave 3, parallel)

Phase 3: permanent anti-collapse structural regression guard (`test_encoding_nondegeneracy.py`).
Phase 4: rewrite `test_soundness_regression.py`'s defect-enshrining assertions.
Phase 5: resolve the dead `false_at` methods (deadness proof, then delete or keep-and-fix).
