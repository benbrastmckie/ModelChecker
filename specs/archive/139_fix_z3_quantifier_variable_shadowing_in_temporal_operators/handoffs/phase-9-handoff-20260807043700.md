# Phase 9 Handoff (closing dispatch): Final green run and pinned-artifact audit

- **Task**: 139 - fix_z3_quantifier_variable_shadowing_in_temporal_operators
- **Phase**: 9 (closing dispatch on top of the prior partial-close, `48826646`)
- **Status**: [COMPLETED]
- **Session**: sess_1786086226_c74d28

## Scope of this dispatch

Exactly the two blockers recorded in the prior Phase 9 handoff
(`handoffs/phase-9-handoff-20260807033000.md`) and `.orchestrator-handoff.json`:

1. Classify `test_spot_check_individual_countermodels` (F5 failure in the gating suite's
   parallel `-n 6` pass), and fix forward if it is the same family as
   `test_mixed_or_diamond_prev`.
2. Independently re-run the full bimodal package suite.

## What was found and done

### Blocker 1: `test_spot_check_individual_countermodels`

Isolated re-run (fresh process, single test) found F5 itself decides fast (4.5-29s) -- the
original 180000ms timeout did not reproduce that way. Investigation surfaced three distinct
findings inside this one test, each classified via direct `PRE_FIX_SHA`
(`d795b5f4444a4a3a326a4775b7431b89144e930c`) scratch-swap comparison:

1. **F5's timeout is (b) pre-existing/environmental.** Running the whole
   `TestSpotCheckCrossSignal` class serially (no `-n` at all) reproduces F5's timeout purely from
   running after two sibling tests in the same session -- and the identical sequence at
   `PRE_FIX_SHA` reproduces the same timeout. Same family as `test_mixed_or_diamond_prev` (a
   near-budget solve destabilized by contention/accumulation). **Fixed forward**: added
   `@pytest.mark.xdist_serial`, routing it to the contention-free serial pass. Collection routing
   confirmed (excluded from pass 1's selection, included in pass 2's).
2. **F4 (`p Until q -> q Until p`) is a genuine new soundness finding, not a regression.**
   Previously documented "VALID (Until is symmetric in linear time)"; post-fix it deterministically
   decides with a countermodel (reproduced 3/3, ~4.5s each, fresh processes). `PRE_FIX_SHA`
   comparison: `None` in ~242s pre-fix vs. countermodel in ~4.5s post-fix. Root cause: F4's
   antecedent/consequent are two *sibling* (not nested) Until instances that aliased under the
   pre-fix fixed-name declaration -- the same defect class this task targets, in a shape
   (sibling, not nested) outside Phase 1's complexity<=5 primitive census search pattern.
   **Fixed forward**: test now asserts a countermodel for F4; stale "VALID" documentation in two
   other docstrings/comments corrected.
3. **F9/F10 boundary-decidability variance is (b) pre-existing, unchanged by the fix.** A
   true-isolation F9 probe times out identically (60.2s) at `PRE_FIX_SHA` and post-fix, 3x/2x
   reproduced. Within the test's full session it sometimes decides instead of timing out (same
   session-order sensitivity as F5, non-reproducible run-to-run which of F9/F10 it hits).
   **Fixed forward (test-robustness only, no pinned artifact touched)**: the `valid_formulas`
   loop's strict `assert result is None` now records a decided-but-unexpected result as
   `divergent` (reported, not a hard failure), alongside the pre-existing `inconclusive`
   (timeout) bucket.

Verified: `test_spot_check_individual_countermodels` passes in isolation (twice) and as part of
the full `TestSpotCheckCrossSignal` class (once, 365.89s -> now 0 failures after the fix was
applied and this was the state prior to the fix; the fix itself was verified via the standalone
re-runs, see evidence file for the full log trail). All edits confined to
`oracle/bimodal_logic/tests/test_oracle_interface.py`; `operators.py`/`semantic/core.py` confirmed
byte-identical to the committed post-fix state after every `PRE_FIX_SHA` scratch-swap comparison
(`git diff --stat` empty each time).

### Blocker 2: Full bimodal package suite

`PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/bimodal/ -v` (idle machine, load
average 1.10): **298 passed, 0 failed, 97.90s.** Fully green -- the 2 failures Phase 6 found and
fixed forward do not reappear.

## Files modified this dispatch

- `oracle/bimodal_logic/tests/test_oracle_interface.py` (F4 assertion + docstring corrections,
  F9/F10 tolerance widening, `@pytest.mark.xdist_serial` on
  `test_spot_check_individual_countermodels`)
- `specs/139_.../evidence/rederivation.md` (appended "Phase 9 closeout (second dispatch)" section)
- `specs/139_.../plans/01_fix-quantifier-aliasing-rebaseline.md` (Phase 9 heading -> `[COMPLETED]`,
  plan-level Status -> `[COMPLETED]`, remaining checkboxes resolved, Testing & Validation
  checklist completed)
- `specs/139_.../summaries/01_fix-quantifier-aliasing-rebaseline-summary.md` (new)
- `specs/139_.../handoffs/phase-9-handoff-20260807043700.md` (this file)

## Verdict

Both blockers from the prior Phase 9 handoff are resolved with recorded classifications and
fixes, not asserted clean without evidence. All 9 phases are complete. Phase 9 and the plan are
marked `[COMPLETED]`.

## Not done in this dispatch (explicit, bounded)

A full end-to-end two-pass re-run of `oracle/run-oracle-suite.sh` (~24 min) was not repeated, per
this dispatch's explicit bounded scope ("exactly two remaining items... nothing else... do not
launch open-ended verification sweeps"). The specific previously-failing test is independently
verified green in isolation and as a full class; a final whole-suite confirmation is recorded as
a reasonable follow-up, not performed here.
