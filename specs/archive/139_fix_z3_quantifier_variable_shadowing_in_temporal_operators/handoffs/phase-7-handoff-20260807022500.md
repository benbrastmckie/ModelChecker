# Phase 7 handoff: exhaustive re-derivation scan

**Status**: COMPLETED

## What was done

The exhaustive complexity<=5 scan launched at the end of the prior dispatch
(`python oracle/scan_runner.py --timeout-ms 10000 --out-dir
specs/139_.../baselines/derivation-run/`) completed and wrote its `SCAN_COMPLETE`
marker before this dispatch began. Verified via the marker only (never PID liveness):

- `total_formulas`: 274 (matches the plan's expected 274)
- `conclusive`: 103
- `disagreements`: 0 (hard-constraint check passes -- sanctioned to proceed to Phase 8)
- `timeout_count`: 171
- `wall_clock_seconds`: 3549.99 (~59.2 min)
- Slowest conclusive solve: 10.094s at idx174 (1-based), ~0.94% over the nominal 10s budget on
  wall-clock elapsed time -- recorded as a materially thinner margin than the prior derivation's
  8.646s/13.5% headroom, not smoothed over.

**Pre-flight deviation, recorded not silently worked around**: the run was launched despite the
machine failing its idle pre-flight check (an unrelated `lean lake build` sustaining 500-1300%
CPU for roughly the first third of the run, easing partway through). `conclusive_count=103`
clears the plan's own ~95 stop-and-re-run floor, so the run is used as-is per the plan's own
sanctioned criterion -- not because the mixed-load condition is being minimized.

## Files modified

- Plan checklist updates only (Phase 7 tasks/verification marked complete with recorded
  evidence).

## Next phase

Phase 8: rebuild `known_conclusive_complexity5.json` and recompute
`MIN_CONCLUSIVE_GATING_FORMULAS` from this run's `progress.jsonl`.
