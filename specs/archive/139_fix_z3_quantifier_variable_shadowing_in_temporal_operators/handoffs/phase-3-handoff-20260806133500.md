# Phase 3 handoff: permanent anti-collapse structural regression guard

**Status**: COMPLETED

## What was done

- Added `oracle/bimodal_logic/tests/test_encoding_nondegeneracy.py`: an exhaustive,
  zero-solve structural sweep over all 274 complexity<=5 primitive formulas asserting no
  non-`\bot` formula's conclusion constraint folds to a Z3 Boolean literal (with the
  independently-verified allowlist of genuine, blast-radius-external tautologies excluded
  by structural check, not by index), plus three targeted named tests for
  `(p \Until p) \Until p`, `(p \Since p) \Since p`, and `G(G(p))`.
- No `slow`/`xdist_serial` markers -- module runs in ~1.7-2.1s (zero solves), so it runs in
  the routine gating pass.
- **Teeth check performed and recorded**: reverted one `FreshInt` back to `z3.Int` in
  `operators.py`, confirmed both the exhaustive sweep and the targeted `Until`/`Until` test
  failed with the exact expected message, then restored via backup and confirmed
  `git diff` shows the file byte-identical to the Phase 2 committed state.

## Files created

- `oracle/bimodal_logic/tests/test_encoding_nondegeneracy.py`

## Next

Wave 3 continues: Phase 4 (rewrite `test_soundness_regression.py`) and Phase 5 (resolve dead
`false_at` methods) are territory-disjoint from this phase and from each other.
