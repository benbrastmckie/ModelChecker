# Phase 8 handoff: manifest rebuild and gating floor re-derivation

**Status**: COMPLETED

## What was done

- Rebuilt `oracle/bimodal_logic/tests/data/known_conclusive_complexity5.json` from
  `baselines/derivation-run/progress.jsonl` (103 conclusive entries, 1-based `idx` converted to
  0-based `index`). Verified zero mismatches against the live enumerator
  (`_verify_manifest_matches_enumeration`-equivalent check) before installing.
- **Headline finding**: `conclusive_count` is unchanged in COUNT (103, same as the pre-fix
  manifest) but NOT unchanged in SET -- 7 formulas gained conclusiveness (indices 41, 153, 157,
  173, 217, 219, 254), 7 different formulas lost it (indices 19, 29, 45, 57, 143, 168, 246), net
  zero. Cross-referenced every changed index against `evidence/pre-fix-census.json` /
  `post-fix-census.json`: **none of the 7 losses were "conclusive-and-wrong"** via the aliasing
  collapse -- all are deeply-nested Box/quantified formulas that never folded to a Boolean
  literal in either version, and their loss is explained by the same Z3
  term-identity-simplification-shortcut-loss mechanism already documented for `Box(p)->Box(p)`
  (Phase 2) and `BM_CM_4` (Phase 6): a genuine solve-time cost, not a soundness correction. The 7
  gains are a mix of legitimate `\bot`-fold cases (structurally outside the defect's blast
  radius) and ordinary budget-boundary solve-time variance. Full per-formula table in
  `evidence/rederivation.md`.
- Recomputed `MIN_CONCLUSIVE_GATING_FORMULAS`: `103 - 3 = 100`, identical to the existing value.
  Rewrote its derivation comment in `test_cross_oracle_differential.py` to cite this run's own
  measurement (10.094s slowest solve, thinner margin than the prior 8.646s) rather than silently
  inherit stale numbers under an unchanged constant. `git diff` on that file touches only the
  `MIN_CONCLUSIVE_GATING_FORMULAS` assignment and comment block -- confirmed via `git diff
  --stat`.
- Did not touch `SELF_SCAN_SOLVE_TIMEOUT_MS`, `MIN_CONCLUSIVE_SCAN_FORMULAS`, or
  `_assert_scan_report`.
- Recorded the Task 137/BimodalHarness linkage status honestly (still unconfirmed;
  `bimodal_harness` importable only under `nix develop`, not checked against BimodalHarness
  directly in this dispatch either).
- **Verified the new manifest against a real solve**, not just structurally: ran
  `TestGatingConclusiveScan::test_known_conclusive_population_self_consistent` (re-solves all
  103 conclusive formulas). First attempt (concurrent with two other Z3-heavy background jobs
  this same dispatch launched) failed at 98/103 -- diagnosed as **self-inflicted contention**,
  not a real finding, and discarded. Re-ran in true isolation: **passed cleanly**, 103/103,
  176.10s.
- While investigating the earlier self-inflicted-contention failure, also resolved the two
  Phase-6-carried-forward unclassified oracle tests plus a third "(c) candidate" finding
  (`test_mixed_or_diamond_prev`) that had been left open in `evidence/post-fix-measurements.md`
  but not listed in the orchestrator handoff's blockers. See the Phase 8 commit and
  `evidence/post-fix-measurements.md`'s "Follow-up dispatch" section for full detail; summary:
  two environmental (pass cleanly in isolation), one genuine large solve-time regression
  (`test_mixed_or_diamond_prev`, 1.47s pre-fix to ~73s post-fix) fixed forward by widening its
  local timeout from 60000ms to 150000ms.

## Files modified

- `oracle/bimodal_logic/tests/data/known_conclusive_complexity5.json` (rebuilt)
- `oracle/bimodal_logic/tests/test_cross_oracle_differential.py`
  (`MIN_CONCLUSIVE_GATING_FORMULAS` comment only)
- `oracle/bimodal_logic/tests/test_oracle_interface.py`
  (`test_mixed_or_diamond_prev`'s local `timeout_ms`, fix-forward, not part of the plan's
  original Phase 8 file list but the same sanctioned category of change as Phase 6's `BM_CM_4`
  fix-forward)
- `specs/139_.../evidence/rederivation.md` (new)
- `specs/139_.../evidence/post-fix-measurements.md` (appended)
- `specs/139_.../plans/01_fix-quantifier-aliasing-rebaseline.md` (Phase 7/8 checklists)

## Next phase

Phase 9: full gating suite end-to-end run, full bimodal package suite, pinned-artifact audit,
anti-collapse guard confirmation, and final conclusive-rate write-up (the "well above 38.7%"
hypothesis is falsified on this measurement -- 37.6%, flat -- and must be reported as such).
