# Phase 9 handoff: final green run and pinned-artifact audit

**Status**: PARTIAL -- one unresolved failure, reported plainly, not forced green

## What was done

- Ran the full oracle gating suite end to end once external machine contention had cleared.
  **Pass 2 (serial, contention-free) is fully green**: 8/8 passed in 473.76s, including
  `TestGatingConclusiveScan` against the newly rebuilt Phase 8 manifest and unchanged floor.
- **Pass 1 (parallel, `-n 6`) had 2 failures**, investigated:
  1. `test_mixed_or_diamond_prev`: the Phase 8 widened timeout (60000->150000) held in isolation
     but not under `-n 6` contention. **Fixed forward** using the codebase's established
     mechanism: added `@pytest.mark.xdist_serial`, moving it to the contention-free serial pass.
     Verified via collection (correctly excluded from pass 1, included in pass 2) and a standalone
     re-run (70.84s, passed).
  2. `test_spot_check_individual_countermodels`: failed on **F5**, a *different* formula than
     Phase 6's F9 finding. **Left as an explicit, unresolved blocker** -- not independently
     classified (no isolated re-run, no `PRE_FIX_SHA` comparison) because a "stop new long-running
     verification, close out now" direction arrived mid-investigation. Recorded honestly, not
     asserted clean.
- Pinned-artifact audit: `PINNED OK` for all three Hard-Constraint artifacts, confirmed
  byte-identical to `PRE_FIX_SHA`.
- Anti-collapse guard: 4/4 passed, 4.18s, confirmed present in the gating (not-`slow`) selection.
- `disagreements == 0` confirmed throughout every scan-report-producing run this task performed.
- Final conclusive-rate comparison recorded: 103/274 (37.6%) both pre- and post-fix -- the "well
  above 38.7%" hypothesis is reported as **falsified**, not pursued by threshold adjustment. The
  fix is justified on soundness grounds, not throughput.
- Full bimodal package suite: **not independently re-run in this final dispatch** (explicit
  direction not to launch new long-running verification). Relying on Phase 6's already-established
  result (296 passed, 2 fixed-forward and confirmed green at the time). Recorded as a follow-up
  gap, not silently treated as done to Phase 9's own standard.

## Outstanding blocker

`test_spot_check_individual_countermodels` (F5, `oracle/bimodal_logic/tests/
test_oracle_interface.py::TestSpotCheckCrossSignal`) fails under the gating suite's parallel pass
(`-n 6`) at `TEMPORAL_SOLVE_TIMEOUT_MS=180000`. Not classified: could be the same
xdist-contention-inflates-a-near-budget-solve mechanism as `test_mixed_or_diamond_prev` (in which
case the same `xdist_serial` fix-forward would apply), or something else. Next session should
re-run it in true isolation and, if it fails there too, apply the `PRE_FIX_SHA` scratch-swap
methodology used throughout this task before fixing or asserting it clean.

## Files modified

- `oracle/bimodal_logic/tests/test_oracle_interface.py`
  (`test_mixed_or_diamond_prev`'s `@pytest.mark.xdist_serial`)
- `specs/139_.../evidence/rederivation.md` (Phase 9 section appended)
- `specs/139_.../plans/01_fix-quantifier-aliasing-rebaseline.md` (Phase 9 checklist, marked
  `[PARTIAL]`)

## Next steps

1. Classify `test_spot_check_individual_countermodels`/F5 (isolation re-run, then `PRE_FIX_SHA`
   comparison if it still fails).
2. Re-run the full bimodal package suite as a genuinely final Phase 9 check.
3. Update `code/docs/core/TESTING_GUIDE.md` section 8.8's baseline-derivation numbers (noted as a
   follow-up, out of this plan's file scope).
