# Phase 1 Handoff: Overhaul pyproject.toml

**Status**: COMPLETED
**Timestamp**: 2026-06-01

## What Was Done

Replaced the entire `code/pyproject.toml` with the bimodal-logic restructured version:
- name: model-checker -> bimodal-logic
- version: 1.2.12 -> 0.1.0
- description: updated to bimodal oracle description
- classifiers: updated to Python 3.10/3.11/3.12 with math/library topics
- keywords: replaced with bimodal-specific terms
- requires-python: >=3.8 -> >=3.10
- optional-dependencies [all]: removed (redundant with core deps)
- entry-points: added bimodal_harness.oracle_providers with z3_base
- testpaths: narrowed from theory_lib to theory_lib/bimodal/tests

## Verification

TOML parses correctly via tomllib. All assertions pass:
- name == 'bimodal-logic'
- version == '0.1.0'
- requires-python == '>=3.10'
- entry-points['bimodal_harness.oracle_providers']['z3_base'] == 'bimodal_logic.provider:Z3OracleProvider'
- testpaths == ['src/model_checker/theory_lib/bimodal/tests']

## Next Phase

Phase 2: Create bimodal_logic package stubs under code/src/bimodal_logic/
