# Phase 4 Handoff: Property-based tests and gate validation

**Task**: 112 - Fold/unfold formula normalization utilities
**Phase**: 4 of 4
**Status**: COMPLETED
**Timestamp**: 20260601-114359

## What was done

- Implemented `random_formula(atoms, max_depth, rng)` helper in the test file generating formulas with all 17 tags
- Added `TestRoundTrip` class with 6 property-based tests over 120 random formulas each:
  - `test_unfold_fold_roundtrip_100`: unfold(fold(unfold(f))) == unfold(f) for 120 formulas
  - `test_fold_idempotent_after_unfold`: fold(fold(unfold(f))) == fold(unfold(f)) for 120 formulas
  - `test_unfold_idempotent`: unfold(unfold(f)) == unfold(f) for 120 formulas
  - `test_unfold_produces_only_primitives`: all tags in unfold(f) are primitive for 120 formulas
  - `test_normalize_level0_equals_unfold`: normalize(f, 0) == unfold(f) for 60 formulas
  - `test_normalize_level3_equals_fold_unfold`: normalize(f, 3) == fold(unfold(f)) for 60 formulas
- Added `TestAllEnrichedPatternsFoldable` with 11 gate tests verifying fold recognizes all enriched patterns from their primitive expansions

## Verification results

- All 90 tests in `test_fold_unfold.py` pass
- All 451 bimodal unit tests pass (no regressions)
- Round-trip property: unfold(fold(unfold(f))) == unfold(f) verified for 120 random formulas
- Fold idempotency: fold(fold(unfold(f))) == fold(unfold(f)) verified for 120 random formulas
- All 11 enriched operators are correctly foldable from their primitive expansions

## Deviations from plan

None - all plan items completed as specified.
