# Phase 1 Handoff: Concurrency guard primitive (TDD)

- **Task**: 135 - Fix concurrent model building segfault
- **Phase**: 1 of 7
- **Status**: COMPLETED

## What was done

- Wrote `code/src/model_checker/models/tests/unit/test_concurrency.py` first and confirmed RED
  (`ModuleNotFoundError: No module named 'model_checker.models.concurrency'`) before creating
  the module.
- Created `code/src/model_checker/models/concurrency.py`:
  - `ConcurrentConstructionError(RuntimeError)` with a docstring stating the single-threaded
    -only contract, why it exists, and the remedy (build sequentially or one model per process).
  - `_ConstructionGuard` holding `_owner`/`_depth`, protected by a `threading.Lock` held only
    for the short check-and-set.
  - `single_threaded_construction()` context manager (acquire/release via `try/finally`
    semantics through `__enter__`/`__exit__`).
  - `guard_construction(func)` decorator, `functools.wraps`-preserving, built on the context
    manager.
- Confirmed GREEN: all 15 tests pass (`pytest .../test_concurrency.py -v` -> 15 passed).
- Confirmed dependency-free: `grep -nE "^import |^from " concurrency.py` shows only
  `functools`, `threading`, `typing` -- no `z3`, no `model_checker.models.semantic`/`structure`.
- Marked all Phase 1 plan checkboxes `[x]` and the phase heading `[COMPLETED]`.

## Plan deviations

None (implementation followed plan).

## Verification results

```
PYTHONPATH=code/src pytest code/src/model_checker/models/tests/unit/test_concurrency.py -v
=> 15 passed in 0.39s
```

## Resource note

Confirmed at Phase 1 start via `pgrep -af "run-oracle"` that `oracle/run-oracle-suite.sh` is
running concurrently in this repo. This phase used only a single lightweight pytest invocation
against one new, isolated test file (15 tests, 0.39s) -- well under the "single quick unit test"
threshold, not the heavy sampling that must wait for the oracle job to finish.

## Next phase

Phase 2: wire the guard into `SemanticDefaults`, `ModelDefaults`, and `ModelConstraints`
construction paths via `__init_subclass__` wrapping. Verification for Phase 2 runs the models,
iterate, bimodal, and logos unit suites -- still targeted node-id/package runs, not a full-suite
sweep, so should remain safe even if the oracle job is still active, but will re-check
`pgrep -af "run-oracle"` before running.
