# Phase 5 Handoff: Gate Re-Run and Final Outcome

- **Task**: 144 - fix_oracle_per_formula_solve_timeouts
- **Phase**: 5 of 5 (final)
- **Status**: COMPLETED. All 5 phases done. Task outcome: "measured, no lever found" (plan's own
  anticipated contingency).

## Summary

All three encoding-level candidates investigated in Phases 2-4 were rejected after rigorous
measurement (Phase 2: neutral/regressive on formulas outside the benchmark; Phase 3: regressive;
Phase 4: illegal Z3 pattern). The accepted encoding is byte-identical to the Phase 1 baseline
throughout, confirmed repeatedly via exact `rlimit count` reproduction.

Phase 5 ran the Step 6 gate twice: once standalone (green, load ~2.6) and once via the full
`verify-refactor.sh` (red at Step 6, load ~3.95) — a direct empirical confirmation of the research
report's diagnosis that these formulas sit at ~1.0x headroom and the gate's pass/fail outcome is
contention-sensitive. `disagreements=0` in every run; this is purely a performance/budget-margin
issue, never a soundness one.

See `specs/144_fix_oracle_per_formula_solve_timeouts/summaries/01_oracle-solve-cost-reduction-summary.md`
for the full write-up, headroom table, and all 5 new dead ends (7-11).

## Verification Performed

- `git diff` against the pre-task-144 commit confirms: no budget widened
  (`SELF_SCAN_SOLVE_TIMEOUT_MS`, per-solve `timeout_ms=` unchanged), no floor lowered
  (`MIN_CONCLUSIVE_SCAN_FORMULAS=90`, `MIN_CONCLUSIVE_GATING_FORMULAS=100` both pinned), no test
  xfailed/skipped, `ORACLE_PASS2_TIMEOUT` untouched, zero `oracle/` test files modified.
- `test_encoding_nondegeneracy.py` and `test_soundness_regression.py` both green at every
  encoding-touching checkpoint throughout the task.
- Full differential suite (`test_cross_oracle_differential.py`) run as part of both Step 6
  invocations in Phase 5; `disagreements=0` in both (pass and fail alike).
- `compare_bimodal_baseline.sh` (verify-refactor Step 7): 0 regressions.

## Artifacts Produced

- `bench_solve_cost.py` — reusable seeded measurement harness (available for any future
  investigation of this cost, including the profiling follow-up recommended in the summary).
- `baselines/01_solve-cost-baseline.json`/`.md` through `05_final-solve-cost.json`/`.md`.
- `baselines/02_phase2-patterns.json`/`.md`, `baselines/03_phase3-shift-grounding.json`/`.md`.
- `baselines/05_oracle-suite-run.txt` (green standalone gate run).
- `baselines/05_verify-refactor-run.txt` (full gate run, Step 6 red, all other steps green).
- Five inline dead-end comments (7-11) in `core.py`/`operators.py`, matching the codebase's existing
  style.
- `summaries/01_oracle-solve-cost-reduction-summary.md`.

## No Further Phases

This was the final phase. The task is complete in the sense that all planned investigation was
carried out faithfully and rigorously; the gate itself remains intermittently red under contention,
which is an honest, data-backed finding rather than an unmet obligation of this plan. See the
summary's "Recommendation for Future Work" for the suggested next step (a `smt.qi.profile`-based
follow-up), which is explicitly out of scope for this task.
