# Phase 3 Handoff: Pattern Annotations and Constraint Reordering

**Phase**: 3 - Pattern Annotations and Constraint Reordering
**Status**: COMPLETED
**Timestamp**: 2026-05-29T02:00Z

## What Was Done

### Applied (no regressions):
- Constraint reordering: world_interval before lawful in return list (already done in Phase 2)

### Reverted (caused regressions):
1. **lawful MultiPattern** (`MultiPattern(Select(world_function(w), t), Select(world_function(w), t+1))`):
   - Caused consistent regression on BM_CM_2 and BM_CM_4 (countermodel examples)
   - Root cause: MultiPattern requires both terms to be in the ground term set before firing.
     For countermodel examples (SAT), the solver needs to freely instantiate the lawful axiom
     to build the model. The pattern restriction caused missed instantiations.
   - Reverted.

2. **converse MultiPattern** (`MultiPattern(task_rel(w,d,u), task_rel(u,-d,w))`):
   - Caused consistent regression on BM_CM_4
   - Root cause: Same as lawful -- the biconditional requires instantiation in both directions,
     but the MultiPattern only fires when BOTH terms are already known. For countermodel search,
     the solver needs the converse axiom to propagate from one direction to the other.
   - Reverted.

## Deviations from Plan

- **lawful MultiPattern**: SKIPPED (caused regressions on BM_CM_2, BM_CM_4). The plan's
  risk mitigation "Test theorem examples that depend on lawful transitions" was incomplete --
  the problem manifests in countermodel (SAT) examples, not theorem (UNSAT) examples.
- **converse MultiPattern**: SKIPPED (caused regression on BM_CM_4).
- **Constraint reordering**: Completed in Phase 2 (world_interval before lawful).

## Conclusion

Pattern annotations on axioms governing the core world structure (lawful, converse) are
too risky because they restrict E-matching for countermodel search. The existing pattern
on `forward_comp` (from before Task 97) is safe because forward_comp fires on EXISTING
task_rel facts (not world structure). Axioms that build the model structure need free
instantiation.

## Final State: 43/43 tests passing

## Next Phase

Phase 4: Solver Configuration Guards
- Add qi.max_instances and max_memory guard to Z3 solver adapter
