# Phase 2 Handoff: Tier 1 Safe Changes

**Phase**: 2 - Tier 1 Safe Changes -- Tautology Removal and Array Inequality
**Status**: COMPLETED
**Timestamp**: 2026-05-29T01:30Z

## What Was Done

### Applied (no regressions):
1. Removed `classical_truth` tautology (lines 506-517 in original). This was `Or(P, Not(P))` -- always true by LEM, wasted E-matching budget.
2. Moved `world_interval` before `lawful` in the return list for better MBQI seeding.
3. Updated docstring to reflect removed constraints and new ordering rationale.

### Reverted (caused regressions):
- Array inequality for `world_uniqueness` was attempted: replaced ForAll/Exists with `world_function(w1) != world_function(w2)`. This caused 8 failures (all countermodel examples: MD_CM_1,2,4,5,6, BM_CM_1,2,4).
- Root cause: Z3 array disequality checks ALL indices in the array, not just those within both worlds' valid intervals. This conflicts with `valid_array_domain` constraints, leading to unsound world models.
- The original ForAll/Exists formulation was restored.

## Deviations from Plan

- **world_uniqueness array inequality**: SKIPPED (caused 8 regressions). The plan assumed semantic equivalence, but Z3 array disequality operates on the entire array rather than just the shared interval. Future work could explore a Skolem-based uniqueness constraint as an alternative.

## Final State: 43/43 tests passing

## Next Phase

Phase 3: Pattern Annotations and Constraint Reordering
- Add MultiPattern to lawful and converse constraints
- Note: reordering already partially done in this phase (world_interval moved)
