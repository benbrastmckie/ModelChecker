# Phase 4 Handoff: Register imposition and green its test suite

- **Task**: 120
- **Phase**: 4 of 5
- **Status**: COMPLETED

## What Was Done

- Added `'imposition',` to `AVAILABLE_THEORIES` in
  `code/src/model_checker/theory_lib/__init__.py`, with an inline comment matching the
  existing entries' style.
- Ran `PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/imposition -q`: all 110
  tests passed on the first run (9.90s) — no collection errors, no runtime failures.
- Confirmed registration consistency: `AVAILABLE_THEORIES == ['bimodal', 'logos',
  'exclusion', 'imposition']`, `discover_theories() == ['bimodal', 'exclusion',
  'imposition', 'logos']` (alphabetical, no orphans), and
  `from model_checker.theory_lib import imposition` resolves via the public API.

## Verification

- `PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/imposition -q` -> 110 passed.
- `AVAILABLE_THEORIES` and `discover_theories()` both report `imposition` consistently.

## Files Modified

- `code/src/model_checker/theory_lib/__init__.py` — appended `'imposition',` to
  `AVAILABLE_THEORIES`.

## Next Phase

Phase 5: consolidated verification — combined exclusion+imposition run, logos+bimodal
regression guard, final z3-residue sweep across both theories.
