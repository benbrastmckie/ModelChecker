# Phase 2 Handoff: Register exclusion and green its test suite

- **Task**: 120
- **Phase**: 2 of 5
- **Status**: COMPLETED

## What Was Done

- Added `'exclusion',` to `AVAILABLE_THEORIES` in
  `code/src/model_checker/theory_lib/__init__.py`, with an inline comment matching the
  existing entries' style.
- Ran `PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/exclusion -q`: all 143
  tests passed on the first run (13.93s) — no collection errors, no runtime failures. The
  Phase 1 port (solver abstraction + the `WitnessSemanticError` -> `WitnessError` fix) left
  no further API drift to triage.
- Confirmed registration consistency: `AVAILABLE_THEORIES == ['bimodal', 'logos',
  'exclusion']`, `discover_theories() == ['bimodal', 'exclusion', 'logos']` (alphabetical,
  no orphans), and `from model_checker.theory_lib import exclusion` resolves via the public
  API without warnings.

## Verification

- `PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/exclusion -q` -> 143 passed.
- `AVAILABLE_THEORIES` and `discover_theories()` both report `exclusion` consistently.

## Files Modified

- `code/src/model_checker/theory_lib/__init__.py` — appended `'exclusion',` to
  `AVAILABLE_THEORIES`.

## Next Phase

Phase 3: restore `imposition` from `abb3bf7d^` and apply the same porting recipe.
