# Phase 1 Handoff: Restore exclusion and port the solver abstraction

- **Task**: 120
- **Phase**: 1 of 5
- **Status**: COMPLETED

## What Was Done

- Restored `code/src/model_checker/theory_lib/exclusion/` from `abb3bf7d^` (via snapshot ->
  scoped `git checkout abb3bf7d^ -- <path>` -> `git stash pop`, per the task 119 process note,
  since the working tree had unrelated dirty state blocking a direct checkout).
- Ported all `import z3` occurrences (14 files, production + tests) to
  `from model_checker import z3_shim as z3`.
- Ported `z3.is_true`/`z3.is_false` call sites in the 3 production files that used them
  (`semantic/core.py`, `semantic/__init__.py`, `operators.py`) to bare `is_true`/`is_false`,
  adding `from model_checker.solver import is_true, is_false` (or `is_true` only where
  `is_false` was unused) to each.
- Removed `semantic_backup.py` (docstring-only stub) and `semantic_original.py` (1565-line
  pre-refactor monolith) as dead code — neither was imported anywhere in the tree.
- Fixed an unanticipated API drift: `semantic/core.py` imported `WitnessSemanticError` from
  the shared `theory_lib/errors.py`, which no longer exists there (current `errors.py`
  reparents `WitnessRegistryError`/`WitnessConstraintError`/`WitnessPredicateError` under
  `WitnessError` directly). Fixed in the theory code only (import/raise `WitnessError`
  instead); `theory_lib/errors.py` was not touched (outside this task's `file_scope`).

## Verification

- `grep -rn "^import z3$" code/src/model_checker/theory_lib/exclusion` -> no matches.
- `grep -rn "z3\.is_true\|z3\.is_false" code/src/model_checker/theory_lib/exclusion` -> no matches.
- `PYTHONPATH=code/src python -c "import model_checker.theory_lib.exclusion"` -> exit 0.
- `PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/exclusion -q --collect-only` ->
  143 tests collected, 0 collection errors.

## Files Modified

- `code/src/model_checker/theory_lib/exclusion/**` (restored + ported; see plan Phase 1
  checklist for the full deviation note)
- Removed: `code/src/model_checker/theory_lib/exclusion/semantic_backup.py`,
  `code/src/model_checker/theory_lib/exclusion/semantic_original.py`

## Next Phase

Phase 2: register `exclusion` in `AVAILABLE_THEORIES` and run the full test suite to green.
