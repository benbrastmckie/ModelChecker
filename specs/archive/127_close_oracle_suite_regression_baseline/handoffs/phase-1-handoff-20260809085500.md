# Phase 1 Handoff: Quiet-Machine Preflight and Pin Re-derivation

**Status:** COMPLETED
**Date:** 2026-08-09

## What was done

- Created `specs/127_close_oracle_suite_regression_baseline/run2/` scratch staging directory.
- Recorded quiet-machine "before" capture (`run2/machine-before-phase1.txt`): no foreign
  pytest/z3/actively-CPU-consuming lean-worker process; 1-min load average fluctuated 1.96-2.95
  across four samples (deviation from strict <2.0, attributed to concurrent Claude Code swarm
  sessions, not Z3/pytest contention). Judged acceptable for Phase 1's lightweight tasks; Phase 2
  will get a stricter dedicated check before the actual timed run.
- Confirmed `nix develop --command python3 -c "import xdist"` → `3.8.0`.
- Confirmed `nix develop --command python3 -c "import bimodal_harness"` → `OK`.
- Captured four collection counts (`run2/collection-counts.txt`): total 606, gating-parallel 594,
  gating-serial 10, exhaustive/slow 2. Sub-counts sum correctly (594+10+2=606).
- Captured guard inventory (`run2/guard-inventory.txt`): five ordered assertions at lines
  1399/1406/1419/1436/1454 in `test_cross_oracle_differential.py`; constants
  `SELF_SCAN_SOLVE_TIMEOUT_MS=10000`, `MIN_CONCLUSIVE_SCAN_FORMULAS=90`,
  `MIN_CONCLUSIVE_GATING_FORMULAS=100`, `MIN_CONCLUSIVE_TEMPORAL_BH_FORMULAS=45`; 4 xfail markers
  in `test_oracle_interface.py`, all `strict=True`; `KNOWN_EXTERNAL_DEFECTS.md` exists (8110
  bytes / 118 lines).
- Confirmed `specs/126_.../baselines/serial-rebaseline/` is still empty (removal deferred to
  Phase 8).

## Deviations

- Load-average quiet threshold: see above. Not a deviation from any pinned test value or
  assertion -- only from the numeric "<2.0" convenience threshold, and only for a phase that does
  not itself run CPU-bound work.

## Next phase

Phase 2: Gating-Suite Baseline Run Into Staging. Requires a fresh, stricter quiet-machine check
immediately before backgrounding the ~20-35 minute two-pass run.
