# Phase 5 Handoff: Re-pin and Re-scope `code/scripts/verify-refactor.sh`

**Status:** COMPLETED for its own scope (Steps 3, 5, 6 + header). One criterion unmet for reasons
outside that scope — see Deviations.
**Date:** 2026-08-09

## What changed

`code/scripts/verify-refactor.sh` only. Steps 1, 2, 4, 7 untouched.

- **Step 3**: `BASELINE_ORACLE_COUNT` 550 → 606, plus new `BASELINE_ORACLE_PARALLEL_COUNT=594`,
  `BASELINE_ORACLE_SERIAL_COUNT=10`, `BASELINE_ORACLE_SLOW_COUNT=2`, each with exact-equality
  semantics, plus a partition check that the three sub-counts sum to the total. `!=` was kept, not
  relaxed to `>=`.
  - **Latent bug caught here.** pytest prints `594/606 tests collected (12 deselected)` once a
    marker deselects anything, so the pre-existing `[0-9]+ tests? collected` extractor would have
    matched the **total** for every sub-count and the new pins would have silently checked
    nothing. The extractor now takes the numerator and is commented to say why.
- **Step 5**: `XFAIL_LINES=(767 942 1020 1133 1431)` and the line-number comparison deleted — the
  file it pinned now contains zero `xfail(` markers, so the check could not pass and no longer
  described the accommodation. Replaced with content-matched sub-checks 5a–5d (defect record
  exists; five guard assertions each exactly once and in order; four floor/budget constants at
  pinned values; all `xfail(` markers `strict=True`). Line numbers are used only for *relative*
  order, never pinned.
- **Step 6**: `PYTHONPATH=code/src python -m pytest oracle/bimodal_logic/tests/ -q` →
  `bash oracle/run-oracle-suite.sh`, with the runner's exit status captured explicitly so any
  non-zero exit (including the xdist preflight refusal) increments `FAILURES`.
- **Header**: rewritten to describe the seven checks as they now are.

## Verification

- `bash -n code/scripts/verify-refactor.sh` — clean.
- `grep -nEi 'task [0-9]|tasks [0-9]'` on both edited scripts — empty.
- Step 3 live against the real tree: total 606, gating-parallel 594, `xdist_serial` 10, slow 2 —
  matches all four pins and the partition check.
- Step 5 live: 5a, 5b, 5c, 5d all OK.
- **Negative test — all four required mutations detected, control passes.** Full messages are in
  the implementation summary and `run2/negative-test-results.txt`. Summary: (i) deleted assertion
  → 5b "appears 0 time(s) … expected exactly 1"; (ii) transposed pair → 5b two out-of-order
  failures; (iii) `MIN_CONCLUSIVE_GATING_FORMULAS` 100 → 1 → 5c "a floor or budget was changed";
  (iv) `strict=True` removed → 5d "only 3 of 4 … a non-strict xfail silently absorbs an XPASS".
- `git diff --stat -- oracle/ code/` shows only the intended Phase 5 change plus the earlier
  additive runner hook.

## Deviations

**`bash code/scripts/verify-refactor.sh --skip-oracle` exits 1, not 0.** Steps 1, 2, 3 and all of
Step 5 pass. The two failures are in Steps 4 and 7, which Phase 5 is explicitly instructed to
leave untouched, so they are not caused by this change:

```
FAIL: bimodal suite failed on both attempts                <- Step 4
FAIL: compare_bimodal_baseline.sh reported regressions     <- Step 7
```

Step 4 fails on both attempts — the retry allowance for the documented Z3-timing flake does not
absorb it — on
`code/src/model_checker/theory_lib/bimodal/tests/unit/test_bimodal.py::test_example_cases[BM_CM_1-example_case7]`,
with a fast deterministic `AssertionError: Test failed for example: BM_CM_1`, not a timeout. This
is very likely the same defect as the oracle-side
`test_regression_all_active_examples[BM_CM_1-example_case7]`, and is the better handle on it. Not
repaired here (explicit non-goal). Recorded in
`specs/126_refactor_repo_core_infrastructure_theory_lib/baselines/oracle-baseline-STATUS.md`.

## Next phase

Phase 7 (and Phase 4) are **BLOCKED on machine quietness**, not on any work product. A sibling
project's Lean toolchain was at 168–216% CPU when they came due (up to 776% earlier in the
session). Resume by re-checking quietness, then:

1. Phase 4: `nix develop --command bash oracle/run-oracle-exhaustive-scan.sh`, backgrounded,
   completion established from the `SCAN_COMPLETE` marker (never from PID absence).
2. Phase 7: `nix develop --command bash -c 'bash code/scripts/verify-refactor.sh'`, backgrounded.
   **Expected to fail** — at Step 6 (the adjudicated red gating suite) and now also at Steps 4 and
   7. Record verbatim; do not add `--skip-oracle` to make it green and do not report the gate as
   passing.
3. Phase 8: remove `run2/` and the empty `baselines/serial-rebaseline/`, then commit.
