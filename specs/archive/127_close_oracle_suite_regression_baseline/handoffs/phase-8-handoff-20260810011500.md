# Phase 4 + Phase 8 Handoff: Exhaustive Scan Blocked, Final Cleanup Landed

**Date:** 2026-08-10
**Phase 4:** `[BLOCKED]` — attempted twice, neither attempt adjudicable, neither complete.
**Phase 8:** `[COMPLETED]` — cleanup done, final commit landed.

## Phase 4: two launches, no adjudicable result

| Attempt | Window | Quiet at launch | Outcome |
|---|---|---|---|
| 1 | 22:54:17–23:06:28Z | load 1.05–1.14, no foreign hog | Contention at **+85 s** (cslib `lean --worker` 291–467%, `lake` 794.8%, load1 → 11.52). Aborted at formula 14/274. |
| 2 | 23:57:44–~01:07Z | load 0.57–0.76, **zero** lean/lake processes | Clean ~46 min through formula ~215, then `lean --worker` 285%/205% transient and ~100% **sustained** from 00:47, a second from 00:58. Terminated at formula 234/274 (~85%). |

`SCAN_COMPLETE` and `report.json` were never written in either attempt.

**Why `[BLOCKED]` and not `[COMPLETED]` via the red/incomplete branch.** That branch requires
recording the outcome to Phase 3(c)'s honesty standard *including the quiet-machine captures*. An
unadjudicable run cannot supply those. Recording an environmental non-result there would
misrepresent it as a category (c) finding about the scan — the precise dishonesty this plan's
rubric exists to prevent.

**`baselines/exhaustive-scan/` was deliberately NOT created**, per this plan's Phase 8: nothing is
to be manufactured to fill the gap. Its absence is the honest state. The 2 `slow` tests remain
unaccounted for.

Neither run's per-formula outcomes were triaged or promoted — the scan's verdict per formula is
decided by a 10 s wall-clock solve budget, exactly what contention corrupts. `SELF_SCAN_SOLVE_TIMEOUT_MS`,
`known_conclusive_complexity5.json`, and `ORACLE_EXHAUSTIVE_TIMEOUT` are untouched.

## Phase 8: cleanup

- Removed `specs/127_close_oracle_suite_regression_baseline/run2/` (scratch). `run/` left intact.
- Removed the empty `baselines/serial-rebaseline/`.
- Removed this session's two gitignored `oracle/scan-results/` scratch dirs, whose partial
  progress the record explicitly designates as not-evidence. The pre-existing
  `20260807T155847Z` dir was left alone.
- **Deviation (addition, not omission):** promoted the evidence the committed record cites out of
  `run2/` into `baselines/gate-run-2026-08-09/` **before** deleting it, with a README. Without
  this, `oracle-baseline-STATUS.md` would cite deleted files. All `run2/` references in that file
  were repointed; zero remain.
- Staged narrowly. This branch has concurrent-session activity in `specs/116`, `specs/129`,
  `specs/138`, `specs/events.jsonl`, and `specs/.orchestrator-multi-state.json`; none was staged.

## State at handoff

- Phases 1, 2, 3, 5, 6, 7, 8 `[COMPLETED]`; Phase 4 `[BLOCKED]` (environmental).
- Gating oracle suite: **RED**, category (c), now on two independent adjudicable runs.
- `git diff --stat -- oracle/bimodal_logic/` empty. No budget, floor, marker, guard, or xfail
  strictness changed anywhere.

## Follow-up

1. Run the exhaustive scan on a host with no Lean or Z3 workload; only then adjudicate
   `test_complexity_5_scan_self_consistent`.
2. Diagnose `test_mixed_and_all_future_neg` (new) and `test_mixed_and_box_next`, both
   `OracleTimeoutError` at 60000 ms, depth=1, M=3 — and why the failing set grew between two
   quiet-machine runs on an unchanged tree.
3. Diagnose `BM_CM_1`/`example_case7`, deterministic and fast in-package, which also causes
   Step 7's misleading "reported regressions" via `set -euo pipefail`.
4. Diagnose why `test_temporal_propositional_interleaving` does not terminate within 900 s.
