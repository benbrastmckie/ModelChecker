# Phase 3 Handoff - 20260724T084915Z

## Immediate Next Action
Phase 3 complete; proceed to Phase 4 (add pytest-xdist dev extra, document `-n auto`, final
verification).

## Current State
- **Files**: code/pyproject.toml (`testpaths`), code/tests/e2e/test_simple_output_verify.py
  (deleted), code/src/model_checker/builder/tests/integration/test_interactive.py (deleted),
  code/src/model_checker/theory_lib/tests/unit/test_error_handling.py (repaired)
- **Work state**: `testpaths = ["tests", "src/model_checker"]`.
  `PYTHONPATH=code/src pytest code/tests/ code/src/model_checker --collect-only -q` reports
  2095 tests collected, 0 errors.

## Key Decisions Made
1. Deleted `test_simple_output_verify.py` and `test_interactive.py`: their referenced classes
   (`ModelDataCollector`, `SequentialSaveManager`, `ConsoleInputProvider`) do not exist anywhere
   in the source tree — no restored equivalent to repair against.
2. Repaired `test_error_handling.py` in two passes: first `WitnessSemanticError` → `WitnessError`
   (per plan), then — after re-collecting surfaced 4 more absent names not in the plan's list —
   rewrote `TestImpositionErrorHandling` against the real `SemanticError` base and deleted
   `TestLogosErrorHandling` outright (no `Logos*` classes exist).

## What NOT to Try
1. Fixing `builder/module.py`'s `_initialize_output_management()` runtime `ImportError` (same
   missing `SequentialSaveManager`/`ConsoleInputProvider` names): it's a runtime bug (deferred
   import), not a collection error, and out of `file_scope`/Non-Goals — flagged for the
   downstream green-gate task instead.
2. Assuming the plan's 3-error list was exhaustive: re-run `--collect-only` after every fix — a
   second wave of errors appeared only after the first `WitnessSemanticError` fix.

## Critical Context (max 5 items)
- `iterate/graph.py` imports `networkx` unconditionally (Phase 1 context, still relevant).
- `WitnessError`/`WitnessRegistryError`/`WitnessConstraintError` do NOT auto-set `theory=` in
  their constructors (pre-existing test-content mismatch, left as-is — runtime, not collection).
- Parent-plan's originally-flagged files (`test_model_building_sync.py`, `test_system_imports.py`,
  `tests/utils/helpers.py`) all still collect cleanly, untouched.

## References (read only if stuck)
- Plan: specs/121_restore_package_identity_and_repair_test_infrastru/plans/01_restore-package-identity-test-infra.md, Phase 3
- errors.py: code/src/model_checker/theory_lib/errors.py (full available exception list)
