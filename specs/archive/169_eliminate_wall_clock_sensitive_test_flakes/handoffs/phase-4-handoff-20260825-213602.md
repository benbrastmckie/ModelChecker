# Phase 4 Handoff: Redesign the Repeated-Operation Timing Assertions

**Status**: [COMPLETED]

## What changed

`code/src/model_checker/builder/tests/e2e/test_project_edge_cases.py`:

- Added module-level constants `WARM_ITERATION_MAX_SECONDS = 10.0` and
  `WARM_ITERATION_SLACK_SECONDS = 2.0`, and a standalone
  `assert_warm_iterations_consistent(testcase, warm_times)` helper implementing the redesigned
  two-bound check: absolute ceiling plus `max(warm_times) < median(warm_times) + slack`.
- `test_repeated_project_operations_maintain_consistent_performance` now runs one explicit
  discarded warm-up iteration before the measured loop, then calls the helper instead of the
  removed `max_time / min_time` ratio assertion.
- `test_multiple_project_generation_completes_within_reasonable_time`: reviewed per plan
  instruction, left unchanged (already absolute-only; marker application deferred to Phase 5) —
  recorded here as the deliberate no-op the plan calls for, not a silent skip.
- Added `import statistics` and a module-level `import time` (test method still has a
  now-redundant local `import time` in the untouched sibling method; left alone to avoid
  unrelated churn in a no-op test).

## RED/GREEN confirmation (Task 1)

Confirmed via injected sample data before wiring into the test method:

| Sample | Shape | Result |
|---|---|---|
| `[0.29, 0.28, 0.30, 5.0, 0.31]` | synthetic cold-start-heavy / regression spike | **FAILS** (correctly flags a real regression) |
| `[0.0026, 0.0019, 0.0018, 0.0017, 0.0018]` | realistic (actual measured `BuildProject.generate()` timings) | **PASSES** |
| `[0.087, 0.005, 0.006, 0.0055, 0.0058]` | historical failure's shape: ratio ~17.4, all well under 10s | **PASSES** (proves the redesign no longer reproduces the reported false positive) |

## Scope Hypothesis confirmation

`grep -rn "time_variation_ratio\|/ min_time\|max_time / min\|ratio.*time\b" code/ --include=*.py`
confirmed the removed ratio assertion was the only unbounded ratio-over-time assertion in test
code; the one other numeric hit (`logos_solver_benchmark.py`'s `time_ratio`) is a benchmark
script, not a test assertion, and out of scope.

## Verification

- `PYTHONPATH=code/src pytest code/src/model_checker/builder/tests/e2e/test_project_edge_cases.py -v`:
  10 passed, 8 subtests passed.
- Ran `TestPerformanceAndScalabilityScenarios` 10 consecutive times standalone: all green.
- `PYTHONPATH=code/src pytest code/src/model_checker/builder -n 6 -q` (full builder suite,
  contended): 236 passed.
- `grep -n "time_variation_ratio" code/`: no output (confirmed removed).

## Deviations from plan

None (implementation followed plan).

## Resume point for next dispatch

Eligible next: Phase 2 (depends on 1, done), Phase 3 (no deps), Phase 5 (depends on 4, now done).
Per cheapest-closure-first, Phase 2 (interface tier, 1.5h) is next since it is cheaper than
Phase 3 (full tier, 1.5h); Phase 5 remains eligible in parallel-wave terms but is sequenced after
Phase 2/3 in this single-dispatch ordering.
