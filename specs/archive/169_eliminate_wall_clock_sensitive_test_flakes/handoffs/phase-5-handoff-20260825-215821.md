# Phase 5 Handoff: Marker Taxonomy and Full Inventory Marking

**Status**: [COMPLETED]

## What changed

- `code/pyproject.toml`: registered `xdist_serial` in
  `[tool.pytest.ini_options].markers`, wording adapted from `oracle/conftest.py`'s
  identically-named marker.
- `code/src/model_checker/builder/tests/e2e/test_project_edge_cases.py`: `@pytest.mark.xdist_serial`
  applied at the class level to `TestPerformanceAndScalabilityScenarios` (covers
  `test_multiple_project_generation_completes_within_reasonable_time` and
  `test_repeated_project_operations_maintain_consistent_performance`), with a self-documenting
  comment.
- `code/src/model_checker/builder/tests/integration/test_performance.py`: `@pytest.mark.xdist_serial`
  on `test_module_loading_performance` and `test_serialization_performance`.
- `code/src/model_checker/builder/tests/unit/test_progress_bar_ordering.py`: `@pytest.mark.xdist_serial`
  on `test_freeze_complete_time_consistency`.
- `code/src/model_checker/builder/tests/unit/test_project_version.py`: `@pytest.mark.xdist_serial`
  on `test_version_detection_performance_is_reasonable`.
- `code/src/model_checker/builder/tests/unit/test_serialize.py`: `@pytest.mark.xdist_serial` on
  `test_serialize_semantic_theory_handles_large_operator_collections`.
- `builder/tests/test_refactoring_target_behavior.py::test_performance_improvement` left
  unchanged on `@pytest.mark.performance` (confirmed via diff: zero changes to that file).

## Verification

- Before-state (pristine, via `git stash` of the five marking edits + `pyproject.toml`):
  `pytest tests/ src/model_checker -m "not packaging and not performance and not unstable"
  --collect-only -q` -> **2295** selected.
- After-state (markers applied): `-m "xdist_serial" --collect-only -q` -> **7** selected, exactly
  the intended inventory (2 + 2 + 1 + 1 + 1 across the five files).
- After-state gating selection: `-m "not packaging and not performance and not unstable and not
  xdist_serial" --collect-only -q` -> **2288** = 2295 - 7. Delta matches the newly-marked count
  exactly, no more.
- `pytest ... -m "xdist_serial" -q`: 7 passed.
- Full test-file run of all five modified files: 51 passed.
- No unknown-marker warnings in any collect-only or run output.

## Deviations from plan

None (implementation followed plan).

## Concurrency note for the next dispatch

Between this dispatch's Phase 3 verification (full-suite background run) and this Phase 5 close,
a second commit (`63de5f78`, identical `task 169 phase 3: ...` message, same session id) and this
phase's uncommitted marking edits appeared in the working tree without this dispatch having made
them -- consistent with another agent instance concurrently resuming the same task under the same
orchestrator session. No conflicting edits were found (all changes were consistent with the
plan's own Phase 3/5 task lists), and this dispatch verified and closed both phases cleanly, but a
duplicate-dispatch review may be warranted if this recurs.

## Resume point for next dispatch

Wave 2 (phases 2, 5) is now fully closed. Wave 3's Phase 6 (CI Wiring, depends on Phase 5 -- done)
is the next eligible phase.
