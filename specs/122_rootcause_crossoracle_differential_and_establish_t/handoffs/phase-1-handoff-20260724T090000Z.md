# Phase 1 Handoff: Environment prep, results scaffold, and collection verification

- **Status**: COMPLETED
- **Branch**: task-117-restore-model-checker (confirmed)

## Work State

- Cleared stale `test_cross_oracle_differential.*.pyc` under `code/src/model_checker/theory_lib/bimodal/tests/unit/__pycache__/`.
- `pytest-xdist` was not importable in the base environment; `pip install -e 'code[dev]'` fails
  under this Nix-managed pip config (forced `--user`, no user site-packages). Installed only
  `pytest-xdist==3.8.0` + `execnet==2.1.2` via `pip install --no-user --no-deps --target=<scratchpad>/pylibs`
  to avoid shadowing the system `pytest==9.0.3` with the newer transitive `pytest==9.1.1` that a
  full dependency install would pull in. All task-122 pytest invocations must add
  `<scratchpad>/pylibs` to `PYTHONPATH` alongside `code/src` (see collection-counts.txt for the
  exact path). `pytest -n auto` smoke-verified green.
- Created `specs/122_.../baselines/` with `collection-counts.txt` and a `RELEASE-BASELINE.md` stub.
- Collection sanity checks:
  - In-package bimodal: 286 tests, 0 errors (down from 818 -- task 118 phase 5 relocated 7
    oracle-dependent files, not just the differential one; confirmed via `git show --stat 31b69077`).
  - Full suite (root-scoped `code/tests/ code/src/model_checker`, or `cd code/`): 2095 tests,
    0 errors -- matches task-121 baseline exactly.
  - **Correction**: a bare `pytest --collect-only -q` from the repo root does NOT pick up
    `code/pyproject.toml`'s testpaths (no config at repo root) and instead scans the whole repo
    incl. `code/boneyard/`, giving 2516/26-errors. All later full-suite phases (4, 5, 6) must use
    the root-scoped explicit paths or `cd code/`, never a bare root-level `pytest --collect-only`.
  - Oracle suite: 550 tests, 0 errors; `test_cross_oracle_differential.py` confirmed present there
    and absent from the in-package tree.

## Continuation Context

Phase 2/3 (wave 2) can proceed. Both must use `PYTHONPATH="code/src:<scratchpad>/pylibs"` (or the
oracle equivalent `PYTHONPATH="oracle:code/src:<scratchpad>/pylibs"`) for any pytest invocation
that uses `-n auto`. The pylibs path is:
`/tmp/claude-1000/-home-benjamin-Projects-ModelChecker/324bc632-4923-44c1-ae5e-b2f7a0399756/scratchpad/pylibs`
-- this is a session-scoped scratchpad directory, not part of the repo; downstream tasks/sessions
that don't have it must reinstall pytest-xdist+execnet the same way (documented above) rather than
assuming this path exists.

## Blockers

None.
