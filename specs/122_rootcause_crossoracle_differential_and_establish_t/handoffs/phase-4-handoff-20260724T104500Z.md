# Phase 4 Handoff: In-package bimodal suite green without BimodalHarness

**Task**: 122
**Phase**: 4 of 7
**Status**: COMPLETED

## What was done

Confirmed the in-package `theory_lib/bimodal` suite (286 tests) is fully green without
`BimodalHarness` on the path: 286 passed, 0 failed, 0 errored. This is a clean result versus
the task-118 baseline of 818/813/5 -- the difference (818 -> 286) is task 118's relocation of 7
oracle-dependent files (not just the 1 differential file), already documented in
`baselines/collection-counts.txt`.

Along the way, discovered and root-caused an `-n auto` (12-worker)-specific CPU-contention flake
in `test_bimodal.py::test_example_cases[BM_CM_1-example_case7]`: it failed identically in two
consecutive full-suite `-n auto` runs (a Z3 solve pushed from ~10s to ~15s under 12-way
contention), but passed 3/3 in isolation and 43/43 when only its own file ran under `-n auto`.
Adopted `-n 6` for the full bimodal suite per the plan's own risk-mitigation guidance; this
produced 286/286 passed at 43.43s. Full analysis in `baselines/bimodal-tally.md`.

## Files touched

- `specs/122_.../baselines/junit-bimodal.xml`, `bimodal-run.txt` -- definitive `-n 6` green run.
- `specs/122_.../baselines/junit-bimodal-attempt1-flaky.xml`, `bimodal-run-attempt1-flaky.txt`,
  `junit-bimodal-attempt2-flaky.xml`, `bimodal-run-attempt2-flaky.txt` -- preserved `-n auto`
  attempts showing the flake.
- `specs/122_.../baselines/bimodal-tally.md` -- new, full root-cause + tally record.
- `specs/122_.../plans/01_rootcause-differential-green-gate.md` -- Phase 4 tasks checked off,
  heading marked `[COMPLETED]`.

## Deviations from plan

- Phase 4's heading was found already marked `[IN PROGRESS]` at the start of this dispatch (a
  prior session apparently marked it in-progress without launching any run -- no
  `junit-bimodal.xml` existed and no pytest process was running). Treated as not-yet-started.
- Used `-n 6` instead of `-n auto` for the definitive record, per the plan's own contingency
  ("if flakiness appears, pin the affected tests to -n 0 or a single worker and record the
  reason") -- reduced worker count rather than pinning the single test, since the flake was
  full-suite-parallelism-specific, not intrinsic to the test itself.
- Also installed `pytest-xdist`/`execnet` into this session's own scratchpad
  (`/tmp/claude-1000/.../8bdc0147.../scratchpad/pylibs`), reusing the packages/versions from
  the Phase 1 deviation record but in a durable-for-this-session location.

## Next steps

Phase 5: relocated oracle suite (`PYTHONPATH=oracle:code/src`, `-n auto` or `-n 6` if the same
contention appears, backgrounded, `--junitxml`). Then Phase 6 (everything-else), Phase 7
(consolidated release baseline).
