# Phase 4 handoff: Allocate the discriminating follow-up as a specified task

**Status**: COMPLETED

## What changed

- `specs/state.json`: appended task 183 ("discriminate_gating_shortfall_axiom_vs_contention",
  status `not_started`, task_type `python`, topic `test-reliability`), consolidating report
  items 0a, 1, and 2. `next_project_number` incremented 183 -> 184. Allocated under the
  `specs/.scope-lock` mutex (acquired via `task-lock.sh scope-acquire`, released after the
  write) to guard against the two other concurrently-running implementation agents.
- `specs/TODO.md`: regenerated via `generate-todo.sh`; task 183 now appears.
- `specs/180_diagnose_oracle_conclusive_population_shortfall/reports/01_gating-conclusive-shortfall-diagnosis.md`:
  added one cross-reference paragraph under "What remains open" pointing items 0a/1/2 at task 183,
  noting item 3 was closed directly by this task's Phase 1 (not deferred), and item 4 needs no
  further task (it is the diagnosis's own recorded conclusion).

## Deliberately NOT done

No shortfall-remediation task was created. TESTING_GUIDE.md 8.9's standing-rule escalation trigger
(two review cycles / ~2 months with no active repair work) has not fired — the marking is ~1 week
old and repair work is actively in progress, per the plan's explicit instruction.

## Verification

- `jq '.next_project_number' specs/state.json`: 184 (was 183, +1).
- `jq '[.active_projects[].project_number] | length'`: 22 (was 21, +1).
- `jq -e '.active_projects[] | select(.project_number == 183) | .description | test("33494135668") and test("cfb9cb4a") and test("9ce3b4ad")'`: true.
- `specs/TODO.md` contains `### 183. Discriminate gating shortfall axiom vs contention`.
- `bash .claude/scripts/task-lock.sh check 183`: no task directory / lock exists for 183 (none was
  created — Phase 4 only allocates the state.json entry, per the plan's Files-to-modify list).
- `specs/.scope-lock` mutex directory absent after release — no lock left behind.

## Deviations from plan

None.
