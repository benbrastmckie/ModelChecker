# Phase 1 handoff: Correct and extend the marker's entry-criteria record

**Status**: COMPLETED

## What changed

`oracle/bimodal_logic/tests/test_cross_oracle_differential.py`, comment-only edit inside the
`GATING_RECHECK_SOLVE_TIMEOUT_MS` entry-criteria block (criteria (3), (3b), (4)):

- (3): added an "AXIOM AVENUE CLOSED" paragraph recording that `f9cc081e` is not an ancestor of
  `98d3ad8d` (all six run checkouts), scoped explicitly to those six runs, not HEAD (C7).
- (3b): replaced the false "identical 96/103, 7-timeout" claim with the real six-run table
  (96-98/103 conclusive, 5-7 timeouts), added the sixth (09-01) run, preserved the existing
  xdist_serial-closure sentence, added the classifier-laundering-bug explanation for why the
  per-node-id promotion streak reset every night (all six classified `NEW`, fixed in `cfb9cb4a`
  which never reached CI), added the axiom-bearing local data point (93/103, 951.21s) with its
  non-idle-host caveat kept in the same sentence, and added the pre-axiom-signature sentence from
  the report's (d).
- (4): confirmed both `compute_promotion_streak` and `compute_per_test_promotion_streak` exist in
  `.github/scripts/unstable_watch_classify.py` and that TESTING_GUIDE.md 8.9 describes the latter
  as driving promotion; repointed criterion (4) at the per-node-id function and noted the legacy
  function is retained as a job-level upper bound only.

## Verification

- `git diff -U0` on the file: comment lines only, zero executable-statement changes.
- `--collect-only -q`: 72 tests collected, no syntax break.
- `-m "not slow and not unstable" -q`: 69 passed, 3 deselected, 386.15s, exit 0.
- Grep confirms `GATING_RECHECK_SOLVE_TIMEOUT_MS = 40000` and `MIN_CONCLUSIVE_GATING_FORMULAS = 100`
  unchanged (C1, C2).
- `bash .claude/scripts/check-task-references.sh`: reports 109 pre-existing occurrences, all under
  `.opencode/**`, none in any file this phase touched — no new violation introduced by this phase.

## Deviations from plan

None. All six task items executed as specified.

## Next phase

Phase 3 depends on Phase 1's criterion (3) closing sentence about instrumentation
("enabling that instrumentation is a possible future round, not attempted here") remaining
unchanged until Phase 3 amends it — confirmed intact after this phase's edits.
