# Phase 2 handoff: Annotate TESTING_GUIDE.md 8.9's "Currently marked" entry

**Status**: COMPLETED

## What changed

`code/docs/core/TESTING_GUIDE.md`, section 8.9's "Currently marked" bullet for
`TestGatingConclusiveScan::test_known_conclusive_population_self_consistent`: appended one
pointer-shaped sentence noting the recorded signature was last confirmed against pre-axiom code
and that the first post-axiom real CI run must be checked against it explicitly. No numbers are
restated here — they live in the source-site block edited in Phase 1. The documented signature,
the four entry criteria, the exit-criteria paragraph, the 20-run default, and the standing-rule
paragraph are all byte-identical (confirmed by diff — the change is confined to the one bullet).

## Standing-rule justification (recorded here, not in the guide, per the plan's pointer-not-
restatement instruction)

The marking dates to `25eadae8` (2026-08-25), roughly one week old, well inside 8.9's
two-review-cycle window. The rule's joint "no active repair work in progress" condition is not
met either: repair work is live and partially landed (`cfb9cb4a`, the closed 2x budget-widening
avenue, the closed `xdist_serial` avenue, and the axiom avenue closed in Phase 1). Neither
de-quarantining nor re-quarantining is recommended (C4) — the marker stays exactly as-is.

## Classifier-lesson confirmation

8.9's "The classifier lives in an importable module, not YAML" paragraph (around line 997) already
documents the rendered-text/bare-substring guard rule and the sequential-assert source-listing
mechanism that `cfb9cb4a` fixed. No new guide text was needed or added for this lesson — confirmed
by reading that paragraph before editing, per the plan's instruction not to duplicate it.

## Verification

- `git diff code/docs/core/TESTING_GUIDE.md`: confined to the one bullet; entry criteria,
  exit-criteria paragraph, and standing-rule paragraph unchanged.
- `PYTHONPATH=code/src python3 -m pytest code/tests/ci/test_unstable_deselection_wiring.py code/tests/ci/test_unstable_watch_classifier.py -q`: 63 passed.
- `bash .claude/scripts/check-task-references.sh`: same pre-existing 109 `.opencode/**` occurrences, none in the edited file — no new violation.

## Deviations from plan

None.
