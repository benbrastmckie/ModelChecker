# Phase 3 handoff: Wire opt-in per-formula instrumentation into the gating test

**Status**: COMPLETED

## What changed

`oracle/bimodal_logic/tests/test_cross_oracle_differential.py`:

- New module-level helper `_resolve_scan_instrumentation(env_var_name)`, placed after
  `_write_completion_marker`: returns `(None, None, 0)` when the named env var is unset/empty,
  else `(Path(value), Path(value) / "progress.jsonl", 10)` — mirrors Decision D2's inline
  resolution verbatim in behaviour.
- Wired into `test_known_conclusive_population_self_consistent` via `ORACLE_GATING_SCAN_OUT_DIR`
  (distinct from `ORACLE_SCAN_OUT_DIR`), threading `progress_path`/`heartbeat_every`/`artifact_dir`
  into the existing `_generate_differential_report(...)` call. The `_assert_scan_report(...)` call
  itself is untouched (confirmed character-identical by diff).
- `test_complexity_5_scan_self_consistent`'s D2 block left completely unchanged (confirmed
  byte-identical to HEAD via `awk` range diff).
- Docstring paragraph added to the gating test explaining the env var, default-unchanged
  behaviour, and its purpose (recovering per-formula timeout identities).
- Criterion (3)'s closing sentence amended to record the instrumentation is now wired, opt-in,
  behind `ORACLE_GATING_SCAN_OUT_DIR`, distinct from `ORACLE_SCAN_OUT_DIR`.
- New `TestResolveScanInstrumentation` class (5 tests, Z3-free, `_StubOracle`-based): unset/empty
  env var defaults; set env var yields artifact dir + progress.jsonl + heartbeat=10; the two env
  vars resolve independently; unset-through-helper writes no files.

## Verification

- Collection: 77 tests collected (was 72; +5 new), no syntax break.
- `TestResolveScanInstrumentation` run standalone: 5 passed.
- Full fast subset (`-m "not slow and not unstable"`): 74 passed, 3 deselected, 393.20s, exit 0
  (log: scratchpad `phase3_fastsubset.log`), zero FAILED/ERROR lines.
- `_assert_scan_report(report, min_conclusive=MIN_CONCLUSIVE_GATING_FORMULAS)` call line: no diff
  (character-identical).
- `_assert_scan_report` function body: byte-identical to HEAD (awk range diff).
- `TestFullScanReport` (D2) class body: byte-identical to HEAD (awk range diff).
- `@pytest.mark.unstable` count: 2 (unchanged). `@pytest.mark.xdist_serial` count: 3 (unchanged).
- `grep -c ORACLE_SCAN_OUT_DIR`: file 10 (was 9; +1 from a docstring mention of the existing name,
  no code-site change), `run-oracle-exhaustive-scan.sh` 2 (unchanged).
  `grep -c ORACLE_GATING_SCAN_OUT_DIR`: file 20 (new), `run-oracle-exhaustive-scan.sh` 0 — the
  exhaustive script never references the new gating-only variable, so it cannot collide.
- `GATING_RECHECK_SOLVE_TIMEOUT_MS = 40000` and `MIN_CONCLUSIVE_GATING_FORMULAS = 100` unchanged
  (grep-confirmed).
- The long gating scan itself was NOT run in this phase or anywhere in this dispatch, per the
  plan's non-goals and the concurrent-host constraint.

## Deviations from plan

None. All seven task items executed as specified.
