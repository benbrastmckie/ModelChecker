# Phase 7 Handoff: Residual docs sweep and the output/errors.py source fix

**Status**: COMPLETED

## What was fixed

- `code/docs/contracts/THEORY_LICENSING.md`: deleted the fabricated "Automated License
  Generation" section (`--generate-license`, `--base-theory`, `--author`, `--theory-name` --
  no such automation exists anywhere in the source).
- `code/src/model_checker/theory_lib/docs/CONTRIBUTING.md`: replaced `model-checker -t
  my_test_name` with `PYTHONPATH=code/src pytest code/tests/ -k my_test_name`.
- `docs/installation/DEVELOPER_SETUP.md`: removed `./dev_cli.py --profile examples/slow.py`;
  pointed to the file's existing working `python -m cProfile -o profile.stats dev_cli.py ...`
  alternative.
- `code/src/model_checker/output/errors.py`: `OutputDirectoryError`'s permission-branch default
  suggestion no longer recommends the nonexistent `--output-dir` flag.
- Added `test_output_directory_error_suggestion_names_no_unregistered_flag()` to
  `code/tests/cli/test_docs_flag_matrix.py`, reusing `_allowed_tokens()` to assert no
  `OutputDirectoryError` suggestion string ever names an unregistered `--flag`.
- `code/README.md`'s `--iso-debug` mention was checked and already correctly framed as a
  `dev_cli.py`-accepted flag; no change needed.

## Deviation: xfail removed ahead of Phase 8

After this phase's markdown fixes, `_scan_doc_violations()` returned zero violations across all
204 scanned files (the last 6 tokens -- `THEORY_LICENSING.md` 4, `CONTRIBUTING.md` 1,
`DEVELOPER_SETUP.md` 1 -- were exactly this phase's own targets). With zero violations remaining,
`test_documented_flags_are_registered`'s `@pytest.mark.xfail(strict=True, ...)` decorator
immediately produced `XPASS(strict)`, which pytest reports as a hard FAILED, not a pass -- per
the plan's own Risk table ("`strict=True` makes the eventual xpass a hard failure, mechanically
forcing the Phase 8 flip"). Since every commit must land in a green state (git-workflow.md), the
xfail decorator's removal -- Phase 8's first task -- was pulled forward into this commit rather
than leaving `code/tests/cli/` red. This was the mechanically forced consequence the plan's Risk
table anticipated, not a scope decision. Phase 8's remaining tasks (final violation-inventory
counts, testing-guide note, full-suite run) are unaffected and still open.

## Verification

- `grep -n -- '--generate-license\|--base-theory\|--author\|--theory-name' code/docs/contracts/THEORY_LICENSING.md` -- no matches (the one remaining `--author` hit in that file, at a different line, is a `python test_package.py` invocation outside the guard's `model-checker`/`dev_cli.py`/`python -m model_checker` prefix scope and outside this phase's named targets).
- `grep -n "model-checker -t" code/src/model_checker/theory_lib/docs/CONTRIBUTING.md` -- no matches.
- `grep -n -- '--profile' docs/installation/DEVELOPER_SETUP.md` -- no matches.
- `PYTHONPATH=code/src pytest code/tests/ -q -k "output or errors"` -- 14 passed, 464 deselected.
- `PYTHONPATH=code/src pytest code/tests/cli/ -v` -- 74 passed, 0 xfailed, 0 xpassed (fully green;
  the guard is now a hard-failing test).

## Next phase

Phase 8: since the xfail removal already landed in this commit, Phase 8's remaining scope is the
final violation-inventory count update, a short testing-guide note describing the guard, and a
full-repository test-suite run to confirm no regressions.
