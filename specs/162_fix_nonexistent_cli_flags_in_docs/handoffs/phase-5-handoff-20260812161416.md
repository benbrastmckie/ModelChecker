# Phase 5 Handoff: Fix architecture docs

**Status**: COMPLETED

## What was fixed

- `docs/architecture/PIPELINE.md`: removed `--verbose --format json` from the CLI Flags example
  (replaced with `--save json`); removed `--verbose` from the CLI-flags diagram box (replaced
  with `--contingent`, preserving box alignment); removed `--verbose` from the debugging-example
  command line.
- `docs/architecture/SETTINGS.md`: removed the `• --verbose Overrides all other settings`
  diagram entry from the CLI Arguments precedence box.
- `docs/architecture/ITERATE.md`: removed the `DEBUG_CONFIG` dict's fabricated `'verbose': True`
  settings key; fixed the `# Debug messages (with --verbose)` comment to reference
  `MODELCHECKER_VERBOSE=true`; left the `./dev_cli.py --iso-debug` example unchanged and added a
  note that it is a `dev_cli.py` wrapper flag, not a `model-checker` flag.

## Verification

- `grep -rn -- '--verbose\|--format' docs/architecture/` -- no matches.
- `grep -n -- '--iso-debug' docs/architecture/ITERATE.md` -- still returns the preserved example.
- Doc-flag lint restricted to these three files: 0 remaining violations (was 3, all in
  PIPELINE.md). Total inventory dropped from 21 to 18 remaining tokens.
- `PYTHONPATH=code/src pytest code/tests/cli/ -q` -- 72 passed, 1 xfailed (green overall).

## Deviations from plan

None.

## Next phase

Phase 6 (wave 3, independent): `code/src/model_checker/settings/README.md`.
