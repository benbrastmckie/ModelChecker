# Phase 2 Handoff: Doc-scan test (RED demonstration) and violation inventory

**Status**: COMPLETED

## What was built

`code/tests/cli/test_docs_flag_matrix.py`:
- `_DOC_GLOBS`, `_iter_doc_files()`, `_scan_doc_violations()`.
- `test_documented_flags_are_registered()`, marked `xfail(strict=True)`, scanning the real
  documentation tree and asserting zero unregistered/fabricated invocation-line flag tokens.
- Sanity assertion that more than 50 files were scanned.

`specs/162_fix_nonexistent_cli_flags_in_docs/reports/02_violation-inventory.md`: full RED
inventory, 204 files scanned, 11 violating files, 46 violating tokens, grouped by owning phase
(3-7).

## Scope Hypothesis reconciliation

Plan predicted 10 files / ~47 tokens; actual is 11 files / 46 tokens. Difference is immaterial
(within prototype-estimate noise); no phase-to-file ownership changes were needed.

## Verification

- `PYTHONPATH=code/src pytest code/tests/cli/test_docs_flag_matrix.py -v` -- 10 passed, 1 xfailed.
- `PYTHONPATH=code/src pytest code/tests/cli/ -q` -- 72 passed, 1 xfailed (green overall).

## Deviations from plan

None material. See Scope Hypothesis reconciliation above for the one-file/one-token count delta.

## Next phase

Phases 3-7 (wave 3, may run in any order since they own disjoint file sets): fix the 46
inventoried violations plus the prose-only sites named in the plan's Risks table, and the
`output/errors.py` source-string fix in Phase 7.
