# Phase 5 Handoff: Full-gate verification and hard-constraint audit

**Status**: COMPLETED

## What was verified

- `PYTHONPATH=code/src pytest code/tests/ -v` — 666 passed, 5 skipped, 0 failed (178.40s).
- `PYTHONPATH=code/src pytest code/tests/ci/ -v` — 157 passed.
- Both gating `--collect-only` invocations reproduced against the research report's baseline:
  parallel pass `2133/2580 (447 deselected)` -> `2154/2601 (447 deselected)`; serial pass
  `9/2580 (2571 deselected)` -> `9/2601 (2592 deselected)`. Both invariant counts (447 deselected
  on the parallel pass, 9 selected on the serial pass) are byte-identical to baseline; the +21
  growth is exactly this task's 21 new hermetic unit tests (14 + 1 + 6), correctly picked up by
  the parallel pass's default selection and correctly excluded from the serial pass.
- Constraint audit against `git diff f5b6b190~1..HEAD` (the whole task's diff):
  - `-n 4` unchanged in `tests.yml` and `flake.nix` (parity checked). No `-n 6` reversion.
  - `timeout-minutes` byte-identical: `20` (general-tests), `30` (flake-check).
  - Zero `@pytest.mark.unstable`/`@pytest.mark.development` lines added anywhere in the diff.
  - Zero new `matrix.python-version` occurrences in `tests.yml` beyond the 3 pre-existing
    structural uses (job name, Python setup step); `TestSamplerIsNotMatrixGated` passing.
  - The sampler's 22 original tests (corrected count, see Phase 1) all present, unrewritten:
    per-class `--collect-only` sums to 22 exactly; the whole task's diff on the test file
    contains zero deletion lines (additions only).
  - No speculative remediation tokens (`--max-worker-restart`, memory cap, retry wrapper,
    `--dist` change, worker recycling) anywhere in the diff; the only "scheduler" hits are prose
    describing the verified xdist `LoadScheduling` algorithm, not a config change.
  - File scope: `git diff --stat` (excluding `specs/**`) touches exactly the four declared
    file-scope files, nothing else.
- Item D OPEN in every location: `TestRecordIntegrityItemDStaysOpen` (6 tests) passing, covering
  the sampler docstring and the `tests.yml` comment block; the bystander comment in
  `test_frame_class_mapping.py` also states "item D -- OPEN" directly (grep-confirmed).

## Correction made during this phase

The Phase 4 checklist and its handoff had miscounted the new `TestRecordIntegrityItemDStaysOpen`
guard class as "7 tests" — a `--collect-only` cross-check during this phase's constraint audit
showed it is actually 6 (the +21 total-collected arithmetic across both gating passes only
reconciled once this was corrected: 14 + 1 + 6 = 21, matching 2601 - 2580 = 21 exactly). Fixed
in both the Phase 4 handoff and the plan's Phase 4 checklist text; not amended into the already-
committed Phase 4 commit, per this codebase's "never amend, create a new commit" convention.

## Files touched

- `specs/174_root_cause_xdist_worker_crash/plans/01_xdist-crash-instrumentation-record.md`
  (Phase 5 heading -> COMPLETED, plan-level Status -> COMPLETED, Phase 4 count correction,
  Phase 5 checklist and Testing & Validation section fully ticked with results)
- `specs/174_root_cause_xdist_worker_crash/handoffs/phase-4-handoff-20260901T082000Z.md`
  (count correction only, post-commit)

## Outcome

All five phases complete. Root cause remains NOT identified; item D remains OPEN. The
instrumentation (worker-id attribution, 0.5s interval) and the durable honest record are landed
and gate-verified. No hard constraint was violated. See the implementation summary for the full
task-level account.
