# Phase 4 Handoff: The durable, executable honest record

**Status**: COMPLETED

## What was built

- `.github/scripts/worker_rss_sample.py`: module docstring rewritten. Opening line corrected
  (no longer claims "the Python 3.12 `general-tests` matrix leg" -- the sampler has run on every
  leg since `TestSamplerIsNotMatrixGated` landed). Hypothesis paragraph replaced with the current
  ledger: 4 confirmed observations (not 2), `gw2` x2 / `gw0` x1 (confounded) / a third `gw2`
  occurrence, observed on both Python 3.11 and 3.12; hypothesis 1 weakened not eliminated;
  hypothesis 1b (chunk-contiguous heavy-Z3 concentration, per the verified `LoadScheduling`
  source) newly articulated and explicitly UNTESTED; hypothesis 2 (3.12-only ABI) further
  weakened; hypothesis 3 unconfirmed either way; fixed-worker-binding weakened by mechanism.
  Added: lead (d) recorded as moot with reasoning, the containment-expiry note (bimodal's gating
  exclusion is containment/incidental/temporary, not a fix, with the expiry condition named),
  and the deferred-next-step note (worker-side `pytest_runtest_logstart` hook, deferred because
  `conftest.py` is outside this task's file scope). Explicit "Root cause: NOT identified. Item
  D: OPEN." statement retained/restated. The pre-existing "removable in one piece" note was
  preserved (a test's error message references it).
- `.github/workflows/tests.yml`: telemetry comment block rewritten to carry the same ledger
  summary and containment-expiry note, condensed for a comment context. Careful to never write
  the literal token `matrix.python-version` into the new prose (the `TestSamplerIsNotMatrixGated`
  trip risk the plan flagged), and left the "Deliberately non-gating" and "parallel-pass pytest
  invocation" paragraphs untouched.
- `code/src/model_checker/theory_lib/bimodal/tests/unit/test_frame_class_mapping.py`: added an
  "innocent bystander" header note pointing at the sampler docstring, citing the actual fixture
  shape (`N=2, M=2`, function scope, one `solver.check()` per test) and the 0.23s replacement-
  worker re-run. No marker added, no test changed.
- `code/tests/ci/test_worker_rss_sampler.py`: new `TestRecordIntegrityItemDStaysOpen` (7 tests)
  guarding BOTH the sampler docstring and the `tests.yml` comment block for: root-cause-open
  statement, containment-expiry note ("containment" + "not a fix" + re-admission language), the
  deferred worker-side-log next step, and a denylist of resolution-claiming phrases. Whitespace
  is normalized (`" ".join(text.lower().split())`) before multi-word phrase matching, since prose
  wraps across lines in the source and a naive substring check would false-negative on a
  mid-phrase line break -- caught and fixed during this phase (see Deviation below).

## Deviation from plan text (not from plan intent)

The plan's Phase 4 checklist item for the guard test did not anticipate that the docstring's own
prose line-wrapping could split a multi-word phrase (e.g. "containment, not a\nfix") across a
line boundary, making a naive `"not a fix" in doc` check fail even though the record correctly
states it. Fixed by normalizing whitespace in the guard test rather than fighting docstring line
widths. This was caught by actually running the new tests (RED) before declaring them done, not
assumed correct.

## Scope Hypothesis outcome

`grep -rn "3.12-only\|item D\|node down"` over the four in-scope files, run before starting this
phase, confirmed the only stale claim was the sampler docstring's "Python-3.12-only" framing
(now corrected) -- no other in-scope location carried a conflicting item-D status claim.

## Verification performed

- `PYTHONPATH=code/src pytest code/tests/ci/test_worker_rss_sampler.py -v` -- 43 passed (37 +
  6 new record-integrity tests... actually 7; see file for exact count).
- `PYTHONPATH=code/src pytest code/tests/ci/ -q` -- 157 passed (full CI-guard suite).
- Ran `TestSamplerIsNotMatrixGated` + `test_workflow_parity.py` + `test_unstable_deselection_wiring.py`
  immediately after the `tests.yml` comment edit (27 passed) -- per the plan's explicit
  per-edit-not-just-phase-end mitigation for this exact risk.
- `python3 -m py_compile .github/scripts/worker_rss_sample.py` -- OK.
- `PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/bimodal/tests/unit/test_frame_class_mapping.py --collect-only -q`
  -- 14 tests, unchanged before/after.
- `grep -c "development\|unstable" .../test_frame_class_mapping.py` -- 0, unchanged before/after.
- `git diff --stat .../test_frame_class_mapping.py` -- additions only (12 insertions, 0
  deletions).

## Files touched

- `.github/scripts/worker_rss_sample.py`
- `.github/workflows/tests.yml`
- `code/src/model_checker/theory_lib/bimodal/tests/unit/test_frame_class_mapping.py`
- `code/tests/ci/test_worker_rss_sampler.py`
- `specs/174_root_cause_xdist_worker_crash/plans/01_xdist-crash-instrumentation-record.md`
  (Phase 4 heading -> COMPLETED, checklist items ticked)

## Next phase

Phase 5 (full-gate verification and hard-constraint audit) is next, depends on Phase 4
(satisfied). Run `PYTHONPATH=code/src pytest code/tests/ -v` (full suite) and
`code/tests/ci/ -v`, reproduce both gating `--collect-only` counts from the research report,
and mechanically audit all six hard constraints against `git diff` for the whole task.
