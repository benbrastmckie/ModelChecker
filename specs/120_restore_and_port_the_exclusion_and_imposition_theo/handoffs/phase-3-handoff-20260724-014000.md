# Phase 3 Handoff: Restore imposition and apply the porting recipe

- **Task**: 120
- **Phase**: 3 of 5
- **Status**: COMPLETED

## What Was Done

- Restored `code/src/model_checker/theory_lib/imposition/` from `abb3bf7d^` (snapshot ->
  scoped checkout -> stash pop, same process as Phase 1).
- Ported all `import z3` occurrences (production + tests) to
  `from model_checker import z3_shim as z3`.
- Ported `z3.is_true`/`z3.is_false` in the one production site (`semantic/model.py`) and in
  `tests/unit/test_model.py`'s local `evaluate_z3_boolean` closures and
  `unittest.mock.patch('z3.is_true'|'z3.is_false')` targets (moved-symbol test adjustment,
  patch targets repointed at the test module's own imported `is_true`/`is_false` names).
- Verified all `logos` and `iterate` dependency imports resolve unchanged against the
  current API (direct `python -c` check of every exact import statement used in
  `operators.py`, `semantic/model.py`, `semantic/core.py`, `__init__.py`).
- Confirmed `examples_refactored/` is clean (no `import z3`, no `theory_lib.errors` usage).
- Fixed two unanticipated API drifts beyond the solver abstraction (same risk category as
  Phase 1's `WitnessSemanticError`):
  1. `ImpositionSemanticError`/`ImpositionOperationError`/`ImpositionHelperError` were removed
     entirely from the shared `theory_lib/errors.py` (not reparented, unlike the `Witness*`
     hierarchy). Fixed by using the existing `SemanticError` base class at each of the 4 raise
     sites (`semantic/core.py` x2, `semantic/helpers.py` x2), passing `theory="imposition"`
     explicitly and inlining the two `ImpositionHelperError` call sites' auto-generated
     messages (that class took a bare function-name argument `SemanticError` does not).
  2. The `ImpositionSemantics` protocol import from `theory_lib.types` (aliased
     `ImpositionSemanticsProtocol`) was removed from current `types.py`; the alias was unused
     in the file, so the import was simply dropped rather than substituted.
  `theory_lib/errors.py` and `theory_lib/types.py` themselves were left untouched (out of
  this task's `file_scope`).

## Verification

- `grep -rn "^import z3$" code/src/model_checker/theory_lib/imposition` -> no matches.
- `grep -rn "z3\.is_true\|z3\.is_false" code/src/model_checker/theory_lib/imposition` -> no matches.
- `PYTHONPATH=code/src python -c "import model_checker.theory_lib.imposition"` -> exit 0.
- `PYTHONPATH=code/src pytest code/src/model_checker/theory_lib/imposition -q --collect-only`
  -> 110 tests collected, 0 collection errors.

## Files Modified

- `code/src/model_checker/theory_lib/imposition/**` (restored + ported; see plan Phase 3
  checklist for the full deviation notes)

## Next Phase

Phase 4: register `imposition` in `AVAILABLE_THEORIES` and run the full test suite to green.
