"""CI-configuration contract tests: keep .github/workflows/tests.yml and flake.nix in sync, and
keep the wall-clock timing-marker taxonomy enforced against source.

See test_workflow_parity.py and test_timing_marker_coverage.py for the two guards.
"""
